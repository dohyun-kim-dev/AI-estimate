# 🤖 AI 프롬프트 가이드 - 효율적인 개발을 위한 질문법

## 📋 기본 원칙

### 1. 맥락(Context) 제공
```
❌ "이 버그 고쳐줘"
✅ "React TypeScript 프로젝트에서 GenericDataTable 컴포넌트의 allowWrap 속성이 true일 때 텍스트가 셀 밖으로 넘치는 문제를 해결해줘. 현재 CSS는 word-break: break-word로 설정되어 있어."
```

### 2. 원하는 결과 명시
```
❌ "테이블 수정해줘"
✅ "테이블에서 1) 프로필 컬럼을 두 번째로 이동 2) 상태 드롭다운을 마지막 컬럼으로 3) allowWrap true인 컬럼들의 텍스트 줄바꿈이 세로 가운데 정렬되도록 수정해줘"
```

### 3. 제약사항/요구사항 포함
```
✅ "기존 API 인터페이스는 변경하지 말고, styled-components만 사용해서 해결해줘"
✅ "타입 에러가 발생하지 않도록 하면서 이 기능을 추가해줘"
```

---

## 🎯 상황별 프롬프트 템플릿

### 🐛 버그 수정 요청
```markdown
**문제 상황**: [구체적인 현상 설명]
**발생 조건**: [언제, 어떤 상황에서 발생하는지]
**에러 메시지**: [콘솔 에러가 있다면 정확한 메시지]
**예상 원인**: [본인이 추정하는 원인]
**원하는 결과**: [어떻게 동작해야 하는지]
**제약사항**: [변경하면 안 되는 부분]

예시:
문제 상황: 상담요청 관리 페이지에서 처리상태 드롭다운 클릭 시 API 호출이 두 번 발생
발생 조건: StatusDropdown에서 옵션 선택 후 바로 다른 옵션 선택할 때
에러 메시지: 콘솔에 "처리상태가 변경되었습니다" 토스트가 두 번 표시
예상 원인: handleStatusChange 함수에서 디바운싱 처리가 안 되어 있는 것 같음  
원하는 결과: API 호출은 한 번만, 토스트도 한 번만 표시
제약사항: 기존 API 인터페이스와 컴포넌트 구조는 유지
```

### 🚀 새 기능 개발 요청
```markdown
**기능 개요**: [무엇을 만들고 싶은지]
**사용자 플로우**: [어떤 순서로 동작해야 하는지]  
**UI/UX 요구사항**: [디자인 관련 요구사항]
**API 연동**: [필요한 API가 있는지, 어떤 데이터를 주고받는지]
**예외 처리**: [에러 상황들과 어떻게 처리할지]

예시:
기능 개요: 견적서 다운로드 이력에서 엑셀 일괄 다운로드 기능 추가
사용자 플로우: 체크박스로 여러 항목 선택 → "선택 항목 엑셀 다운로드" 버튼 클릭 → 압축파일로 다운로드
UI/UX 요구사항: 테이블 첫 번째 컬럼에 체크박스, 상단에 "전체선택" 체크박스와 다운로드 버튼
API 연동: 기존 downloadEstimateExcel 함수 활용, 여러 ID 배열로 호출
예외 처리: 선택 항목 없을 때 경고, 다운로드 실패 시 토스트 에러 메시지
```

### 🔧 리팩토링 요청  
```markdown
**현재 문제점**: [코드의 어떤 부분이 문제인지]
**개선 방향**: [어떻게 개선하고 싶은지]
**성능/유지보수**: [어떤 측면에서 좋아져야 하는지]
**하위 호환성**: [기존 사용처에 영향이 없어야 하는지]

예시:
현재 문제점: GenericDataTable에서 formatter 함수가 매번 새로 생성되어 불필요한 리렌더링 발생
개선 방향: useMemo나 useCallback으로 최적화, 또는 formatter를 컴포넌트 외부로 분리
성능/유지보수: 리렌더링 줄여서 성능 개선, 코드 가독성 향상
하위 호환성: 기존 사용하는 모든 페이지에서 동일하게 동작해야 함
```

---

## 💡 효율성을 높이는 고급 기법

### 1. 배치 작업 요청
```
"다음 3가지를 한 번에 처리해줘:
1) inquiry 페이지의 상태 드롭다운을 pending/approved/rejected 3개로 변경
2) 각 상태에 맞는 색상 적용 (진행: 파란색, 완료: 초록색, 실패: 빨간색) 
3) API 호출 시 success 토스트 메시지 표시"
```

### 2. 선택지 제시 요청
```
"사용자 프로필 이미지 처리를 위해 3가지 방안을 제시해줘:
1) 성능 최적화 관점에서 가장 좋은 방법
2) 구현 난이도가 가장 낮은 방법  
3) 유지보수가 가장 쉬운 방법
각각의 장단점도 함께 설명해줘"
```

### 3. 코드 리뷰 요청
```
"이 컴포넌트 코드를 리뷰해줘. 특히 다음 관점에서:
- TypeScript 타입 안정성
- React 성능 최적화 
- 재사용성과 확장성
- 코드 가독성
개선점이 있다면 구체적인 수정 방법도 제시해줘"
```

---

## 🚫 피해야 할 나쁜 프롬프트

### ❌ 모호한 표현
```
"이거 좀 고쳐줘"
"에러 나는데 해결해줘"
"더 좋게 만들어줘"
```

### ❌ 정보 부족
```
"테이블이 이상해" (어떤 테이블인지, 무엇이 이상한지 불명)
"API가 안 돼" (어떤 API인지, 어떤 에러인지 불명)  
```

### ❌ 너무 추상적
```
"사용자 경험을 개선해줘" (구체적으로 어떤 부분을 어떻게 개선할지 불명)
"성능을 좋게 해줘" (어떤 성능 지표를, 어떤 방식으로 개선할지 불명)
```

---

## 📌 체크리스트

프롬프트 작성 전 다음을 확인하세요:

- [ ] 구체적인 파일명/컴포넌트명을 포함했는가?
- [ ] 현재 상태와 원하는 상태를 명확히 했는가?
- [ ] 제약사항이나 주의사항을 언급했는가?
- [ ] 관련된 에러 메시지나 코드를 첨부했는가?
- [ ] 한 번에 처리할 수 있는 적절한 범위인가?

---

## 🎯 실전 예시

### Before (비효율적)
```
사용자: "테이블 고쳐줘"
AI: "어떤 테이블의 무엇을 수정하실까요?"
사용자: "inquiry 페이지"  
AI: "구체적으로 어떤 문제가 있나요?"
사용자: "컬럼 순서가 이상해"
AI: "어떤 순서로 바꾸고 싶으신가요?"
```

### After (효율적)
```
사용자: "inquiry 페이지의 GenericDataTable에서 컬럼 순서를 'No → 프로필 → 이름 → 이메일 → 전화번호 → 처리상태' 순으로 변경하고, 처리상태 컬럼에 pending/approved/rejected 3개 옵션으로 드롭다운 수정해줘. API는 기존 updateEstimateRequestStatus 함수 사용."
AI: [바로 수정 작업 진행]
```

이렇게 하면 **5~10번의 왕복 대화**를 **1번의 명확한 요청**으로 줄일 수 있습니다.
