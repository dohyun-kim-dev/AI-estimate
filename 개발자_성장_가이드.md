# 📚 프론트엔드 개발자 필수 숙지사항

## 🎯 현재 레벨: Junior Frontend Developer (7개월차)

---

## 🔥 즉시 개선해야 할 핵심 역량

### 1. 🐛 디버깅 능력 강화 (최우선)

#### 문제 발생 시 체크리스트:
1. **콘솔 확인** → 에러 메시지 정확히 읽기
2. **네트워크 탭** → API 요청/응답 상태 확인  
3. **Elements 탭** → DOM 구조와 CSS 스타일 확인
4. **Sources 탭** → 브레이크포인트로 코드 흐름 추적

#### 자주 발생하는 에러와 해결법:
```typescript
// ❌ 타입 에러를 AI에게 바로 물어보기 전에
// ✅ 먼저 이것들을 확인하세요

// 1. 타입 정의 확인
interface User {
  name: string;
  email?: string; // optional인지 required인지
}

// 2. null/undefined 체크
const user = data?.user; // optional chaining 사용
if (user?.email) {
  // email이 존재할 때만 실행
}

// 3. 배열 메서드 사용 전 배열인지 확인
if (Array.isArray(users)) {
  users.map(user => ...)
}
```

### 2. 🏗️ 설계 사고력 개발

#### 기능 개발 전 필수 질문:
1. **데이터 흐름**: 어디서 데이터가 오고, 어디로 가는가?
2. **상태 관리**: 어떤 상태가 필요하고, 어디서 관리할 것인가?
3. **컴포넌트 분리**: 재사용 가능한 부분은 무엇인가?
4. **에러 처리**: 실패 시나리오는 무엇인가?

#### 실전 예시:
```typescript
// ❌ 바로 코딩 시작
const MyComponent = () => {
  // 코드 작성...
}

// ✅ 먼저 설계
/*
1. 데이터: API에서 사용자 목록을 받아옴
2. 상태: loading, error, users 필요  
3. 기능: 검색, 필터링, 페이지네이션
4. 에러: 네트워크 실패, 데이터 없음, 권한 없음
*/
const UserList = () => {
  const [users, setUsers] = useState<User[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // ...
}
```

---

## 💪 기술 역량 강화 로드맵

### Phase 1: 기초 다지기 (1개월)

#### TypeScript 마스터
- [ ] 기본 타입 (string, number, boolean, array, object)
- [ ] 인터페이스와 타입 별칭 차이점 이해
- [ ] 제네릭의 개념과 활용법
- [ ] 유틸리티 타입 (Pick, Omit, Partial, Required)

```typescript
// 실무에서 자주 쓰는 패턴들
interface ApiResponse<T> {
  statusCode: number;
  data: T;
  message: string;
}

type UserFormData = Pick<User, 'name' | 'email'>;
type PartialUser = Partial<User>;
```

#### React Hooks 깊이 이해
- [ ] useState의 동작 원리와 상태 업데이트 타이밍
- [ ] useEffect의 의존성 배열과 정리 함수
- [ ] useMemo와 useCallback의 사용 시점
- [ ] 커스텀 훅 만들어보기

```typescript
// ❌ 잘못된 useEffect 사용
useEffect(() => {
  fetchData();
}, []); // fetchData가 바뀔 수 있는데 의존성에 없음

// ✅ 올바른 사용
const fetchData = useCallback(async () => {
  // API 호출
}, [dependency]);

useEffect(() => {
  fetchData();
}, [fetchData]);
```

### Phase 2: 심화 학습 (2개월)

#### 상태 관리 패턴
- [ ] 언제 useState vs useReducer vs 전역 상태 관리를 쓸지 판단
- [ ] Context API의 적절한 사용법
- [ ] 상태 구조 설계 방법

#### 성능 최적화
- [ ] React.memo 사용법
- [ ] 불필요한 리렌더링 찾고 해결하기
- [ ] 코드 스플리팅과 lazy loading

```typescript
// 성능 최적화 예시
const ExpensiveComponent = React.memo(({ data, onUpdate }) => {
  const processedData = useMemo(() => {
    return data.map(item => heavyComputation(item));
  }, [data]);
  
  const handleUpdate = useCallback((id: string) => {
    onUpdate(id);
  }, [onUpdate]);
  
  return <div>{/* 렌더링 */}</div>;
});
```

---

## 🛠️ 실무 스킬 체크리스트

### Git & 협업
- [ ] 의미 있는 커밋 메시지 작성
- [ ] 브랜치 전략 이해 (feature, develop, main)
- [ ] PR(Pull Request) 작성과 코드 리뷰 참여
- [ ] 컨플릭트 해결 능력

### 코드 품질
- [ ] ESLint, Prettier 설정과 활용
- [ ] 일관된 네이밍 컨벤션 사용
- [ ] 컴포넌트와 함수의 단일 책임 원칙
- [ ] 적절한 주석과 문서화

```typescript
// ❌ 나쁜 네이밍
const data = fetchUserData();
const handleClick = () => { /* 무엇을 처리하는지 불명 */ };

// ✅ 좋은 네이밍  
const userList = await fetchUserList();
const handleUserDelete = (userId: string) => {
  // 사용자 삭제 처리
};
```

### 테스팅 기초
- [ ] 유닛 테스트의 개념 이해
- [ ] Jest와 React Testing Library 기본 사용법
- [ ] 테스트 가능한 코드 작성하기

---

## 📋 일일 개발 루틴

### 🌅 개발 시작 전 (10분)
1. **목표 설정**: 오늘 구현할 기능을 명확히 정의
2. **기술 조사**: 필요한 라이브러리나 API 미리 확인
3. **설계 스케치**: 컴포넌트 구조와 데이터 흐름 간단히 그려보기

### 💻 개발 중
1. **작은 단위로 커밋**: 기능별로 나눠서 개발
2. **정기적 테스트**: 브라우저에서 동작 확인
3. **코드 리뷰**: 본인 코드를 한 번 더 검토

### 🌙 개발 후 (10분)
1. **회고**: 오늘 배운 것, 어려웠던 것 기록
2. **내일 계획**: 다음에 할 일 정리
3. **학습 정리**: 새로 배운 개념이나 에러 해결법 메모

---

## 🎯 2개월 후 목표

### 기술적 목표
- [ ] TypeScript 타입 에러를 혼자 해결할 수 있다
- [ ] React 성능 이슈를 식별하고 최적화할 수 있다
- [ ] 재사용 가능한 컴포넌트를 설계할 수 있다
- [ ] API 연동과 에러 처리를 완벽하게 할 수 있다

### 업무 효율성 목표
- [ ] 기능 개발 속도 50% 향상
- [ ] 버그 발생률 70% 감소
- [ ] 코드 리뷰에서 받는 수정 요청 50% 감소
- [ ] AI 도구 의존도 30% 감소 (스스로 해결하는 비율 증가)

---

## 💡 학습 리소스

### 필독서 (우선순위 순)
1. **"리액트를 다루는 기술" (김민준)** - React 기초부터 심화까지
2. **"타입스크립트 교과서" (조현영)** - TypeScript 완벽 가이드
3. **"클린 코드" (로버트 C. 마틴)** - 코드 품질 향상

### 온라인 강의
1. **React 공식 문서** - 최신 패턴과 Best Practice
2. **TypeScript Handbook** - 공식 문서로 깊이 있는 학습
3. **JavaScript Info** - 자바스크립트 기초 탄탄히

### 실습 프로젝트 아이디어
1. **개인 프로젝트**: Todo 앱을 TypeScript + React로 만들기
2. **오픈소스 기여**: 작은 버그 수정이나 문서 개선
3. **코드 리뷰**: 다른 개발자 코드 분석해보기

---

## 🚨 주의사항

### 하지 말아야 할 것들
- ❌ 에러 메시지를 읽지 않고 바로 AI에게 질문
- ❌ 복사-붙여넣기만 하고 코드 이해하지 않기
- ❌ 한 번에 너무 많은 기능을 구현하려고 하기
- ❌ 테스트 없이 배포하기

### 반드시 해야 할 것들
- ✅ 매일 새로운 것 하나씩 학습하고 기록
- ✅ 에러가 발생하면 원인을 찾을 때까지 파고들기
- ✅ 다른 개발자의 코드를 읽고 분석하기
- ✅ 완벽하지 않더라도 일단 동작하게 만든 후 개선하기

---

## 📊 월간 점검표

매월 말에 체크해보세요:

### 기술 역량
- [ ] 이번 달에 새로 배운 개념 3개 이상
- [ ] 혼자 해결한 복잡한 문제 2개 이상  
- [ ] 재사용 가능한 컴포넌트 1개 이상 작성
- [ ] 성능 최적화나 리팩토링 경험 1회 이상

### 업무 효율성
- [ ] AI 없이도 해결한 문제의 비율이 증가했는가?
- [ ] 기능 개발 속도가 향상되었는가?
- [ ] 버그 발생 빈도가 줄어들었는가?
- [ ] 코드 리뷰 품질이 개선되었는가?

---

> **💬 멘탈 관리**: 
> 7개월 차는 아직 주니어입니다. 완벽하지 않아도 괜찮으니, 꾸준히 성장하는 것에 집중하세요. 
> 매일 조금씩이라도 발전한다면, 1년 후엔 훨씬 더 나은 개발자가 되어 있을 것입니다! 🚀
