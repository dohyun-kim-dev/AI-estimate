{
    "sourceFile": "src/components/common/Footer.tsx",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 34,
            "patches": [
                {
                    "date": 1755954060726,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1755957485393,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -101,11 +101,11 @@\n \n   const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n \n   const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: '/', fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: '/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: '/settings', fallbackIcon: 'settings', text: '설정' },\n+    { key: 'consultation', href: '/ai', fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: '/ai/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: '/ai/setting', fallbackIcon: 'settings', text: '설정' },\n     { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n   ]\n \n   return (\n"
                },
                {
                    "date": 1755958476465,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -101,11 +101,11 @@\n \n   const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n \n   const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: '/ai', fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: '/ai/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: '/ai/setting', fallbackIcon: 'settings', text: '설정' },\n+    { key: 'consultation', href: '/', fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: '/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: '/settings', fallbackIcon: 'settings', text: '설정' },\n     { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n   ]\n \n   return (\n"
                },
                {
                    "date": 1756174010556,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,8 +107,20 @@\n     { key: 'setting', href: '/settings', fallbackIcon: 'settings', text: '설정' },\n     { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n   ]\n \n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault() // Link 컴포넌트의 기본 동작 방지\n+\n+    if (location.pathname === '/') {\n+      // 현재 경로가 '/'일 때만 스크롤을 맨 위로 부드럽게 올림\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      // 다른 경로에 있을 때는 '/'로 이동\n+      navigate('/')\n+    }\n+  }\n+  \n   return (\n     <FooterWrapper $compact={compact}>\n       <FooterContent>\n         {navItems.map((item, idx) => {\n"
                },
                {
                    "date": 1756174051843,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -118,16 +118,23 @@\n       // 다른 경로에 있을 때는 '/'로 이동\n       navigate('/')\n     }\n   }\n-  \n+\n   return (\n     <FooterWrapper $compact={compact}>\n       <FooterContent>\n         {navItems.map((item, idx) => {\n           // 임베드 + 부모 모바일이면 전체화면 메뉴 숨김\n           if (item.external && hideFull) return null\n \n+          if (item.key === 'consultation') {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={handleConsultationClick}\n+              ></ButtonLike>\n           const isActive = item.href ? location.pathname === item.href : false\n           const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n \n           if (item.external) {\n"
                },
                {
                    "date": 1756174061695,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -126,15 +126,21 @@\n         {navItems.map((item, idx) => {\n           // 임베드 + 부모 모바일이면 전체화면 메뉴 숨김\n           if (item.external && hideFull) return null\n \n+          // '견적상담' 아이템을 별도로 처리\n           if (item.key === 'consultation') {\n+            const isActive = location.pathname === item.href\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n             return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={handleConsultationClick}\n-              ></ButtonLike>\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+                {/* <NavText>{item.text}</NavText> */}\n+              </ButtonLike>\n+            )\n+          }\n           const isActive = item.href ? location.pathname === item.href : false\n           const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n \n           if (item.external) {\n"
                },
                {
                    "date": 1756174240709,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,7 @@\n import React, { useEffect, useState } from 'react'\n import styled from 'styled-components'\n-import { Link, useLocation } from 'react-router-dom'\n+import { Link, useLocation,useNavigate } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n \n interface FooterProps { compact?: boolean }\n"
                },
                {
                    "date": 1756174246319,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -81,9 +81,9 @@\n \n const Footer: React.FC<FooterProps> = ({ compact }) => {\n   const location = useLocation()\n   const { isDarkMode } = useThemeStore()\n-\n+  const navigate = useNavigate()\n   // 부모 창의 뷰포트 폭(Widget에서 전달)을 기반으로 임베드 모바일 여부 판정\n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n"
                },
                {
                    "date": 1756364647610,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -81,9 +81,9 @@\n \n const Footer: React.FC<FooterProps> = ({ compact }) => {\n   const location = useLocation()\n   const { isDarkMode } = useThemeStore()\n-  const navigate = useNavigate()\n+  const navigate = useNavigate()  \n   // 부모 창의 뷰포트 폭(Widget에서 전달)을 기반으로 임베드 모바일 여부 판정\n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n@@ -149,9 +149,9 @@\n                 key={idx}\n                 $isActive={false}\n                 onClick={(e) => {\n                   e.preventDefault()\n-                  window.open('/ai-estimate', '_blank', 'noopener,noreferrer')\n+                  window.open('/ai', '_blank', 'noopener,noreferrer')\n                 }}\n               >\n                 <IconWrapper $isActive={false}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n"
                },
                {
                    "date": 1756390735935,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,7 @@\n import React, { useEffect, useState } from 'react'\n import styled from 'styled-components'\n-import { Link, useLocation,useNavigate } from 'react-router-dom'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n \n interface FooterProps { compact?: boolean }\n@@ -80,11 +80,12 @@\n }\n \n const Footer: React.FC<FooterProps> = ({ compact }) => {\n   const location = useLocation()\n+  const navigate = useNavigate()\n   const { isDarkMode } = useThemeStore()\n-  const navigate = useNavigate()  \n-  // 부모 창의 뷰포트 폭(Widget에서 전달)을 기반으로 임베드 모바일 여부 판정\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+\n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n@@ -101,43 +102,40 @@\n \n   const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n \n   const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: '/', fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: '/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: '/settings', fallbackIcon: 'settings', text: '설정' },\n+    { key: 'consultation', href: `/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n     { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n   ]\n \n   const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault() // Link 컴포넌트의 기본 동작 방지\n+    e.preventDefault()\n \n-    if (location.pathname === '/') {\n-      // 현재 경로가 '/'일 때만 스크롤을 맨 위로 부드럽게 올림\n+    const currentPath = `/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n       window.scrollTo({ top: 0, behavior: 'smooth' })\n     } else {\n-      // 다른 경로에 있을 때는 '/'로 이동\n-      navigate('/')\n+      navigate(currentPath)\n     }\n   }\n \n   return (\n     <FooterWrapper $compact={compact}>\n       <FooterContent>\n         {navItems.map((item, idx) => {\n-          // 임베드 + 부모 모바일이면 전체화면 메뉴 숨김\n           if (item.external && hideFull) return null\n-\n-          // '견적상담' 아이템을 별도로 처리\n+          \n           if (item.key === 'consultation') {\n-            const isActive = location.pathname === item.href\n+            const isActive = location.pathname === `/${companyCode}/`\n             const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n             return (\n               <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n                 <IconWrapper $isActive={isActive}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n                 </IconWrapper>\n-                {/* <NavText>{item.text}</NavText> */}\n               </ButtonLike>\n             )\n           }\n           const isActive = item.href ? location.pathname === item.href : false\n@@ -149,30 +147,28 @@\n                 key={idx}\n                 $isActive={false}\n                 onClick={(e) => {\n                   e.preventDefault()\n-                  window.open('/ai', '_blank', 'noopener,noreferrer')\n+                  window.open(`/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n                 }}\n               >\n                 <IconWrapper $isActive={false}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n                 </IconWrapper>\n-                {/* <NavText>{item.text}</NavText> */}\n               </ButtonLike>\n             )\n           }\n \n           return (\n-            <NavItem key={item.href} to={item.href!} $isActive={isActive}>\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n               <IconWrapper $isActive={isActive}>\n                 <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n               </IconWrapper>\n-              {/* <NavText>{item.text}</NavText> */}\n             </NavItem>\n           )\n         })}\n       </FooterContent>\n     </FooterWrapper>\n   )\n }\n\\ No newline at end of file\n \n-export default Footer\n+export default Footer\n"
                },
                {
                    "date": 1756390877622,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -170,5 +170,5 @@\n     </FooterWrapper>\n   )\n }\n \n-export default Footer\n\\ No newline at end of file\n+export default Footer\n"
                },
                {
                    "date": 1756390915278,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,8 +89,9 @@\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n     const searchParams = new URLSearchParams(location.search)\n+    console\n     setIsEmbed(searchParams.get('embed') === '1')\n     const onMsg = (e: MessageEvent) => {\n       if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n         setParentWidth(e.data.width)\n"
                },
                {
                    "date": 1756390921311,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,175 @@\n+import React, { useEffect, useState } from 'react'\n+import styled from 'styled-components'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n+import { useThemeStore } from '@/store/themeStore'\n+import Icon, { IconName } from '@/components/ai-esti/Icon'\n+\n+interface FooterProps { compact?: boolean }\n+\n+const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n+  position: fixed;\n+  bottom: 0;\n+  left: 0;\n+  right: 0;\n+  height: 80px;\n+  background-color: ${({ theme }) => theme.body};\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  z-index: 100;\n+`\n+\n+const FooterContent = styled.div`\n+  max-width: 1200px;\n+  margin: 0 auto;\n+  height: 100%;\n+  display: flex;\n+  align-items: center;\n+  justify-content: space-around;\n+  padding: 0 20px;\n+`\n+\n+const NavItem = styled(Link)<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+\n+  &:hover {\n+    color: ${({ theme }) => theme.accent};\n+  }\n+`\n+\n+const ButtonLike = styled.a<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+  cursor: pointer;\n+\n+  &:hover { color: ${({ theme }) => theme.accent}; }\n+`\n+\n+const IconWrapper = styled.div<{ $isActive?: boolean }>`\n+  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n+  transition: opacity 0.2s ease;\n+\n+  &:hover {\n+    opacity: 1;\n+  }\n+`\n+\n+const NavText = styled.span`\n+  font-weight: 500;\n+`\n+\n+type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n+\n+function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n+  const themePart = isDark ? 'dark' : 'light'\n+  const pickPart = isActive ? '_pick' : ''\n+  return `/main/${key}_${themePart}${pickPart}.png`\n+}\n+\n+const Footer: React.FC<FooterProps> = ({ compact }) => {\n+  const location = useLocation()\n+  const navigate = useNavigate()\n+  const { isDarkMode } = useThemeStore()\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+\n+  const [parentWidth, setParentWidth] = useState<number | null>(null)\n+  const [isEmbed, setIsEmbed] = useState(false)\n+  \n+  useEffect(() => {\n+    const searchParams = new URLSearchParams(location.search)\n+    console.log(searchParams.get('embed'))\n+    setIsEmbed(searchParams.get('embed') === '1')\n+    const onMsg = (e: MessageEvent) => {\n+      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n+        setParentWidth(e.data.width)\n+      }\n+    }\n+    window.addEventListener('message', onMsg)\n+    return () => window.removeEventListener('message', onMsg)\n+  }, [location])\n+\n+  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n+\n+  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n+    { key: 'consultation', href: `/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n+  ]\n+\n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault()\n+\n+    const currentPath = `/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      navigate(currentPath)\n+    }\n+  }\n+\n+  return (\n+    <FooterWrapper $compact={compact}>\n+      <FooterContent>\n+        {navItems.map((item, idx) => {\n+          if (item.external && hideFull) return null\n+          \n+          if (item.key === 'consultation') {\n+            const isActive = location.pathname === `/${companyCode}/`\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n+            return (\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            )\n+          }\n+          const isActive = item.href ? location.pathname === item.href : false\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n+\n+          if (item.external) {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={(e) => {\n+                  e.preventDefault()\n+                  window.open(`/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n+                }}\n+              >\n+                <IconWrapper $isActive={false}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            )\n+          }\n+\n+          return (\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n+              <IconWrapper $isActive={isActive}>\n+                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+              </IconWrapper>\n+            </NavItem>\n+          )\n+        })}\n+      </FooterContent>\n+    </FooterWrapper>\n+  )\n+}\n+\n+export default Footer\n"
                },
                {
                    "date": 1756391120720,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,9 +89,9 @@\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n     const searchParams = new URLSearchParams(location.search)\n-    console.log(searchParams.get('embed'))\n+    console.log(\"searchParams)\n     setIsEmbed(searchParams.get('embed') === '1')\n     const onMsg = (e: MessageEvent) => {\n       if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n         setParentWidth(e.data.width)\n@@ -172,179 +172,4 @@\n   )\n }\n \n export default Footer\n-import React, { useEffect, useState } from 'react'\n-import styled from 'styled-components'\n-import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n-import { useThemeStore } from '@/store/themeStore'\n-import Icon, { IconName } from '@/components/ai-esti/Icon'\n-\n-interface FooterProps { compact?: boolean }\n-\n-const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n-  position: fixed;\n-  bottom: 0;\n-  left: 0;\n-  right: 0;\n-  height: 80px;\n-  background-color: ${({ theme }) => theme.body};\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  z-index: 100;\n-`\n-\n-const FooterContent = styled.div`\n-  max-width: 1200px;\n-  margin: 0 auto;\n-  height: 100%;\n-  display: flex;\n-  align-items: center;\n-  justify-content: space-around;\n-  padding: 0 20px;\n-`\n-\n-const NavItem = styled(Link)<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-\n-  &:hover {\n-    color: ${({ theme }) => theme.accent};\n-  }\n-`\n-\n-const ButtonLike = styled.a<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-  cursor: pointer;\n-\n-  &:hover { color: ${({ theme }) => theme.accent}; }\n-`\n-\n-const IconWrapper = styled.div<{ $isActive?: boolean }>`\n-  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n-  transition: opacity 0.2s ease;\n-\n-  &:hover {\n-    opacity: 1;\n-  }\n-`\n-\n-const NavText = styled.span`\n-  font-weight: 500;\n-`\n-\n-type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n-\n-function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n-  const themePart = isDark ? 'dark' : 'light'\n-  const pickPart = isActive ? '_pick' : ''\n-  return `/main/${key}_${themePart}${pickPart}.png`\n-}\n-\n-const Footer: React.FC<FooterProps> = ({ compact }) => {\n-  const location = useLocation()\n-  const navigate = useNavigate()\n-  const { isDarkMode } = useThemeStore()\n-  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-\n-  const [parentWidth, setParentWidth] = useState<number | null>(null)\n-  const [isEmbed, setIsEmbed] = useState(false)\n-  \n-  useEffect(() => {\n-    const searchParams = new URLSearchParams(location.search)\n-    console\n-    setIsEmbed(searchParams.get('embed') === '1')\n-    const onMsg = (e: MessageEvent) => {\n-      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n-        setParentWidth(e.data.width)\n-      }\n-    }\n-    window.addEventListener('message', onMsg)\n-    return () => window.removeEventListener('message', onMsg)\n-  }, [location])\n-\n-  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n-\n-  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n-    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n-  ]\n-\n-  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault()\n-\n-    const currentPath = `/${companyCode}/`\n-\n-    if (location.pathname === currentPath) {\n-      window.scrollTo({ top: 0, behavior: 'smooth' })\n-    } else {\n-      navigate(currentPath)\n-    }\n-  }\n-\n-  return (\n-    <FooterWrapper $compact={compact}>\n-      <FooterContent>\n-        {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null\n-          \n-          if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/${companyCode}/`\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n-            return (\n-              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            )\n-          }\n-          const isActive = item.href ? location.pathname === item.href : false\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n-\n-          if (item.external) {\n-            return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={(e) => {\n-                  e.preventDefault()\n-                  window.open(`/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n-                }}\n-              >\n-                <IconWrapper $isActive={false}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            )\n-          }\n-\n-          return (\n-            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n-              <IconWrapper $isActive={isActive}>\n-                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-              </IconWrapper>\n-            </NavItem>\n-          )\n-        })}\n-      </FooterContent>\n-    </FooterWrapper>\n-  )\n-}\n-\n-export default Footer\n"
                },
                {
                    "date": 1756391126669,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,9 +89,9 @@\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n     const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams)\n+    console.log(\"searchParams!!!\",searchParams)\n     setIsEmbed(searchParams.get('embed') === '1')\n     const onMsg = (e: MessageEvent) => {\n       if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n         setParentWidth(e.data.width)\n"
                },
                {
                    "date": 1756391162882,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,9 +89,9 @@\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n     const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams)\n+    console.log(\"searchParams!!!\",searchParams , c)\n     setIsEmbed(searchParams.get('embed') === '1')\n     const onMsg = (e: MessageEvent) => {\n       if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n         setParentWidth(e.data.width)\n"
                },
                {
                    "date": 1756392132171,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,9 +89,9 @@\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n     const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams , c)\n+    console.log(\"searchParams!!!\",searchParams , companyCode )\n     setIsEmbed(searchParams.get('embed') === '1')\n     const onMsg = (e: MessageEvent) => {\n       if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n         setParentWidth(e.data.width)\n@@ -103,18 +103,18 @@\n \n   const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n \n   const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n     { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n   ]\n \n   const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n     e.preventDefault()\n \n-    const currentPath = `/${companyCode}/`\n+    const currentPath = `/aiclient/${companyCode}/`\n \n     if (location.pathname === currentPath) {\n       window.scrollTo({ top: 0, behavior: 'smooth' })\n     } else {\n@@ -128,9 +128,9 @@\n         {navItems.map((item, idx) => {\n           if (item.external && hideFull) return null\n           \n           if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/${companyCode}/`\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`\n             const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n             return (\n               <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n                 <IconWrapper $isActive={isActive}>\n@@ -148,9 +148,9 @@\n                 key={idx}\n                 $isActive={false}\n                 onClick={(e) => {\n                   e.preventDefault()\n-                  window.open(`/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n                 }}\n               >\n                 <IconWrapper $isActive={false}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n"
                },
                {
                    "date": 1756716335465,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,8 +2,9 @@\n import styled from 'styled-components'\n import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n+import SocialLoginModal from '@/components/SocialLoginModal'; // SocialLoginModal 컴포넌트 import\n \n interface FooterProps { compact?: boolean }\n \n const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n"
                },
                {
                    "date": 1756716341136,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,9 +2,9 @@\n import styled from 'styled-components'\n import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n-import SocialLoginModal from '@/components/SocialLoginModal'; // SocialLoginModal 컴포넌트 import\n+import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n \n interface FooterProps { compact?: boolean }\n \n const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n"
                },
                {
                    "date": 1756716354300,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,9 +3,9 @@\n import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-\n+import \n interface FooterProps { compact?: boolean }\n \n const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n   position: fixed;\n"
                },
                {
                    "date": 1756716361242,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,9 +3,9 @@\n import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import \n+import { isAuthenticated } from '@/store/authStore';\n interface FooterProps { compact?: boolean }\n \n const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n   position: fixed;\n"
                },
                {
                    "date": 1756716367022,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,9 +3,10 @@\n import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n import { useThemeStore } from '@/store/themeStore'\n import Icon, { IconName } from '@/components/ai-esti/Icon'\n import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import { isAuthenticated } from '@/store/authStore';\n+import { useAuthStore } from '@/store/authStore';\n+\n interface FooterProps { compact?: boolean }\n \n const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n   position: fixed;\n@@ -84,9 +85,9 @@\n   const location = useLocation()\n   const navigate = useNavigate()\n   const { isDarkMode } = useThemeStore()\n   const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-\n+  \n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n"
                },
                {
                    "date": 1756716372750,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -85,8 +85,9 @@\n   const location = useLocation()\n   const navigate = useNavigate()\n   const { isDarkMode } = useThemeStore()\n   const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+  const { isAuthenticated } = useAuthStore()\n   \n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n"
                },
                {
                    "date": 1756716401692,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -86,9 +86,9 @@\n   const navigate = useNavigate()\n   const { isDarkMode } = useThemeStore()\n   const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n   const { isAuthenticated } = useAuthStore()\n-  \n+  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n   const [parentWidth, setParentWidth] = useState<number | null>(null)\n   const [isEmbed, setIsEmbed] = useState(false)\n   \n   useEffect(() => {\n@@ -128,51 +128,79 @@\n   return (\n     <FooterWrapper $compact={compact}>\n       <FooterContent>\n         {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null\n-          \n+          if (item.external && hideFull) return null;\n+  \n+          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n+  \n           if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/aiclient/${companyCode}/`\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive)\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n             return (\n               <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n                 <IconWrapper $isActive={isActive}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n                 </IconWrapper>\n               </ButtonLike>\n-            )\n+            );\n           }\n-          const isActive = item.href ? location.pathname === item.href : false\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n-\n+  \n+          const isActive = item.href ? location.pathname === item.href : false;\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n+  \n           if (item.external) {\n             return (\n               <ButtonLike\n                 key={idx}\n                 $isActive={false}\n                 onClick={(e) => {\n-                  e.preventDefault()\n-                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer')\n+                  e.preventDefault();\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n                 }}\n               >\n                 <IconWrapper $isActive={false}>\n                   <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n                 </IconWrapper>\n               </ButtonLike>\n-            )\n+            );\n           }\n-\n+  \n+          // '나의견적' 또는 '설정' 버튼 처리\n+          if (isSpecialItem) {\n+            const handleItemClick = (e: React.MouseEvent) => {\n+              if (!isAuthenticated()) {\n+                e.preventDefault();\n+                setIsLoginModalOpen(true);\n+              }\n+            };\n+  \n+            return (\n+              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </NavItem>\n+            );\n+          }\n+  \n           return (\n             <NavItem key={item.href} to={item.href} $isActive={isActive}>\n               <IconWrapper $isActive={isActive}>\n                 <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n               </IconWrapper>\n             </NavItem>\n-          )\n+          );\n         })}\n       </FooterContent>\n+  \n+      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n+      <SocialLoginModal\n+        isOpen={isLoginModalOpen}\n+        onClose={() => setIsLoginModalOpen(false)}\n+      />\n     </FooterWrapper>\n+  );\n   )\n }\n \n export default Footer\n"
                },
                {
                    "date": 1756716415355,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -198,9 +198,9 @@\n         isOpen={isLoginModalOpen}\n         onClose={() => setIsLoginModalOpen(false)}\n       />\n     </FooterWrapper>\n-  );\n   )\n }\n+}\n \n export default Footer\n"
                },
                {
                    "date": 1756716426004,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -198,9 +198,9 @@\n         isOpen={isLoginModalOpen}\n         onClose={() => setIsLoginModalOpen(false)}\n       />\n     </FooterWrapper>\n+  );\n   )\n }\n-}\n \n export default Footer\n"
                },
                {
                    "date": 1756716434844,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -194,13 +194,13 @@\n       </FooterContent>\n   \n       {/* 소셜 로그인 모달 컴포넌트 추가 */}\n       <SocialLoginModal\n-        isOpen={isLoginModalOpen}\n-        onClose={() => setIsLoginModalOpen(false)}\n+        isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n       />\n     </FooterWrapper>\n-  );\n   )\n+  \n }\n \n export default Footer\n"
                },
                {
                    "date": 1756716457813,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -169,9 +169,9 @@\n           if (isSpecialItem) {\n             const handleItemClick = (e: React.MouseEvent) => {\n               if (!isAuthenticated()) {\n                 e.preventDefault();\n-                setIsLoginModalOpen(true);\n+                setIsSocialLoginModalOpen(true);\n               }\n             };\n   \n             return (\n@@ -194,9 +194,9 @@\n       </FooterContent>\n   \n       {/* 소셜 로그인 모달 컴포넌트 추가 */}\n       <SocialLoginModal\n-        isOpen={isSocialLoginModalOpen}\n+        $isOpen={isSocialLoginModalOpen}\n         onClose={() => setIsSocialLoginModalOpen(false)}\n       />\n     </FooterWrapper>\n   )\n"
                },
                {
                    "date": 1756716490348,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,206 @@\n+import React, { useEffect, useState } from 'react'\n+import styled from 'styled-components'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n+import { useThemeStore } from '@/store/themeStore'\n+import Icon, { IconName } from '@/components/ai-esti/Icon'\n+import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n+import { useAuthStore } from '@/store/authStore';\n+\n+interface FooterProps { compact?: boolean }\n+\n+const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n+  position: fixed;\n+  bottom: 0;\n+  left: 0;\n+  right: 0;\n+  height: 80px;\n+  background-color: ${({ theme }) => theme.body};\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  z-index: 1001;\n+`\n+\n+const FooterContent = styled.div`\n+  max-width: 1200px;\n+  margin: 0 auto;\n+  height: 100%;\n+  display: flex;\n+  align-items: center;\n+  justify-content: space-around;\n+  padding: 0 20px;\n+`\n+\n+const NavItem = styled(Link)<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+\n+  &:hover {\n+    color: ${({ theme }) => theme.accent};\n+  }\n+`\n+\n+const ButtonLike = styled.a<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+  cursor: pointer;\n+\n+  &:hover { color: ${({ theme }) => theme.accent}; }\n+`\n+\n+const IconWrapper = styled.div<{ $isActive?: boolean }>`\n+  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n+  transition: opacity 0.2s ease;\n+\n+  &:hover {\n+    opacity: 1;\n+  }\n+`\n+\n+const NavText = styled.span`\n+  font-weight: 500;\n+`\n+\n+type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n+\n+function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n+  const themePart = isDark ? 'dark' : 'light'\n+  const pickPart = isActive ? '_pick' : ''\n+  return `/main/${key}_${themePart}${pickPart}.png`\n+}\n+\n+const Footer: React.FC<FooterProps> = ({ compact }) => {\n+  const location = useLocation()\n+  const navigate = useNavigate()\n+  const { isDarkMode } = useThemeStore()\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+  const { isAuthenticated } = useAuthStore()\n+  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n+  const [parentWidth, setParentWidth] = useState<number | null>(null)\n+  const [isEmbed, setIsEmbed] = useState(false)\n+  \n+  useEffect(() => {\n+    const searchParams = new URLSearchParams(location.search)\n+    console.log(\"searchParams!!!\",searchParams , companyCode )\n+    setIsEmbed(searchParams.get('embed') === '1')\n+    const onMsg = (e: MessageEvent) => {\n+      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n+        setParentWidth(e.data.width)\n+      }\n+    }\n+    window.addEventListener('message', onMsg)\n+    return () => window.removeEventListener('message', onMsg)\n+  }, [location])\n+\n+  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n+\n+  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n+    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n+  ]\n+\n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault()\n+\n+    const currentPath = `/aiclient/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      navigate(currentPath)\n+    }\n+  }\n+\n+  return (\n+    <FooterWrapper $compact={compact}>\n+      <FooterContent>\n+        {navItems.map((item, idx) => {\n+          if (item.external && hideFull) return null;\n+  \n+          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n+  \n+          if (item.key === 'consultation') {\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n+            return (\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          const isActive = item.href ? location.pathname === item.href : false;\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n+  \n+          if (item.external) {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={(e) => {\n+                  e.preventDefault();\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n+                }}\n+              >\n+                <IconWrapper $isActive={false}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          // '나의견적' 또는 '설정' 버튼 처리\n+          if (isSpecialItem) {\n+            const handleItemClick = (e: React.MouseEvent) => {\n+              if (!isAuthenticated()) {\n+                e.preventDefault();\n+                setIsSocialLoginModalOpen(true);\n+              }\n+            };\n+  \n+            return (\n+              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </NavItem>\n+            );\n+          }\n+  \n+          return (\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n+              <IconWrapper $isActive={isActive}>\n+                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+              </IconWrapper>\n+            </NavItem>\n+          );\n+        })}\n+      </FooterContent>\n+  \n+      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n+      <SocialLoginModal\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      />\n+    </FooterWrapper>\n+  )\n+  \n+}\n+\n+export default Footer\n"
                },
                {
                    "date": 1756716510570,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -26,216 +26,12 @@\n   display: flex;\n   align-items: center;\n   justify-content: space-around;\n   padding: 0 20px;\n-`\n+    z-index: 1001;\n \n-const NavItem = styled(Link)<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-\n-  &:hover {\n-    color: ${({ theme }) => theme.accent};\n-  }\n `\n \n-const ButtonLike = styled.a<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-  cursor: pointer;\n-\n-  &:hover { color: ${({ theme }) => theme.accent}; }\n-`\n-\n-const IconWrapper = styled.div<{ $isActive?: boolean }>`\n-  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n-  transition: opacity 0.2s ease;\n-\n-  &:hover {\n-    opacity: 1;\n-  }\n-`\n-\n-const NavText = styled.span`\n-  font-weight: 500;\n-`\n-\n-type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n-\n-function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n-  const themePart = isDark ? 'dark' : 'light'\n-  const pickPart = isActive ? '_pick' : ''\n-  return `/main/${key}_${themePart}${pickPart}.png`\n-}\n-\n-const Footer: React.FC<FooterProps> = ({ compact }) => {\n-  const location = useLocation()\n-  const navigate = useNavigate()\n-  const { isDarkMode } = useThemeStore()\n-  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-  const { isAuthenticated } = useAuthStore()\n-  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n-  const [parentWidth, setParentWidth] = useState<number | null>(null)\n-  const [isEmbed, setIsEmbed] = useState(false)\n-  \n-  useEffect(() => {\n-    const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams , companyCode )\n-    setIsEmbed(searchParams.get('embed') === '1')\n-    const onMsg = (e: MessageEvent) => {\n-      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n-        setParentWidth(e.data.width)\n-      }\n-    }\n-    window.addEventListener('message', onMsg)\n-    return () => window.removeEventListener('message', onMsg)\n-  }, [location])\n-\n-  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n-\n-  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n-    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n-  ]\n-\n-  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault()\n-\n-    const currentPath = `/aiclient/${companyCode}/`\n-\n-    if (location.pathname === currentPath) {\n-      window.scrollTo({ top: 0, behavior: 'smooth' })\n-    } else {\n-      navigate(currentPath)\n-    }\n-  }\n-\n-  return (\n-    <FooterWrapper $compact={compact}>\n-      <FooterContent>\n-        {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null;\n-  \n-          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n-  \n-          if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n-            return (\n-              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          const isActive = item.href ? location.pathname === item.href : false;\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n-  \n-          if (item.external) {\n-            return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={(e) => {\n-                  e.preventDefault();\n-                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n-                }}\n-              >\n-                <IconWrapper $isActive={false}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          // '나의견적' 또는 '설정' 버튼 처리\n-          if (isSpecialItem) {\n-            const handleItemClick = (e: React.MouseEvent) => {\n-              if (!isAuthenticated()) {\n-                e.preventDefault();\n-                setIsSocialLoginModalOpen(true);\n-              }\n-            };\n-  \n-            return (\n-              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </NavItem>\n-            );\n-          }\n-  \n-          return (\n-            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n-              <IconWrapper $isActive={isActive}>\n-                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-              </IconWrapper>\n-            </NavItem>\n-          );\n-        })}\n-      </FooterContent>\n-  \n-      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n-      <SocialLoginModal\n-        $isOpen={isSocialLoginModalOpen}\n-        onClose={() => setIsSocialLoginModalOpen(false)}\n-      />\n-    </FooterWrapper>\n-  )\n-  \n-}\n-\n-export default Footer\n-import React, { useEffect, useState } from 'react'\n-import styled from 'styled-components'\n-import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n-import { useThemeStore } from '@/store/themeStore'\n-import Icon, { IconName } from '@/components/ai-esti/Icon'\n-import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import { useAuthStore } from '@/store/authStore';\n-\n-interface FooterProps { compact?: boolean }\n-\n-const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n-  position: fixed;\n-  bottom: 0;\n-  left: 0;\n-  right: 0;\n-  height: 80px;\n-  background-color: ${({ theme }) => theme.body};\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  z-index: 100;\n-`\n-\n-const FooterContent = styled.div`\n-  max-width: 1200px;\n-  margin: 0 auto;\n-  height: 100%;\n-  display: flex;\n-  align-items: center;\n-  justify-content: space-around;\n-  padding: 0 20px;\n-`\n-\n const NavItem = styled(Link)<{ $isActive?: boolean }>`\n   display: flex;\n   flex-direction: column;\n   align-items: center;\n"
                },
                {
                    "date": 1756760772732,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,208 @@\n+import React, { useEffect, useState } from 'react'\n+import styled from 'styled-components'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n+import { useThemeStore } from '@/store/themeStore'\n+import Icon, { IconName } from '@/components/ai-esti/Icon'\n+import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n+import { useAuthStore } from '@/store/authStore';\n+\n+interface FooterProps { compact?: boolean }\n+\n+const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n+  position: fixed;\n+  bottom: 0;\n+  left: 0;\n+  right: 0;\n+  height: 80px;\n+  background-color: ${({ theme }) => theme.body};\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  z-index: 1001;\n+`\n+\n+const FooterContent = styled.div`\n+  max-width: 1200px;\n+  margin: 0 auto;\n+  height: 100%;\n+  display: flex;\n+  align-items: center;\n+  justify-content: space-around;\n+  padding: 0 20px;\n+    z-index: 1001;\n+\n+`\n+\n+const NavItem = styled(Link)<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+\n+  &:hover {\n+    color: ${({ theme }) => theme.accent};\n+  }\n+`\n+\n+const ButtonLike = styled.a<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+  cursor: pointer;\n+\n+  &:hover { color: ${({ theme }) => theme.accent}; }\n+`\n+\n+const IconWrapper = styled.div<{ $isActive?: boolean }>`\n+  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n+  transition: opacity 0.2s ease;\n+\n+  &:hover {\n+    opacity: 1;\n+  }\n+`\n+\n+const NavText = styled.span`\n+  font-weight: 500;\n+`\n+\n+type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n+\n+function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n+  const themePart = isDark ? 'dark' : 'light'\n+  const pickPart = isActive ? '_pick' : ''\n+  return `/main/${key}_${themePart}${pickPart}.png`\n+}\n+\n+const Footer: React.FC<FooterProps> = ({ compact }) => {\n+  const location = useLocation()\n+  const navigate = useNavigate()\n+  const { isDarkMode } = useThemeStore()\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+  const { isAuthenticated } = useAuthStore()\n+  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n+  const [parentWidth, setParentWidth] = useState<number | null>(null)\n+  const [isEmbed, setIsEmbed] = useState(false)\n+  \n+  useEffect(() => {\n+    const searchParams = new URLSearchParams(location.search)\n+    console.log(\"searchParams!!!\",searchParams , companyCode )\n+    setIsEmbed(searchParams.get('embed') === '1')\n+    const onMsg = (e: MessageEvent) => {\n+      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n+        setParentWidth(e.data.width)\n+      }\n+    }\n+    window.addEventListener('message', onMsg)\n+    return () => window.removeEventListener('message', onMsg)\n+  }, [location])\n+\n+  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n+\n+  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n+    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n+  ]\n+\n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault()\n+\n+    const currentPath = `/aiclient/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      navigate(currentPath)\n+    }\n+  }\n+\n+  return (\n+    <FooterWrapper $compact={compact}>\n+      <FooterContent>\n+        {navItems.map((item, idx) => {\n+          if (item.external && hideFull) return null;\n+  \n+          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n+  \n+          if (item.key === 'consultation') {\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n+            return (\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          const isActive = item.href ? location.pathname === item.href : false;\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n+  \n+          if (item.external) {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={(e) => {\n+                  e.preventDefault();\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n+                }}\n+              >\n+                <IconWrapper $isActive={false}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          // '나의견적' 또는 '설정' 버튼 처리\n+          if (isSpecialItem) {\n+            const handleItemClick = (e: React.MouseEvent) => {\n+              if (!isAuthenticated()) {\n+                e.preventDefault();\n+                setIsSocialLoginModalOpen(true);\n+              }\n+            };\n+  \n+            return (\n+              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </NavItem>\n+            );\n+          }\n+  \n+          return (\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n+              <IconWrapper $isActive={isActive}>\n+                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+              </IconWrapper>\n+            </NavItem>\n+          );\n+        })}\n+      </FooterContent>\n+  \n+      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n+      {/* <SocialLoginModal\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      /> */}\n+    </FooterWrapper>\n+  )\n+  \n+}\n+\n+export default Footer\n"
                },
                {
                    "date": 1756760778810,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -199,213 +199,9 @@\n       {/* <SocialLoginModal\n         $isOpen={isSocialLoginModalOpen}\n         onClose={() => setIsSocialLoginModalOpen(false)}\n       /> */}\n-    </FooterWrapper>\n-  )\n-  \n-}\n-\n-export default Footer\n-import React, { useEffect, useState } from 'react'\n-import styled from 'styled-components'\n-import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n-import { useThemeStore } from '@/store/themeStore'\n-import Icon, { IconName } from '@/components/ai-esti/Icon'\n-import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import { useAuthStore } from '@/store/authStore';\n-\n-interface FooterProps { compact?: boolean }\n-\n-const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n-  position: fixed;\n-  bottom: 0;\n-  left: 0;\n-  right: 0;\n-  height: 80px;\n-  background-color: ${({ theme }) => theme.body};\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  z-index: 1001;\n-`\n-\n-const FooterContent = styled.div`\n-  max-width: 1200px;\n-  margin: 0 auto;\n-  height: 100%;\n-  display: flex;\n-  align-items: center;\n-  justify-content: space-around;\n-  padding: 0 20px;\n-    z-index: 1001;\n-\n-`\n-\n-const NavItem = styled(Link)<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-\n-  &:hover {\n-    color: ${({ theme }) => theme.accent};\n-  }\n-`\n-\n-const ButtonLike = styled.a<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-  cursor: pointer;\n-\n-  &:hover { color: ${({ theme }) => theme.accent}; }\n-`\n-\n-const IconWrapper = styled.div<{ $isActive?: boolean }>`\n-  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n-  transition: opacity 0.2s ease;\n-\n-  &:hover {\n-    opacity: 1;\n-  }\n-`\n-\n-const NavText = styled.span`\n-  font-weight: 500;\n-`\n-\n-type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n-\n-function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n-  const themePart = isDark ? 'dark' : 'light'\n-  const pickPart = isActive ? '_pick' : ''\n-  return `/main/${key}_${themePart}${pickPart}.png`\n-}\n-\n-const Footer: React.FC<FooterProps> = ({ compact }) => {\n-  const location = useLocation()\n-  const navigate = useNavigate()\n-  const { isDarkMode } = useThemeStore()\n-  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-  const { isAuthenticated } = useAuthStore()\n-  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n-  const [parentWidth, setParentWidth] = useState<number | null>(null)\n-  const [isEmbed, setIsEmbed] = useState(false)\n-  \n-  useEffect(() => {\n-    const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams , companyCode )\n-    setIsEmbed(searchParams.get('embed') === '1')\n-    const onMsg = (e: MessageEvent) => {\n-      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n-        setParentWidth(e.data.width)\n-      }\n-    }\n-    window.addEventListener('message', onMsg)\n-    return () => window.removeEventListener('message', onMsg)\n-  }, [location])\n-\n-  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n-\n-  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n-    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n-  ]\n-\n-  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault()\n-\n-    const currentPath = `/aiclient/${companyCode}/`\n-\n-    if (location.pathname === currentPath) {\n-      window.scrollTo({ top: 0, behavior: 'smooth' })\n-    } else {\n-      navigate(currentPath)\n-    }\n-  }\n-\n-  return (\n-    <FooterWrapper $compact={compact}>\n-      <FooterContent>\n-        {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null;\n-  \n-          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n-  \n-          if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n-            return (\n-              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          const isActive = item.href ? location.pathname === item.href : false;\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n-  \n-          if (item.external) {\n-            return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={(e) => {\n-                  e.preventDefault();\n-                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n-                }}\n-              >\n-                <IconWrapper $isActive={false}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          // '나의견적' 또는 '설정' 버튼 처리\n-          if (isSpecialItem) {\n-            const handleItemClick = (e: React.MouseEvent) => {\n-              if (!isAuthenticated()) {\n-                e.preventDefault();\n-                setIsSocialLoginModalOpen(true);\n-              }\n-            };\n-  \n-            return (\n-              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </NavItem>\n-            );\n-          }\n-  \n-          return (\n-            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n-              <IconWrapper $isActive={isActive}>\n-                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-              </IconWrapper>\n-            </NavItem>\n-          );\n-        })}\n-      </FooterContent>\n-  \n-      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n-      <SocialLoginModal\n+      <CountModal\n         $isOpen={isSocialLoginModalOpen}\n         onClose={() => setIsSocialLoginModalOpen(false)}\n       />\n     </FooterWrapper>\n"
                },
                {
                    "date": 1756760785221,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,212 @@\n+import React, { useEffect, useState } from 'react'\n+import styled from 'styled-components'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n+import { useThemeStore } from '@/store/themeStore'\n+import Icon, { IconName } from '@/components/ai-esti/Icon'\n+import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n+import { useAuthStore } from '@/store/authStore';\n+\n+interface FooterProps { compact?: boolean }\n+\n+const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n+  position: fixed;\n+  bottom: 0;\n+  left: 0;\n+  right: 0;\n+  height: 80px;\n+  background-color: ${({ theme }) => theme.body};\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  z-index: 1001;\n+`\n+\n+const FooterContent = styled.div`\n+  max-width: 1200px;\n+  margin: 0 auto;\n+  height: 100%;\n+  display: flex;\n+  align-items: center;\n+  justify-content: space-around;\n+  padding: 0 20px;\n+    z-index: 1001;\n+\n+`\n+\n+const NavItem = styled(Link)<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+\n+  &:hover {\n+    color: ${({ theme }) => theme.accent};\n+  }\n+`\n+\n+const ButtonLike = styled.a<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+  cursor: pointer;\n+\n+  &:hover { color: ${({ theme }) => theme.accent}; }\n+`\n+\n+const IconWrapper = styled.div<{ $isActive?: boolean }>`\n+  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n+  transition: opacity 0.2s ease;\n+\n+  &:hover {\n+    opacity: 1;\n+  }\n+`\n+\n+const NavText = styled.span`\n+  font-weight: 500;\n+`\n+\n+type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n+\n+function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n+  const themePart = isDark ? 'dark' : 'light'\n+  const pickPart = isActive ? '_pick' : ''\n+  return `/main/${key}_${themePart}${pickPart}.png`\n+}\n+\n+const Footer: React.FC<FooterProps> = ({ compact }) => {\n+  const location = useLocation()\n+  const navigate = useNavigate()\n+  const { isDarkMode } = useThemeStore()\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+  const { isAuthenticated } = useAuthStore()\n+  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n+  const [parentWidth, setParentWidth] = useState<number | null>(null)\n+  const [isEmbed, setIsEmbed] = useState(false)\n+  \n+  useEffect(() => {\n+    const searchParams = new URLSearchParams(location.search)\n+    console.log(\"searchParams!!!\",searchParams , companyCode )\n+    setIsEmbed(searchParams.get('embed') === '1')\n+    const onMsg = (e: MessageEvent) => {\n+      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n+        setParentWidth(e.data.width)\n+      }\n+    }\n+    window.addEventListener('message', onMsg)\n+    return () => window.removeEventListener('message', onMsg)\n+  }, [location])\n+\n+  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n+\n+  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n+    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n+  ]\n+\n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault()\n+\n+    const currentPath = `/aiclient/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      navigate(currentPath)\n+    }\n+  }\n+\n+  return (\n+    <FooterWrapper $compact={compact}>\n+      <FooterContent>\n+        {navItems.map((item, idx) => {\n+          if (item.external && hideFull) return null;\n+  \n+          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n+  \n+          if (item.key === 'consultation') {\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n+            return (\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          const isActive = item.href ? location.pathname === item.href : false;\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n+  \n+          if (item.external) {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={(e) => {\n+                  e.preventDefault();\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n+                }}\n+              >\n+                <IconWrapper $isActive={false}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          // '나의견적' 또는 '설정' 버튼 처리\n+          if (isSpecialItem) {\n+            const handleItemClick = (e: React.MouseEvent) => {\n+              if (!isAuthenticated()) {\n+                e.preventDefault();\n+                setIsSocialLoginModalOpen(true);\n+              }\n+            };\n+  \n+            return (\n+              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </NavItem>\n+            );\n+          }\n+  \n+          return (\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n+              <IconWrapper $isActive={isActive}>\n+                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+              </IconWrapper>\n+            </NavItem>\n+          );\n+        })}\n+      </FooterContent>\n+  \n+      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n+      {/* <SocialLoginModal\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      /> */}\n+      <CountModa\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      />\n+    </FooterWrapper>\n+  )\n+  \n+}\n+\n+export default Footer\n"
                },
                {
                    "date": 1756760791173,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,213 @@\n+import React, { useEffect, useState } from 'react'\n+import styled from 'styled-components'\n+import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n+import { useThemeStore } from '@/store/themeStore'\n+import Icon, { IconName } from '@/components/ai-esti/Icon'\n+import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n+import { useAuthStore } from '@/store/authStore';\n+import { CountModal } from '@/components/ai-esti/CountModal';\n+\n+interface FooterProps { compact?: boolean }\n+\n+const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n+  position: fixed;\n+  bottom: 0;\n+  left: 0;\n+  right: 0;\n+  height: 80px;\n+  background-color: ${({ theme }) => theme.body};\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  z-index: 1001;\n+`\n+\n+const FooterContent = styled.div`\n+  max-width: 1200px;\n+  margin: 0 auto;\n+  height: 100%;\n+  display: flex;\n+  align-items: center;\n+  justify-content: space-around;\n+  padding: 0 20px;\n+    z-index: 1001;\n+\n+`\n+\n+const NavItem = styled(Link)<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+\n+  &:hover {\n+    color: ${({ theme }) => theme.accent};\n+  }\n+`\n+\n+const ButtonLike = styled.a<{ $isActive?: boolean }>`\n+  display: flex;\n+  flex-direction: column;\n+  align-items: center;\n+  gap: 6px;\n+  text-decoration: none;\n+  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n+  font-size: 12px;\n+  min-width: 56px;\n+  padding: 8px 0;\n+  cursor: pointer;\n+\n+  &:hover { color: ${({ theme }) => theme.accent}; }\n+`\n+\n+const IconWrapper = styled.div<{ $isActive?: boolean }>`\n+  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n+  transition: opacity 0.2s ease;\n+\n+  &:hover {\n+    opacity: 1;\n+  }\n+`\n+\n+const NavText = styled.span`\n+  font-weight: 500;\n+`\n+\n+type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n+\n+function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n+  const themePart = isDark ? 'dark' : 'light'\n+  const pickPart = isActive ? '_pick' : ''\n+  return `/main/${key}_${themePart}${pickPart}.png`\n+}\n+\n+const Footer: React.FC<FooterProps> = ({ compact }) => {\n+  const location = useLocation()\n+  const navigate = useNavigate()\n+  const { isDarkMode } = useThemeStore()\n+  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n+  const { isAuthenticated } = useAuthStore()\n+  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n+  const [parentWidth, setParentWidth] = useState<number | null>(null)\n+  const [isEmbed, setIsEmbed] = useState(false)\n+  \n+  useEffect(() => {\n+    const searchParams = new URLSearchParams(location.search)\n+    console.log(\"searchParams!!!\",searchParams , companyCode )\n+    setIsEmbed(searchParams.get('embed') === '1')\n+    const onMsg = (e: MessageEvent) => {\n+      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n+        setParentWidth(e.data.width)\n+      }\n+    }\n+    window.addEventListener('message', onMsg)\n+    return () => window.removeEventListener('message', onMsg)\n+  }, [location])\n+\n+  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n+\n+  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n+    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n+    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n+    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n+    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n+  ]\n+\n+  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n+    e.preventDefault()\n+\n+    const currentPath = `/aiclient/${companyCode}/`\n+\n+    if (location.pathname === currentPath) {\n+      window.scrollTo({ top: 0, behavior: 'smooth' })\n+    } else {\n+      navigate(currentPath)\n+    }\n+  }\n+\n+  return (\n+    <FooterWrapper $compact={compact}>\n+      <FooterContent>\n+        {navItems.map((item, idx) => {\n+          if (item.external && hideFull) return null;\n+  \n+          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n+  \n+          if (item.key === 'consultation') {\n+            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n+            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n+            return (\n+              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          const isActive = item.href ? location.pathname === item.href : false;\n+          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n+  \n+          if (item.external) {\n+            return (\n+              <ButtonLike\n+                key={idx}\n+                $isActive={false}\n+                onClick={(e) => {\n+                  e.preventDefault();\n+                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n+                }}\n+              >\n+                <IconWrapper $isActive={false}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </ButtonLike>\n+            );\n+          }\n+  \n+          // '나의견적' 또는 '설정' 버튼 처리\n+          if (isSpecialItem) {\n+            const handleItemClick = (e: React.MouseEvent) => {\n+              if (!isAuthenticated()) {\n+                e.preventDefault();\n+                setIsSocialLoginModalOpen(true);\n+              }\n+            };\n+  \n+            return (\n+              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n+                <IconWrapper $isActive={isActive}>\n+                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+                </IconWrapper>\n+              </NavItem>\n+            );\n+          }\n+  \n+          return (\n+            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n+              <IconWrapper $isActive={isActive}>\n+                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n+              </IconWrapper>\n+            </NavItem>\n+          );\n+        })}\n+      </FooterContent>\n+  \n+      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n+      {/* <SocialLoginModal\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      /> */}\n+      <CountModal\n+        $isOpen={isSocialLoginModalOpen}\n+        onClose={() => setIsSocialLoginModalOpen(false)}\n+      />\n+    </FooterWrapper>\n+  )\n+  \n+}\n+\n+export default Footer\n"
                },
                {
                    "date": 1756800515566,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -196,436 +196,12 @@\n         })}\n       </FooterContent>\n   \n       {/* 소셜 로그인 모달 컴포넌트 추가 */}\n-      {/* <SocialLoginModal\n+      <SocialLoginModal\n         $isOpen={isSocialLoginModalOpen}\n         onClose={() => setIsSocialLoginModalOpen(false)}\n-      /> */}\n-      <CountModal\n-        $isOpen={isSocialLoginModalOpen}\n-        onClose={() => setIsSocialLoginModalOpen(false)}\n       />\n-    </FooterWrapper>\n-  )\n-  \n-}\n-\n-export default Footer\n-import React, { useEffect, useState } from 'react'\n-import styled from 'styled-components'\n-import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n-import { useThemeStore } from '@/store/themeStore'\n-import Icon, { IconName } from '@/components/ai-esti/Icon'\n-import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import { useAuthStore } from '@/store/authStore';\n-\n-interface FooterProps { compact?: boolean }\n-\n-const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n-  position: fixed;\n-  bottom: 0;\n-  left: 0;\n-  right: 0;\n-  height: 80px;\n-  background-color: ${({ theme }) => theme.body};\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  z-index: 1001;\n-`\n-\n-const FooterContent = styled.div`\n-  max-width: 1200px;\n-  margin: 0 auto;\n-  height: 100%;\n-  display: flex;\n-  align-items: center;\n-  justify-content: space-around;\n-  padding: 0 20px;\n-    z-index: 1001;\n-\n-`\n-\n-const NavItem = styled(Link)<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-\n-  &:hover {\n-    color: ${({ theme }) => theme.accent};\n-  }\n-`\n-\n-const ButtonLike = styled.a<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-  cursor: pointer;\n-\n-  &:hover { color: ${({ theme }) => theme.accent}; }\n-`\n-\n-const IconWrapper = styled.div<{ $isActive?: boolean }>`\n-  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n-  transition: opacity 0.2s ease;\n-\n-  &:hover {\n-    opacity: 1;\n-  }\n-`\n-\n-const NavText = styled.span`\n-  font-weight: 500;\n-`\n-\n-type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n-\n-function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n-  const themePart = isDark ? 'dark' : 'light'\n-  const pickPart = isActive ? '_pick' : ''\n-  return `/main/${key}_${themePart}${pickPart}.png`\n-}\n-\n-const Footer: React.FC<FooterProps> = ({ compact }) => {\n-  const location = useLocation()\n-  const navigate = useNavigate()\n-  const { isDarkMode } = useThemeStore()\n-  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-  const { isAuthenticated } = useAuthStore()\n-  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n-  const [parentWidth, setParentWidth] = useState<number | null>(null)\n-  const [isEmbed, setIsEmbed] = useState(false)\n-  \n-  useEffect(() => {\n-    const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams , companyCode )\n-    setIsEmbed(searchParams.get('embed') === '1')\n-    const onMsg = (e: MessageEvent) => {\n-      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n-        setParentWidth(e.data.width)\n-      }\n-    }\n-    window.addEventListener('message', onMsg)\n-    return () => window.removeEventListener('message', onMsg)\n-  }, [location])\n-\n-  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n-\n-  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n-    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n-  ]\n-\n-  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault()\n-\n-    const currentPath = `/aiclient/${companyCode}/`\n-\n-    if (location.pathname === currentPath) {\n-      window.scrollTo({ top: 0, behavior: 'smooth' })\n-    } else {\n-      navigate(currentPath)\n-    }\n-  }\n-\n-  return (\n-    <FooterWrapper $compact={compact}>\n-      <FooterContent>\n-        {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null;\n-  \n-          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n-  \n-          if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n-            return (\n-              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          const isActive = item.href ? location.pathname === item.href : false;\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n-  \n-          if (item.external) {\n-            return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={(e) => {\n-                  e.preventDefault();\n-                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n-                }}\n-              >\n-                <IconWrapper $isActive={false}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          // '나의견적' 또는 '설정' 버튼 처리\n-          if (isSpecialItem) {\n-            const handleItemClick = (e: React.MouseEvent) => {\n-              if (!isAuthenticated()) {\n-                e.preventDefault();\n-                setIsSocialLoginModalOpen(true);\n-              }\n-            };\n-  \n-            return (\n-              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </NavItem>\n-            );\n-          }\n-  \n-          return (\n-            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n-              <IconWrapper $isActive={isActive}>\n-                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-              </IconWrapper>\n-            </NavItem>\n-          );\n-        })}\n-      </FooterContent>\n-  \n-      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n-      {/* <SocialLoginModal\n-        $isOpen={isSocialLoginModalOpen}\n-        onClose={() => setIsSocialLoginModalOpen(false)}\n-      /> */}\n-      <CountModa\n-        $isOpen={isSocialLoginModalOpen}\n-        onClose={() => setIsSocialLoginModalOpen(false)}\n-      />\n-    </FooterWrapper>\n-  )\n-  \n-}\n-\n-export default Footer\n-import React, { useEffect, useState } from 'react'\n-import styled from 'styled-components'\n-import { Link, useLocation, useNavigate, useParams } from 'react-router-dom'\n-import { useThemeStore } from '@/store/themeStore'\n-import Icon, { IconName } from '@/components/ai-esti/Icon'\n-import { SocialLoginModal } from '@/components/ai-esti/SocialLoginModal';\n-import { useAuthStore } from '@/store/authStore';\n-\n-interface FooterProps { compact?: boolean }\n-\n-const FooterWrapper = styled.footer<{ $compact?: boolean }>`\n-  position: fixed;\n-  bottom: 0;\n-  left: 0;\n-  right: 0;\n-  height: 80px;\n-  background-color: ${({ theme }) => theme.body};\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  z-index: 1001;\n-`\n-\n-const FooterContent = styled.div`\n-  max-width: 1200px;\n-  margin: 0 auto;\n-  height: 100%;\n-  display: flex;\n-  align-items: center;\n-  justify-content: space-around;\n-  padding: 0 20px;\n-    z-index: 1001;\n-\n-`\n-\n-const NavItem = styled(Link)<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-\n-  &:hover {\n-    color: ${({ theme }) => theme.accent};\n-  }\n-`\n-\n-const ButtonLike = styled.a<{ $isActive?: boolean }>`\n-  display: flex;\n-  flex-direction: column;\n-  align-items: center;\n-  gap: 6px;\n-  text-decoration: none;\n-  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n-  font-size: 12px;\n-  min-width: 56px;\n-  padding: 8px 0;\n-  cursor: pointer;\n-\n-  &:hover { color: ${({ theme }) => theme.accent}; }\n-`\n-\n-const IconWrapper = styled.div<{ $isActive?: boolean }>`\n-  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n-  transition: opacity 0.2s ease;\n-\n-  &:hover {\n-    opacity: 1;\n-  }\n-`\n-\n-const NavText = styled.span`\n-  font-weight: 500;\n-`\n-\n-type ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n-\n-function getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n-  const themePart = isDark ? 'dark' : 'light'\n-  const pickPart = isActive ? '_pick' : ''\n-  return `/main/${key}_${themePart}${pickPart}.png`\n-}\n-\n-const Footer: React.FC<FooterProps> = ({ compact }) => {\n-  const location = useLocation()\n-  const navigate = useNavigate()\n-  const { isDarkMode } = useThemeStore()\n-  const { companyCode } = useParams() // URL에서 companyCode를 가져옵니다.\n-  const { isAuthenticated } = useAuthStore()\n-  const [isSocialLoginModalOpen, setIsSocialLoginModalOpen] = useState(false)\n-  const [parentWidth, setParentWidth] = useState<number | null>(null)\n-  const [isEmbed, setIsEmbed] = useState(false)\n-  \n-  useEffect(() => {\n-    const searchParams = new URLSearchParams(location.search)\n-    console.log(\"searchParams!!!\",searchParams , companyCode )\n-    setIsEmbed(searchParams.get('embed') === '1')\n-    const onMsg = (e: MessageEvent) => {\n-      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n-        setParentWidth(e.data.width)\n-      }\n-    }\n-    window.addEventListener('message', onMsg)\n-    return () => window.removeEventListener('message', onMsg)\n-  }, [location])\n-\n-  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n-\n-  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n-    { key: 'consultation', href: `/aiclient/${companyCode}/`, fallbackIcon: 'chat', text: '견적상담' },\n-    { key: 'estimate', href: `/aiclient/${companyCode}/my-estimate`, fallbackIcon: 'document', text: '나의견적' },\n-    { key: 'setting', href: `/aiclient/${companyCode}/settings`, fallbackIcon: 'settings', text: '설정' },\n-    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n-  ]\n-\n-  const handleConsultationClick = (e: React.MouseEvent<HTMLAnchorElement>) => {\n-    e.preventDefault()\n-\n-    const currentPath = `/aiclient/${companyCode}/`\n-\n-    if (location.pathname === currentPath) {\n-      window.scrollTo({ top: 0, behavior: 'smooth' })\n-    } else {\n-      navigate(currentPath)\n-    }\n-  }\n-\n-  return (\n-    <FooterWrapper $compact={compact}>\n-      <FooterContent>\n-        {navItems.map((item, idx) => {\n-          if (item.external && hideFull) return null;\n-  \n-          const isSpecialItem = item.key === 'estimate' || item.key === 'setting';\n-  \n-          if (item.key === 'consultation') {\n-            const isActive = location.pathname === `/aiclient/${companyCode}/`;\n-            const iconSrc = getIconSrc(item.key, isDarkMode, isActive);\n-            return (\n-              <ButtonLike key={item.href} $isActive={isActive} onClick={handleConsultationClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          const isActive = item.href ? location.pathname === item.href : false;\n-          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive);\n-  \n-          if (item.external) {\n-            return (\n-              <ButtonLike\n-                key={idx}\n-                $isActive={false}\n-                onClick={(e) => {\n-                  e.preventDefault();\n-                  window.open(`/aiclient/${companyCode}/ai`, '_blank', 'noopener,noreferrer');\n-                }}\n-              >\n-                <IconWrapper $isActive={false}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </ButtonLike>\n-            );\n-          }\n-  \n-          // '나의견적' 또는 '설정' 버튼 처리\n-          if (isSpecialItem) {\n-            const handleItemClick = (e: React.MouseEvent) => {\n-              if (!isAuthenticated()) {\n-                e.preventDefault();\n-                setIsSocialLoginModalOpen(true);\n-              }\n-            };\n-  \n-            return (\n-              <NavItem key={item.href} to={item.href} $isActive={isActive} onClick={handleItemClick}>\n-                <IconWrapper $isActive={isActive}>\n-                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-                </IconWrapper>\n-              </NavItem>\n-            );\n-          }\n-  \n-          return (\n-            <NavItem key={item.href} to={item.href} $isActive={isActive}>\n-              <IconWrapper $isActive={isActive}>\n-                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n-              </IconWrapper>\n-            </NavItem>\n-          );\n-        })}\n-      </FooterContent>\n-  \n-      {/* 소셜 로그인 모달 컴포넌트 추가 */}\n-      {/* <SocialLoginModal\n-        $isOpen={isSocialLoginModalOpen}\n-        onClose={() => setIsSocialLoginModalOpen(false)}\n-      /> */}\n       <CountModal\n         $isOpen={isSocialLoginModalOpen}\n         onClose={() => setIsSocialLoginModalOpen(false)}\n       />\n"
                }
            ],
            "date": 1755954060726,
            "name": "Commit-0",
            "content": "import React, { useEffect, useState } from 'react'\nimport styled from 'styled-components'\nimport { Link, useLocation } from 'react-router-dom'\nimport { useThemeStore } from '@/store/themeStore'\nimport Icon, { IconName } from '@/components/ai-esti/Icon'\n\ninterface FooterProps { compact?: boolean }\n\nconst FooterWrapper = styled.footer<{ $compact?: boolean }>`\n  position: fixed;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  height: 80px;\n  background-color: ${({ theme }) => theme.body};\n  border-top: 1px solid ${({ theme }) => theme.border};\n  z-index: 100;\n`\n\nconst FooterContent = styled.div`\n  max-width: 1200px;\n  margin: 0 auto;\n  height: 100%;\n  display: flex;\n  align-items: center;\n  justify-content: space-around;\n  padding: 0 20px;\n`\n\nconst NavItem = styled(Link)<{ $isActive?: boolean }>`\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  gap: 6px;\n  text-decoration: none;\n  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n  font-size: 12px;\n  min-width: 56px;\n  padding: 8px 0;\n\n  &:hover {\n    color: ${({ theme }) => theme.accent};\n  }\n`\n\nconst ButtonLike = styled.a<{ $isActive?: boolean }>`\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  gap: 6px;\n  text-decoration: none;\n  color: ${({ theme, $isActive }) => ($isActive ? theme.accent : theme.subtleText)};\n  font-size: 12px;\n  min-width: 56px;\n  padding: 8px 0;\n  cursor: pointer;\n\n  &:hover { color: ${({ theme }) => theme.accent}; }\n`\n\nconst IconWrapper = styled.div<{ $isActive?: boolean }>`\n  opacity: ${({ $isActive }) => ($isActive ? 1 : 0.7)};\n  transition: opacity 0.2s ease;\n\n  &:hover {\n    opacity: 1;\n  }\n`\n\nconst NavText = styled.span`\n  font-weight: 500;\n`\n\ntype ItemKey = 'consultation' | 'estimate' | 'setting' | 'full'\n\nfunction getIconSrc(key: ItemKey, isDark: boolean, isActive: boolean) {\n  const themePart = isDark ? 'dark' : 'light'\n  const pickPart = isActive ? '_pick' : ''\n  return `/main/${key}_${themePart}${pickPart}.png`\n}\n\nconst Footer: React.FC<FooterProps> = ({ compact }) => {\n  const location = useLocation()\n  const { isDarkMode } = useThemeStore()\n\n  // 부모 창의 뷰포트 폭(Widget에서 전달)을 기반으로 임베드 모바일 여부 판정\n  const [parentWidth, setParentWidth] = useState<number | null>(null)\n  const [isEmbed, setIsEmbed] = useState(false)\n  \n  useEffect(() => {\n    const searchParams = new URLSearchParams(location.search)\n    setIsEmbed(searchParams.get('embed') === '1')\n    const onMsg = (e: MessageEvent) => {\n      if (e?.data?.type === 'aiw:parentViewport' && typeof e.data.width === 'number') {\n        setParentWidth(e.data.width)\n      }\n    }\n    window.addEventListener('message', onMsg)\n    return () => window.removeEventListener('message', onMsg)\n  }, [location])\n\n  const hideFull = isEmbed && parentWidth !== null && parentWidth <= 520\n\n  const navItems: { key: ItemKey; href?: string; fallbackIcon: IconName; text: string; external?: boolean }[] = [\n    { key: 'consultation', href: '/', fallbackIcon: 'chat', text: '견적상담' },\n    { key: 'estimate', href: '/my-estimate', fallbackIcon: 'document', text: '나의견적' },\n    { key: 'setting', href: '/settings', fallbackIcon: 'settings', text: '설정' },\n    { key: 'full', fallbackIcon: 'expand', text: '전체화면', external: true },\n  ]\n\n  return (\n    <FooterWrapper $compact={compact}>\n      <FooterContent>\n        {navItems.map((item, idx) => {\n          // 임베드 + 부모 모바일이면 전체화면 메뉴 숨김\n          if (item.external && hideFull) return null\n\n          const isActive = item.href ? location.pathname === item.href : false\n          const iconSrc = getIconSrc(item.key, isDarkMode, item.external ? false : isActive)\n\n          if (item.external) {\n            return (\n              <ButtonLike\n                key={idx}\n                $isActive={false}\n                onClick={(e) => {\n                  e.preventDefault()\n                  window.open('/ai-estimate', '_blank', 'noopener,noreferrer')\n                }}\n              >\n                <IconWrapper $isActive={false}>\n                  <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n                </IconWrapper>\n                {/* <NavText>{item.text}</NavText> */}\n              </ButtonLike>\n            )\n          }\n\n          return (\n            <NavItem key={item.href} to={item.href!} $isActive={isActive}>\n              <IconWrapper $isActive={isActive}>\n                <Icon src={iconSrc} width={80} height={60} fallbackIcon={item.fallbackIcon} />\n              </IconWrapper>\n              {/* <NavText>{item.text}</NavText> */}\n            </NavItem>\n          )\n        })}\n      </FooterContent>\n    </FooterWrapper>\n  )\n}\n\nexport default Footer"
        }
    ]
}