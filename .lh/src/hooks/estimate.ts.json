{
    "sourceFile": "src/hooks/estimate.ts",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 4,
            "patches": [
                {
                    "date": 1757200364451,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1757200373669,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,5 @@\n-// utils/estimate.ts\n+// src/hooks/estimate.ts\n import { v4 as uuidv4 } from 'uuid';\n \n export function ensureEstimateUuid<T extends Record<string, any>>(est: T): T {\n   if (!est) return est;\n"
                },
                {
                    "date": 1757204999813,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -39,4 +39,16 @@\n <script type=\"application/json\" id=\"invoiceData\">\n ${json}\n </script>`;\n }\n+\n+\n+const getEstimateIdFromContent = (content: string): string | null => {\n+    try {\n+      const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+      if (!m) return null;\n+      const json = JSON.parse(m[1]);\n+      return json?.uuid || null;\n+    } catch {\n+      return null;\n+    }\n+  };\n\\ No newline at end of file\n"
                },
                {
                    "date": 1757210528229,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,54 +1,40 @@\n-// src/hooks/estimate.ts\n import { v4 as uuidv4 } from 'uuid';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n-export function ensureEstimateUuid<T extends Record<string, any>>(est: T): T {\n-  if (!est) return est;\n+export const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!m) return null;\n+    const data = JSON.parse(m[1]);\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) return null;\n+    return data as ProjectEstimate;\n+  } catch {\n+    return null;\n+  }\n+};\n+\n+export const ensureClientUuid = (est: ProjectEstimate) => {\n   if (!est.uuid) est.uuid = uuidv4();\n   return est;\n-}\n+};\n \n-export function normalizeEstimate<T extends Record<string, any>>(est: T): T {\n-  ensureEstimateUuid(est);\n-  est?.categories?.forEach((c: any, ci: number) =>\n-    c?.sub_categories?.forEach((sc: any, si: number) =>\n-      sc?.items?.forEach((it: any, ii: number) => {\n-        if (typeof it.is_deleted !== 'boolean') it.is_deleted = false;\n-        if (!it.item_id) it.item_id = `${ci}-${si}-${ii}`;\n-      })\n-    )\n-  );\n-  return est;\n-}\n+/** 메시지 content에서 견적을 파싱하고 uuid를 보장한 뒤 반환 */\n+export const getOrEnsureEstimateFromContent = (content: string) => {\n+  const estimate = extractEstimateData(content);\n+  if (!estimate) return { estimate: null as ProjectEstimate | null };\n \n-/** reply 전체에서 <script> 블록 제거한 \"인트로\"만 추출 */\n-export function extractIntroFromReply(reply: string) {\n-  return reply.replace(/<script type=\"application\\/json\" id=\"invoiceData\">[\\s\\S]*?<\\/script>/g, '').trim();\n-}\n+  const ensured = ensureClientUuid({ ...estimate });\n+  return { estimate: ensured };\n+};\n \n-/** 인트로 + 스크립트(JSON) 문자열 조립 */\n-export function buildFullEstimateData(estimate: any, intro?: string) {\n-  const prepared = normalizeEstimate(JSON.parse(JSON.stringify(estimate || {})));\n-  const json = JSON.stringify(prepared, null, 2);\n-  const headline =\n\\ No newline at end of file\n-    (intro && intro.length > 0)\n-      ? intro\n-      : '지금까지 논의된 내용을 바탕으로 주요 기능과 예상 비용을 정리한 견적서를 아래에 바로 제공드립니다.';\n-\n-  return `${headline}\n-\n-<script type=\"application/json\" id=\"invoiceData\">\n-${json}\n-</script>`;\n-}\n-\n-\n-const getEstimateIdFromContent = (content: string): string | null => {\n-    try {\n-      const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n-      if (!m) return null;\n-      const json = JSON.parse(m[1]);\n-      return json?.uuid || null;\n-    } catch {\n-      return null;\n-    }\n-  };\n+/** 문자열 content에서 uuid만 바로 뽑고 싶을 때 */\n+export const getEstimateIdFromContent = (content: string): string | null => {\n+  try {\n+    const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!m) return null;\n+    const json = JSON.parse(m[1]);\n+    return json?.uuid || null;\n+  } catch {\n+    return null;\n+  }\n+};\n"
                },
                {
                    "date": 1757210603767,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,40 +1,87 @@\n+// src/hooks/estimate.ts\n import { v4 as uuidv4 } from 'uuid';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n-export const extractEstimateData = (content: string): ProjectEstimate | null => {\n+/** 견적 객체에 uuid가 없으면 생성해서 채워줌 */\n+export function ensureEstimateUuid<T extends Record<string, any>>(est: T): T {\n+  if (!est) return est;\n+  if (!est.uuid) est.uuid = uuidv4();\n+  return est;\n+}\n+\n+/** 견적 객체 정규화: uuid 보장, is_deleted 기본값, item_id 기본값 부여 */\n+export function normalizeEstimate<T extends Record<string, any>>(est: T): T {\n+  ensureEstimateUuid(est);\n+  est?.categories?.forEach((c: any, ci: number) =>\n+    c?.sub_categories?.forEach((sc: any, si: number) =>\n+      sc?.items?.forEach((it: any, ii: number) => {\n+        if (typeof it.is_deleted !== 'boolean') it.is_deleted = false;\n+        if (!it.item_id) it.item_id = `${ci}-${si}-${ii}`;\n+      })\n+    )\n+  );\n+  return est;\n+}\n+\n+/** reply 전체에서 <script> 블록 제거한 \"인트로\"만 추출 */\n+export function extractIntroFromReply(reply: string) {\n+  return reply\n+    .replace(/<script type=\"application\\/json\" id=\"invoiceData\">[\\s\\S]*?<\\/script>/g, '')\n+    .trim();\n+}\n+\n+/** 인트로 + 스크립트(JSON) 문자열 조립 */\n+export function buildFullEstimateData(estimate: any, intro?: string) {\n+  const prepared = normalizeEstimate(JSON.parse(JSON.stringify(estimate || {})));\n+  const json = JSON.stringify(prepared, null, 2);\n+  const headline =\n+    (intro && intro.length > 0)\n+      ? intro\n+      : '지금까지 논의된 내용을 바탕으로 주요 기능과 예상 비용을 정리한 견적서를 아래에 바로 제공드립니다.';\n+\n+  return `${headline}\n+\n+<script type=\"application/json\" id=\"invoiceData\">\n+${json}\n+</script>`;\n+}\n+\n+/** 메시지 content에서 invoiceData JSON을 파싱해서 객체로 반환 */\n+export function extractEstimateData<T = any>(content: string): T | null {\n   try {\n-    const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    const m = content.match(\n+      /<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/\n+    );\n     if (!m) return null;\n     const data = JSON.parse(m[1]);\n-    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) return null;\n-    return data as ProjectEstimate;\n+    if (!data || typeof data !== 'object') return null;\n+    return data as T;\n   } catch {\n     return null;\n   }\n-};\n+}\n \n-export const ensureClientUuid = (est: ProjectEstimate) => {\n-  if (!est.uuid) est.uuid = uuidv4();\n-  return est;\n-};\n+/** 메시지 content에서 견적을 파싱하고 uuid 보장(없으면 생성) 후 돌려줌 */\n+export function getOrEnsureEstimateFromContent<T = any>(content: string) {\n+  const est = extractEstimateData<T>(content);\n+  if (!est) return { estimate: null as T | null };\n+  // 깊은 복사 후 정규화(= uuid/flags/id 보장)\n+  const ensured = normalizeEstimate(JSON.parse(JSON.stringify(est)));\n+  return { estimate: ensured as T };\n+}\n \n-/** 메시지 content에서 견적을 파싱하고 uuid를 보장한 뒤 반환 */\n-export const getOrEnsureEstimateFromContent = (content: string) => {\n-  const estimate = extractEstimateData(content);\n-  if (!estimate) return { estimate: null as ProjectEstimate | null };\n-\n-  const ensured = ensureClientUuid({ ...estimate });\n-  return { estimate: ensured };\n-};\n-\n-/** 문자열 content에서 uuid만 바로 뽑고 싶을 때 */\n-export const getEstimateIdFromContent = (content: string): string | null => {\n+/** 문자열 content에서 uuid만 바로 추출하고 싶을 때 */\n+export function getEstimateIdFromContent(content: string): string | null {\n   try {\n-    const m = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    const m = content.match(\n+      /<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/\n+    );\n     if (!m) return null;\n     const json = JSON.parse(m[1]);\n     return json?.uuid || null;\n   } catch {\n     return null;\n   }\n-};\n+}\n+\n+/** (선택) 이름이 더 직관적인 alias가 필요하면 export */\n+export const ensureClientUuid = ensureEstimateUuid;\n"
                }
            ],
            "date": 1757200364451,
            "name": "Commit-0",
            "content": "// utils/estimate.ts\nimport { v4 as uuidv4 } from 'uuid';\n\nexport function ensureEstimateUuid<T extends Record<string, any>>(est: T): T {\n  if (!est) return est;\n  if (!est.uuid) est.uuid = uuidv4();\n  return est;\n}\n\nexport function normalizeEstimate<T extends Record<string, any>>(est: T): T {\n  ensureEstimateUuid(est);\n  est?.categories?.forEach((c: any, ci: number) =>\n    c?.sub_categories?.forEach((sc: any, si: number) =>\n      sc?.items?.forEach((it: any, ii: number) => {\n        if (typeof it.is_deleted !== 'boolean') it.is_deleted = false;\n        if (!it.item_id) it.item_id = `${ci}-${si}-${ii}`;\n      })\n    )\n  );\n  return est;\n}\n\n/** reply 전체에서 <script> 블록 제거한 \"인트로\"만 추출 */\nexport function extractIntroFromReply(reply: string) {\n  return reply.replace(/<script type=\"application\\/json\" id=\"invoiceData\">[\\s\\S]*?<\\/script>/g, '').trim();\n}\n\n/** 인트로 + 스크립트(JSON) 문자열 조립 */\nexport function buildFullEstimateData(estimate: any, intro?: string) {\n  const prepared = normalizeEstimate(JSON.parse(JSON.stringify(estimate || {})));\n  const json = JSON.stringify(prepared, null, 2);\n  const headline =\n    (intro && intro.length > 0)\n      ? intro\n      : '지금까지 논의된 내용을 바탕으로 주요 기능과 예상 비용을 정리한 견적서를 아래에 바로 제공드립니다.';\n\n  return `${headline}\n\n<script type=\"application/json\" id=\"invoiceData\">\n${json}\n</script>`;\n}\n"
        }
    ]
}