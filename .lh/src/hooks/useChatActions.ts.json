{
    "sourceFile": "src/hooks/useChatActions.ts",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 104,
            "patches": [
                {
                    "date": 1756365557553,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1756365563942,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,6 +1,4 @@\n-// src/hooks/useChatActions.ts\n-\n import { useState } from 'react';\n import useAI from './useAI'; // 기존 useAI 훅 경로\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n"
                },
                {
                    "date": 1756367944529,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,11 +1,12 @@\n-import { useState } from 'react';\n+import { useState, useEffect } from 'react';\n import useAI from './useAI'; // 기존 useAI 훅 경로\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n+import { createChatSession, sendChatMessage } from '@/lib/api/chatApi'; // ⭐️ 추가된 API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -13,11 +14,13 @@\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n   const { sendChat } = useAI(modelName);\n   const { success, error } = useToast();\n-  const { addMessage, updateLastMessage } = useChatStore((s) => ({\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n     addMessage: s.addMessage,\n     updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n   }));\n \n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n@@ -88,12 +91,36 @@\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n     }\n+    \n+    // ⭐️ 사용자 메시지를 먼저 화면에 표시\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    // ⭐️ API 로직: 채팅 세션 ID가 없으면 새로 생성\n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        if (createResponse?.data?.sessionId) {\n+          currentSessionId = createResponse.data.sessionId;\n+          setChatSessionId(currentSessionId);\n+          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n+        } else {\n+          throw new Error('채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n \n     try {\n+      // ⭐️ 메시지 전송 API 호출\n+      await sendChatMessage(currentSessionId, { role: 'user', content: input });\n+      \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n         fileUri: file.fileUri,\n         mimeType: file.mimeType,\n@@ -113,13 +140,13 @@\n     handleSubmit,\n     isProcessing,\n     uploadedFiles,\n     isDragOver,\n-    uploadProgress,\n     isUploading,\n     handleDragOver,\n     handleDragLeave,\n     handleDrop,\n     handleFileInput,\n     removeFile,\n+    uploadProgress,\n   };\n\\ No newline at end of file\n-}\n+}\n"
                },
                {
                    "date": 1756367975907,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,9 +4,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n-import { createChatSession, sendChatMessage } from '@/lib/api/chatApi'; // ⭐️ 추가된 API import\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/Api'; // ⭐️ 추가된 API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -148,5 +148,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-}\n\\ No newline at end of file\n+}\n"
                },
                {
                    "date": 1756368087203,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,9 +4,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/Api'; // ⭐️ 추가된 API import\n+import { createChatSession, sendChatMessage } from '@/lib/api/chatApi'; // ⭐️ 추가된 API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -101,13 +101,14 @@\n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        if (createResponse?.data?.sessionId) {\n+        if (createResponse && createResponse.data && createResponse.data.sessionId) {\n           currentSessionId = createResponse.data.sessionId;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n+          // 서버에서 유효한 세션 ID를 반환하지 않았을 경우\n           throw new Error('채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756368094858,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,9 +4,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n-import { createChatSession, sendChatMessage } from '@/lib/api/chatApi'; // ⭐️ 추가된 API import\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // ⭐️ 추가된 API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n"
                },
                {
                    "date": 1756434162939,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,10 +3,10 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // ⭐️ 추가된 API import\n+import type { ModelName } from '@/app/ai-estimate/types';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -27,9 +27,9 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수\n+  // 파일 업로드 관련 함수 (기존 코드 유지)\n   const handleFileUpload = (fileData: FileUploadData) => {\n     setUploadedFiles((prev) => [...prev, fileData]);\n     setUploadProgress(0);\n     setIsUploading(false);\n@@ -80,9 +80,9 @@\n       });\n     }\n   };\n \n-  // 핵심 로직: handleSubmit\n+  // ⭐️ 핵심 로직: handleSubmit 수정\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -96,19 +96,18 @@\n     // ⭐️ 사용자 메시지를 먼저 화면에 표시\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n     \n-    // ⭐️ API 로직: 채팅 세션 ID가 없으면 새로 생성\n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        if (createResponse && createResponse.data && createResponse.data.sessionId) {\n-          currentSessionId = createResponse.data.sessionId;\n+        \n+        if (createResponse && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id; // ⭐️ _id를 세션 ID로 설정\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n-          // 서버에서 유효한 세션 ID를 반환하지 않았을 경우\n           throw new Error('채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n@@ -117,10 +116,13 @@\n       }\n     }\n \n     try {\n-      // ⭐️ 메시지 전송 API 호출\n-      await sendChatMessage(currentSessionId, { role: 'user', content: input });\n+      // ⭐️ 1. 사용자 메시지를 API로 전송\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input } // API 요구 형식에 맞춰 객체로 수정\n+      });\n       \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n         fileUri: file.fileUri,\n@@ -129,10 +131,19 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n+      // ⭐️ 2. AI의 응답 메시지를 API로 전송\n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: reply } // AI 응답도 객체로 수정\n+      });\n+\n       updateLastMessage(reply);\n       setUploadedFiles([]);\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n       setIsProcessing(false);\n     }\n   };\n@@ -149,5 +160,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-}\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756434249248,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,162 @@\n+import { useState, useEffect } from 'react';\n+import useAI from './useAI';\n+import { useToast } from '@/components/common/ToastProvider';\n+import { useChatStore } from '@/store/chatStore';\n+import { combinePrompts } from '@/ai/promptTemplates';\n+import { uploadFiles, FileUploadData } from '@/firebase.functions';\n+import type { ModelName } from '@/app/ai-estimate/types';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+\n+interface UseChatActionsProps {\n+  modelName: ModelName;\n+  selectedPromptId: string;\n+}\n+\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n+\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n+\n+  // 파일 업로드 관련 함수 (기존 코드 유지)\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n+\n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+\n+    setIsProcessing(true);\n+\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        \n+        // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n+        } else {\n+          // ⭐️ statusCode가 200이 아니거나 데이터가 없을 경우 에러 처리\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n+\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n+      \n+      const filesForGemini = uploadedFiles.map((file) => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+      }));\n+\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForGemini);\n+\n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: reply }\n+      });\n+\n+      updateLastMessage(reply);\n+      setUploadedFiles([]);\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n+    }\n+  };\n+\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n+  };\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756434307434,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -100,9 +100,9 @@\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n         // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n@@ -158,169 +158,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-}\n-import { useState, useEffect } from 'react';\n-import useAI from './useAI'; // 기존 useAI 훅 경로\n-import { useToast } from '@/components/common/ToastProvider';\n-import { useChatStore } from '@/store/chatStore';\n-import { combinePrompts } from '@/ai/promptTemplates';\n-import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types';\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // API import\n-\n-interface UseChatActionsProps {\n-  modelName: ModelName;\n-  selectedPromptId: string;\n-}\n-\n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n-\n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n-\n-  // 파일 업로드 관련 함수 (기존 코드 유지)\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n-\n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  // ⭐️ 핵심 로직: handleSubmit 수정\n-  const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n-\n-    setIsProcessing(true);\n-\n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n-    }\n-    \n-    // ⭐️ 사용자 메시지를 먼저 화면에 표시\n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        \n-        if (createResponse && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id; // ⭐️ _id를 세션 ID로 설정\n-          setChatSessionId(currentSessionId);\n-          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n-        } else {\n-          throw new Error('채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n-\n-    try {\n-      // ⭐️ 1. 사용자 메시지를 API로 전송\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input } // API 요구 형식에 맞춰 객체로 수정\n-      });\n-      \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n-\n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n-\n-      // ⭐️ 2. AI의 응답 메시지를 API로 전송\n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: reply } // AI 응답도 객체로 수정\n-      });\n-\n-      updateLastMessage(reply);\n-      setUploadedFiles([]);\n-\n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n-  };\n-\n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756434326663,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -100,9 +100,9 @@\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n         // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data) {\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n"
                },
                {
                    "date": 1756436677381,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,9 +5,8 @@\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-\n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n }\n@@ -99,15 +98,13 @@\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n-        // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n-          // ⭐️ statusCode가 200이 아니거나 데이터가 없을 경우 에러 처리\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n@@ -130,8 +127,23 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n+      // ⭐️ AI 답변에서 견적서 JSON이 있는지 확인\n+      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          // ⭐️ PDF를 생성하고 서버로 업로드하는 함수 호출\n+          // 이 함수는 견적서 UI 렌더링 후 DOM에서 Canvas를 캡처하는 로직 등이 필요합니다.\n+          // 여기서 필요한 모든 데이터를 전달해야 합니다.\n+          // 예시: const pdfFile = await generatePdfFromDom();\n+          // await uploadEstimatePdf(currentSessionId, '견적서 제목', pdfFile);\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error('견적서 업로드에 실패했습니다.');\n+        }\n+      }\n+\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n         content: { type: 'text', value: reply }\n       });\n"
                },
                {
                    "date": 1756436685720,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,8 +5,9 @@\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+\n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n }\n@@ -98,13 +99,15 @@\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n+        // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n+          // ⭐️ statusCode가 200이 아니거나 데이터가 없을 경우 에러 처리\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n@@ -127,23 +130,8 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n-      // ⭐️ AI 답변에서 견적서 JSON이 있는지 확인\n-      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          // ⭐️ PDF를 생성하고 서버로 업로드하는 함수 호출\n-          // 이 함수는 견적서 UI 렌더링 후 DOM에서 Canvas를 캡처하는 로직 등이 필요합니다.\n-          // 여기서 필요한 모든 데이터를 전달해야 합니다.\n-          // 예시: const pdfFile = await generatePdfFromDom();\n-          // await uploadEstimatePdf(currentSessionId, '견적서 제목', pdfFile);\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error('견적서 업로드에 실패했습니다.');\n-        }\n-      }\n-\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n         content: { type: 'text', value: reply }\n       });\n"
                },
                {
                    "date": 1756440298491,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,166 @@\n+interface UseChatActionsProps {\n+  modelName: ModelName;\n+  selectedPromptId: string;\n+}\n+\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n+\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n+\n+  // 파일 업로드 관련 함수 (기존 코드 유지)\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n+\n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+\n+    setIsProcessing(true);\n+\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        \n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n+        } else {\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n+\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n+      \n+      const filesForGemini = uploadedFiles.map((file) => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+      }));\n+\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForGemini);\n+\n+      // ⭐️ AI 답변에서 견적서 JSON이 있는지 확인\n+      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          // ⭐️ PDF를 생성하고 서버로 업로드하는 함수 호출\n+          // 이 함수는 견적서 UI 렌더링 후 DOM에서 Canvas를 캡처하는 로직 등이 필요합니다.\n+          // 여기서 필요한 모든 데이터를 전달해야 합니다.\n+          // 예시: const pdfFile = await generatePdfFromDom();\n+          // await uploadEstimatePdf(currentSessionId, '견적서 제목', pdfFile);\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error('견적서 업로드에 실패했습니다.');\n+        }\n+      }\n+\n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: reply }\n+      });\n+\n+      updateLastMessage(reply);\n+      setUploadedFiles([]);\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n+    }\n+  };\n+\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n+  };\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756440527984,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,178 +1,12 @@\n-interface UseChatActionsProps {\n-  modelName: ModelName;\n-  selectedPromptId: string;\n-}\n-\n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n-\n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n-\n-  // 파일 업로드 관련 함수 (기존 코드 유지)\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n-\n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n-\n-    setIsProcessing(true);\n-\n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n-    }\n-    \n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n-        } else {\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n-\n-    try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n-      });\n-      \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n-\n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n-\n-      // ⭐️ AI 답변에서 견적서 JSON이 있는지 확인\n-      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          // ⭐️ PDF를 생성하고 서버로 업로드하는 함수 호출\n-          // 이 함수는 견적서 UI 렌더링 후 DOM에서 Canvas를 캡처하는 로직 등이 필요합니다.\n-          // 여기서 필요한 모든 데이터를 전달해야 합니다.\n-          // 예시: const pdfFile = await generatePdfFromDom();\n-          // await uploadEstimatePdf(currentSessionId, '견적서 제목', pdfFile);\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error('견적서 업로드에 실패했습니다.');\n-        }\n-      }\n-\n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: reply }\n-      });\n-\n-      updateLastMessage(reply);\n-      setUploadedFiles([]);\n-\n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n-  };\n-\n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n-}\n import { useState, useEffect } from 'react';\n-import useAI from './useAI';\n+import useAI from './useAI'; // 기존 useAI 훅 경로\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // API import\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -246,8 +80,9 @@\n       });\n     }\n   };\n \n+  // ⭐️ 핵심 로직: handleSubmit 수정\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -257,24 +92,23 @@\n       const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n     }\n     \n+    // ⭐️ 사용자 메시지를 먼저 화면에 표시\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n     \n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n-        // ⭐️ 응답 상태코드와 데이터 존재 여부로 성공 판단\n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n+        if (createResponse && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id; // ⭐️ _id를 세션 ID로 설정\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n-          // ⭐️ statusCode가 200이 아니거나 데이터가 없을 경우 에러 처리\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+          throw new Error('채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n         setIsProcessing(false);\n@@ -282,11 +116,12 @@\n       }\n     }\n \n     try {\n+      // ⭐️ 1. 사용자 메시지를 API로 전송\n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n-        content: { type: 'text', value: input }\n+        content: { type: 'text', value: input } // API 요구 형식에 맞춰 객체로 수정\n       });\n       \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n@@ -296,11 +131,12 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n+      // ⭐️ 2. AI의 응답 메시지를 API로 전송\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n-        content: { type: 'text', value: reply }\n+        content: { type: 'text', value: reply } // AI 응답도 객체로 수정\n       });\n \n       updateLastMessage(reply);\n       setUploadedFiles([]);\n"
                },
                {
                    "date": 1756441088050,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,12 +1,15 @@\n-import { useState, useEffect } from 'react';\n-import useAI from './useAI'; // 기존 useAI 훅 경로\n+\"use client\"\n+\n+import { useState } from 'react';\n+import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi'; // API import\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { generateAndUploadPdf } from '@/lib/pdfUtils';\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n@@ -27,9 +30,8 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수 (기존 코드 유지)\n   const handleFileUpload = (fileData: FileUploadData) => {\n     setUploadedFiles((prev) => [...prev, fileData]);\n     setUploadProgress(0);\n     setIsUploading(false);\n@@ -80,9 +82,8 @@\n       });\n     }\n   };\n \n-  // ⭐️ 핵심 로직: handleSubmit 수정\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -92,23 +93,22 @@\n       const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n     }\n     \n-    // ⭐️ 사용자 메시지를 먼저 화면에 표시\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n     \n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         \n-        if (createResponse && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id; // ⭐️ _id를 세션 ID로 설정\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n           console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n-          throw new Error('채팅방 생성에 실패했습니다.');\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n         setIsProcessing(false);\n@@ -116,12 +116,11 @@\n       }\n     }\n \n     try {\n-      // ⭐️ 1. 사용자 메시지를 API로 전송\n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n-        content: { type: 'text', value: input } // API 요구 형식에 맞춰 객체로 수정\n+        content: { type: 'text', value: input }\n       });\n       \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n@@ -131,12 +130,32 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n-      // ⭐️ 2. AI의 응답 메시지를 API로 전송\n+      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          const scriptTagRegex = /<script[^>]*id=\"invoiceData\"[^>]*>([\\s\\S]*?)<\\/script>/;\n+          const match = reply.match(scriptTagRegex);\n+          \n+          if (match && match[1]) {\n+            const jsonData = JSON.parse(match[1]);\n+            const invoiceTitle = jsonData.project_name || '새로운 견적서';\n+            \n+            await generateAndUploadPdf(jsonData, currentSessionId, invoiceTitle, success, error);\n+          } else {\n+            console.warn('견적서 JSON이 포함된 <script> 태그를 찾을 수 없습니다.');\n+          }\n+\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+        }\n+      }\n+\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n-        content: { type: 'text', value: reply } // AI 응답도 객체로 수정\n+        content: { type: 'text', value: reply }\n       });\n \n       updateLastMessage(reply);\n       setUploadedFiles([]);\n"
                },
                {
                    "date": 1756441094679,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,9 +7,9 @@\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/lib/pdfUtils';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n \n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n"
                },
                {
                    "date": 1756441958565,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,5 @@\n-\"use client\"\n+// src/hooks/useChatActions.ts\n \n import { useState } from 'react';\n import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n@@ -7,10 +7,32 @@\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n+import { generateAndUploadPdf } from '@/lib/pdfUtils';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n+// 견적서 데이터를 추출하는 유틸리티 함수\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!match) return null;\n+\n+    const jsonStr = match[1];\n+    const data = JSON.parse(jsonStr);\n+\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n+      console.error('Invalid estimate data structure:', data);\n+      return null;\n+    }\n+\n+    return data as ProjectEstimate;\n+  } catch (error) {\n+    console.error('Failed to parse estimate data:', error);\n+    return null;\n+  }\n+};\n+\n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n }\n@@ -30,60 +52,17 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n+  // 파일 업로드 관련 함수들... (생략)\n+  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n+  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n+  const removeFile = (fileName: string) => { /* ... */ };\n+  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n+  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n+  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n \n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -104,9 +83,8 @@\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n-          console.log(`새로운 채팅방이 생성되었습니다: ${currentSessionId}`);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n@@ -130,23 +108,16 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n-      if (reply.includes('<script type=\"application/json\" id=\"invoiceData\">')) {\n+      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n+      const estimateData = extractEstimateData(reply);\n+      if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n-          const scriptTagRegex = /<script[^>]*id=\"invoiceData\"[^>]*>([\\s\\S]*?)<\\/script>/;\n-          const match = reply.match(scriptTagRegex);\n-          \n-          if (match && match[1]) {\n-            const jsonData = JSON.parse(match[1]);\n-            const invoiceTitle = jsonData.project_name || '새로운 견적서';\n-            \n-            await generateAndUploadPdf(jsonData, currentSessionId, invoiceTitle, success, error);\n-          } else {\n-            console.warn('견적서 JSON이 포함된 <script> 태그를 찾을 수 없습니다.');\n-          }\n-\n+          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n+          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n         }\n"
                },
                {
                    "date": 1756441971512,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -7,9 +7,9 @@\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/lib/pdfUtils';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n"
                },
                {
                    "date": 1756451666171,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,6 +1,4 @@\n-// src/hooks/useChatActions.ts\n-\n import { useState } from 'react';\n import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n@@ -108,28 +106,42 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n+      let finalReply = reply;\n+\n       // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n+          // 1. UUID 생성\n+          const pdfUuid = crypto.randomUUID();\n+          \n+          // 2. 응답 메시지에 UUID를 포함시키는 로직 추가\n+          finalReply = reply.replace(\n+            '<script type=\"application/json\" id=\"invoiceData\">',\n+            `<script type=\"application/json\" id=\"invoiceData\" data-uuid=\"${pdfUuid}\">`\n+          );\n+\n+          // 3. PDF 생성 및 업로드 (UUID를 파일명으로 사용)\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          // PDF 업로드 실패 시 UUID를 추가하지 않은 원래의 응답을 사용\n+          finalReply = reply;\n         }\n       }\n \n+      // ⭐️ 수정된 메시지를 서버에 전송하고 스토어 업데이트\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n-        content: { type: 'text', value: reply }\n+        content: { type: 'text', value: finalReply }\n       });\n \n-      updateLastMessage(reply);\n+      updateLastMessage(finalReply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756451739875,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,166 +1,327 @@\n-import { useState } from 'react';\n-import useAI from './useAI';\n+\"use client\";\n+\n+import React, { useState } from 'react';\n+import styled from 'styled-components';\n+import { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import Icon from './Icon';\n+import Modal from '@/components/common/Modal';\n import { useToast } from '@/components/common/ToastProvider';\n-import { useChatStore } from '@/store/chatStore';\n-import { combinePrompts } from '@/ai/promptTemplates';\n-import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types';\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { useThemeStore } from '@/store/themeStore';\n+import html2canvas from 'html2canvas';\n+import jsPDF from 'jspdf';\n+import { PrintableInvoice } from './PrintableInvoice';\n+import { getDownloadEstimateUrl } from '@/lib/api/user/userApi';\n \n-// 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n-  try {\n-    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n-    if (!match) return null;\n+const CardWrapper = styled.div`\n+  background-color: ${({ theme }) => theme.surface1};\n+  color: ${({ theme }) => theme.text};\n+  border-radius: 12px;\n+  overflow: hidden;\n+  margin: 24px 0;\n+  padding: 20px 12px;\n+`;\n \n-    const jsonStr = match[1];\n-    const data = JSON.parse(jsonStr);\n+const Header = styled.div`\n+  padding: 0px;\n \n-    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n-      console.error('Invalid estimate data structure:', data);\n-      return null;\n-    }\n+`;\n \n-    return data as ProjectEstimate;\n-  } catch (error) {\n-    console.error('Failed to parse estimate data:', error);\n-    return null;\n+  const Flex = styled.div`\n+  display: flex;\n+  align-items: flex-start;\n+  justify-content: space-between;\n+  margin: 0 0 18px 0;\n+`;\n+\n+const Title = styled.h2`\n+  font-size: 20px;\n+font-style: normal;\n+font-weight: 700;\n+line-height: normal;\n+`;\n+\n+\n+const Right = styled.div`\n+  display: flex;\n+  align-items: center;\n+  gap: 8px;\n+`;\n+\n+const Price = styled.p`\n+  font-size: 20px;\n+font-style: normal;\n+font-weight: 700;\n+line-height: normal;\n+letter-spacing: 0.4px;\n+  margin: 0 0 8px 0;\n+  \n+  span {\n+    font-size: 0.7em;\n+    font-weight: normal;\n+    color: ${({ theme }) => theme.subtleText};\n+    margin-left: 8px;\n   }\n-};\n+`;\n \n-interface UseChatActionsProps {\n-  modelName: ModelName;\n-  selectedPromptId: string;\n+const Period = styled.p`\n+  font-size: 16px;\n+font-style: normal;\n+font-weight: 700;\n+line-height: normal;\n+letter-spacing: 0.32px;\n+  color: ${({ theme }) => theme.text};\n+  margin: 0 0 24px 0;\n+\n+  .p {\n+  font-size: 12px;\n+font-style: normal;\n+font-weight: 400;\n+line-height: normal;\n+letter-spacing: 0.24px;\n+color: ${({ theme }) => theme.subtleText};\n }\n+`;\n \n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n+const ActionButtons = styled.div`\n+  display: flex;\n+  gap: 10px;\n+  padding 20px 12px;\n+  border-top: 1px solid ${({ theme }) => theme.border};\n+  margin: 4px;\n+`;\n \n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n+const Line = styled.div`\n+  border-left: 1px solid ${({ theme }) => theme.border};\n+  `;\n \n-  // 파일 업로드 관련 함수들... (생략)\n-  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n-  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n-  const removeFile = (fileName: string) => { /* ... */ };\n-  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n-  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n-  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n+const ActionButton = styled.button<{ primary?: boolean }>`\n+  flex: 1;\n+  padding: 12px;\n+  border-radius: 8px;\n+  border: none;\n+  background-color: ${({ theme, primary }) => (primary ? theme.accent : 'transparent')};\n+  color: ${({ theme, primary }) => (primary ? (theme.body) : theme.accent)};\n+  font-family: Roboto;\n+  font-size: 14px;\n+  font-style: normal;\n+  font-weight: 600;\n+  line-height: 160%; \n+  letter-spacing: 0.28px;\n+  cursor: pointer;\n+  transition: all 0.2s ease;\n \n-  const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+  &:hover {\n+    opacity: 0.8;\n\\ No newline at end of file\n+  }\n+`;\n \n-    setIsProcessing(true);\n+const ShareInput = styled.div`\n+  display: flex;\n+  gap: 8px;\n+  margin-top: 8px;\n \n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n-    }\n-    \n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-        } else {\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n+  input {\n+    flex: 1;\n+    height: 44px;\n+    border-radius: 8px;\n+    border: 1px solid #e5e7eb;\n+    background: #f9fafb;\n+    color: #111827;\n+    padding: 0 12px;\n+  }\n \n+  button {\n+    height: 44px;\n+    padding: 0 14px;\n+    border-radius: 8px;\n+    background: #2E2E48;\n+    color: white;\n+    font-size: 14px;\n+font-style: normal;\n+font-weight: 400;\n+line-height: 160%;\n+letter-spacing: 0.32px;\n+  }\n+`;\n+\n+interface EstimateCardProps {\n+  // ⭐️ ProjectEstimate 타입에 uuid 속성을 추가했다고 가정\n+  estimate: ProjectEstimate & { uuid?: string };\n+}\n+\n+const EstimateCard: React.FC<EstimateCardProps> = ({ estimate }) => {\n+  const [openShare, setOpenShare] = useState(false)\n+  const { success } = useToast()\n+  const { isDarkMode } = useThemeStore()\n+\n+  const generatePDF = async () => {\n     try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n+      // 임시 컨테이너 생성\n+      const tempDiv = document.createElement('div');\n+      tempDiv.style.position = 'absolute';\n+      tempDiv.style.left = '-9999px';\n+      document.body.appendChild(tempDiv);\n+\n+      // PrintableInvoice 렌더링\n+      const root = document.createElement('div');\n+      root.style.width = '780px';\n+      root.style.backgroundColor = 'white';\n+      tempDiv.appendChild(root);\n+\n+      // React 컴포넌트를 DOM에 렌더링\n+      const { createRoot } = await import('react-dom/client');\n+      const reactRoot = createRoot(root);\n+      reactRoot.render(<PrintableInvoice estimate={estimate} />);\n+\n+      // 렌더링이 완료될 때까지 잠시 대기\n+      await new Promise(resolve => setTimeout(resolve, 100));\n+\n+      // HTML을 캔버스로 변환\n+      const canvas = await html2canvas(root, {\n+        scale: 1.5,\n+        useCORS: true,\n+        logging: false,\n+        imageTimeout: 0,\n+        backgroundColor: null\n       });\n-      \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n \n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n+      // PDF 생성\n+      const imgWidth = 210; // A4 가로 크기 (mm)\n+      const pageHeight = 297; // A4 세로 크기 (mm)\n+      const imgHeight = canvas.height * imgWidth / canvas.width;\n+      const pdf = new jsPDF('p', 'mm');\n \n-      let finalReply = reply;\n+      // 여러 페이지로 나누기\n+      let heightLeft = imgHeight;\n+      let position = 0;\n+      let pageNumber = 1;\n \n-      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n-      const estimateData = extractEstimateData(reply);\n-      if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          // 1. UUID 생성\n-          const pdfUuid = crypto.randomUUID();\n-          \n-          // 2. 응답 메시지에 UUID를 포함시키는 로직 추가\n-          finalReply = reply.replace(\n-            '<script type=\"application/json\" id=\"invoiceData\">',\n-            `<script type=\"application/json\" id=\"invoiceData\" data-uuid=\"${pdfUuid}\">`\n-          );\n+      // 첫 페이지 추가 (JPEG 압축 사용)\n+      const imageData = canvas.toDataURL('image/jpeg', 0.7);\n+      pdf.addImage(imageData, 'JPEG', 0, position, imgWidth, imgHeight);\n+      heightLeft -= pageHeight;\n \n-          // 3. PDF 생성 및 업로드 (UUID를 파일명으로 사용)\n-          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          // PDF 업로드 실패 시 UUID를 추가하지 않은 원래의 응답을 사용\n-          finalReply = reply;\n-        }\n+      // 남은 높이가 있으면 추가 페이지 생성\n+      while (heightLeft >= 0) {\n+        position = -(pageHeight * pageNumber);\n+        pdf.addPage();\n+        pdf.addImage(imageData, 'JPEG', 0, position, imgWidth, imgHeight);\n+        heightLeft -= pageHeight;\n+        pageNumber++;\n       }\n+      \n+      // PDF를 Blob으로 생성\n+      const pdfBlob = pdf.output('blob');\n+      \n+      // Blob URL 생성\n+      const blobUrl = URL.createObjectURL(pdfBlob);\n+      \n+      // 새 창에서 PDF 열기\n+      window.open(blobUrl, '_blank');\n+      \n+      // React root 정리\n+      reactRoot.unmount();\n+      \n+      // 메모리 누수 방지를 위해 일정 시간 후 Blob URL 해제\n+      setTimeout(() => {\n+        URL.revokeObjectURL(blobUrl);\n+      }, 60000); // 1분 후 해제\n+      \n+      // 임시 요소들 제거\n+      document.body.removeChild(tempDiv);\n+      success('PDF가 생성되었습니다.');\n+    } catch (error) {\n+      console.error('PDF 생성 중 오류:', error);\n+    }\n+  };\n \n-      // ⭐️ 수정된 메시지를 서버에 전송하고 스토어 업데이트\n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: finalReply }\n-      });\n \n-      updateLastMessage(finalReply);\n-      setUploadedFiles([]);\n \n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n+  const getCompanyCode = () => {\n+    const pathParts = window.location.pathname.split('/');\n+    const companyCodeIndex = pathParts.indexOf('aiclient') + 1;\n+    return (companyCodeIndex > 0 && pathParts.length > companyCodeIndex)\n+      ? pathParts[companyCodeIndex]\n+      : 'heredot';\n   };\n \n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n-}\n+  const companyCode = getCompanyCode();\n+\n+  // ⭐️ 새로운 공유 URL 생성 로직\n+  const shareUrl = estimate.uuid\n+    ? `${window.location.origin}${getDownloadEstimateUrl(companyCode, estimate.uuid)}`\n+    : '';\n+\n+\n+  \n+  // estimated_period가 '30주'와 같은 문자열일 경우를 가정하고 숫자만 추출합니다.\n+  const weekValue = parseInt(estimate.estimated_period);\n+\n+  // 1개월의 평균 주 수 (30.41일 / 7일)를 사용하여 개월 수를 계산하고 올림 처리합니다.\n+  const weeksPerMonth = 4.345;\n+  const monthValue = Math.ceil(weekValue / weeksPerMonth);\n+\n+  // 최종적으로 보여줄 기간 문자열을 생성합니다.\n+  const displayPeriod = `(약 ${monthValue}개월)`;\n+\n+  const handleCopy = async () => {\n+    try {\n+      await navigator.clipboard.writeText(shareUrl)\n+      success('링크가 복사되었습니다.')\n+    } catch {\n+      // ignore\n+    }\n+  }\n+\n+  return (\n+    <CardWrapper>\n+      <Header>\n+        <Flex>\n+        <Title>{estimate.project_name}</Title>\n+        <Right>\n+        <span style={{ display: 'flex', gap: '8px' }}>\n+        <Icon\n+    onClick={() => setOpenShare(true)}\n+    // ⭐️ isDarkMode 상태에 따라 이미지 경로를 변경\n+    src={isDarkMode ? '/ai-estimate/share2_dark.png' : '/ai-estimate/share2_light.png'}\n+    width={36}\n+    height={36}\n+  />\n+          <Icon \n+            onClick={generatePDF}\n+            src={isDarkMode ? '/ai-estimate/download_dark.png' : '/ai-estimate/download_light.png'} \n+            width={36} \n+            height={36} \n+          />\n+        </span>\n+        </Right>\n+\n+        </Flex>\n+        <Price>\n+          KRW {estimate.total_price}\n+          <span>(부가세 별도)</span>\n+        </Price>\n+        {/* 계산된 displayPeriod를 Period 컴포넌트에 적용 */}\n+        <Period>\n+          <span style={{marginRight: '4px'}}>{estimate.estimated_period}</span>\n+          <span className=\"p\">{displayPeriod}</span>\n+        </Period>\n+        {/* <ActionButtons>\n+          <ActionButton>AI 예산 줄이기</ActionButton>\n+          <Line></Line>\n+          <ActionButton>AI 맞춤 추천</ActionButton>\n+        </ActionButtons> */}\n+      </Header>\n+\n+      <Modal open={openShare} title=\"견적서 공유\" onClose={() => setOpenShare(false)} width={520}>\n+        <div style={{ color: '#A1A1AA', fontSize: 14, marginBottom: 32 }}>공유받은 사용자는 견적 내용을 확인할 수 있습니다.</div>\n+        <ShareInput>\n+          <input readOnly value={shareUrl} placeholder=\"https://aigocorp.com/id...\" />\n+          <button onClick={handleCopy}>링크복사</button>\n+        </ShareInput>\n+      </Modal>\n+    </CardWrapper>\n+  );\n+};\n+\n+export default EstimateCard;\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756451829037,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,327 +1,160 @@\n-\"use client\";\n-\n-import React, { useState } from 'react';\n-import styled from 'styled-components';\n-import { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import Icon from './Icon';\n-import Modal from '@/components/common/Modal';\n+import { useState } from 'react';\n+import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n-import { useThemeStore } from '@/store/themeStore';\n-import html2canvas from 'html2canvas';\n-import jsPDF from 'jspdf';\n-import { PrintableInvoice } from './PrintableInvoice';\n-import { getDownloadEstimateUrl } from '@/lib/api/user/userApi';\n+import { useChatStore } from '@/store/chatStore';\n+import { combinePrompts } from '@/ai/promptTemplates';\n+import { uploadFiles, FileUploadData } from '@/firebase.functions';\n+import type { ModelName } from '@/app/ai-estimate/types';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n-const CardWrapper = styled.div`\n-  background-color: ${({ theme }) => theme.surface1};\n-  color: ${({ theme }) => theme.text};\n-  border-radius: 12px;\n-  overflow: hidden;\n-  margin: 24px 0;\n-  padding: 20px 12px;\n-`;\n+// 견적서 데이터를 추출하는 유틸리티 함수\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!match) return null;\n \n-const Header = styled.div`\n-  padding: 0px;\n+    const jsonStr = match[1];\n+    const data = JSON.parse(jsonStr);\n \n-`;\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n+      console.error('Invalid estimate data structure:', data);\n+      return null;\n+    }\n \n-  const Flex = styled.div`\n-  display: flex;\n-  align-items: flex-start;\n-  justify-content: space-between;\n-  margin: 0 0 18px 0;\n-`;\n-\n-const Title = styled.h2`\n-  font-size: 20px;\n-font-style: normal;\n-font-weight: 700;\n-line-height: normal;\n-`;\n-\n-\n-const Right = styled.div`\n-  display: flex;\n-  align-items: center;\n-  gap: 8px;\n-`;\n-\n-const Price = styled.p`\n-  font-size: 20px;\n-font-style: normal;\n-font-weight: 700;\n-line-height: normal;\n-letter-spacing: 0.4px;\n-  margin: 0 0 8px 0;\n-  \n-  span {\n-    font-size: 0.7em;\n-    font-weight: normal;\n-    color: ${({ theme }) => theme.subtleText};\n-    margin-left: 8px;\n+    return data as ProjectEstimate;\n+  } catch (error) {\n+    console.error('Failed to parse estimate data:', error);\n+    return null;\n   }\n-`;\n+};\n \n-const Period = styled.p`\n-  font-size: 16px;\n-font-style: normal;\n-font-weight: 700;\n-line-height: normal;\n-letter-spacing: 0.32px;\n-  color: ${({ theme }) => theme.text};\n-  margin: 0 0 24px 0;\n-\n-  .p {\n-  font-size: 12px;\n-font-style: normal;\n-font-weight: 400;\n-line-height: normal;\n-letter-spacing: 0.24px;\n-color: ${({ theme }) => theme.subtleText};\n+interface UseChatActionsProps {\n+  modelName: ModelName;\n+  selectedPromptId: string;\n }\n-`;\n \n-const ActionButtons = styled.div`\n-  display: flex;\n-  gap: 10px;\n-  padding 20px 12px;\n-  border-top: 1px solid ${({ theme }) => theme.border};\n-  margin: 4px;\n-`;\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n \n-const Line = styled.div`\n-  border-left: 1px solid ${({ theme }) => theme.border};\n-  `;\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n \n-const ActionButton = styled.button<{ primary?: boolean }>`\n-  flex: 1;\n-  padding: 12px;\n-  border-radius: 8px;\n-  border: none;\n-  background-color: ${({ theme, primary }) => (primary ? theme.accent : 'transparent')};\n-  color: ${({ theme, primary }) => (primary ? (theme.body) : theme.accent)};\n-  font-family: Roboto;\n-  font-size: 14px;\n-  font-style: normal;\n-  font-weight: 600;\n-  line-height: 160%; \n-  letter-spacing: 0.28px;\n-  cursor: pointer;\n-  transition: all 0.2s ease;\n+  // 파일 업로드 관련 함수들... (생략)\n+  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n+  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n+  const removeFile = (fileName: string) => { /* ... */ };\n+  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n+  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n+  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n \n-  &:hover {\n-    opacity: 0.8;\n-  }\n-`;\n+  const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n-const ShareInput = styled.div`\n-  display: flex;\n-  gap: 8px;\n-  margin-top: 8px;\n+    setIsProcessing(true);\n \n-  input {\n-    flex: 1;\n-    height: 44px;\n-    border-radius: 8px;\n-    border: 1px solid #e5e7eb;\n-    background: #f9fafb;\n-    color: #111827;\n-    padding: 0 12px;\n-  }\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        \n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+        } else {\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n \n-  button {\n-    height: 44px;\n-    padding: 0 14px;\n-    border-radius: 8px;\n-    background: #2E2E48;\n-    color: white;\n-    font-size: 14px;\n-font-style: normal;\n-font-weight: 400;\n-line-height: 160%;\n-letter-spacing: 0.32px;\n-  }\n-`;\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n+      \n+      const filesForGemini = uploadedFiles.map((file) => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+      }));\n \n-interface EstimateCardProps {\n-  // ⭐️ ProjectEstimate 타입에 uuid 속성을 추가했다고 가정\n-  estimate: ProjectEstimate & { uuid?: string };\n-}\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForGemini);\n \n-const EstimateCard: React.FC<EstimateCardProps> = ({ estimate }) => {\n-  const [openShare, setOpenShare] = useState(false)\n-  const { success } = useToast()\n-  const { isDarkMode } = useThemeStore()\n+      let finalReply = reply;\n \n-  const generatePDF = async () => {\n-    try {\n-      // 임시 컨테이너 생성\n-      const tempDiv = document.createElement('div');\n-      tempDiv.style.position = 'absolute';\n-      tempDiv.style.left = '-9999px';\n-      document.body.appendChild(tempDiv);\n+      const estimateData = extractEstimateData(reply);\n+      if (estimateData) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          const pdfUuid = crypto.randomUUID();\n+          \n+          finalReply = reply.replace(\n+            '<script type=\"application/json\" id=\"invoiceData\">',\n+            `<script type=\"application/json\" id=\"invoiceData\" data-uuid=\"${pdfUuid}\">`\n+          );\n \n-      // PrintableInvoice 렌더링\n-      const root = document.createElement('div');\n-      root.style.width = '780px';\n-      root.style.backgroundColor = 'white';\n-      tempDiv.appendChild(root);\n+          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          finalReply = reply;\n+        }\n+      }\n \n-      // React 컴포넌트를 DOM에 렌더링\n-      const { createRoot } = await import('react-dom/client');\n-      const reactRoot = createRoot(root);\n-      reactRoot.render(<PrintableInvoice estimate={estimate} />);\n-\n-      // 렌더링이 완료될 때까지 잠시 대기\n-      await new Promise(resolve => setTimeout(resolve, 100));\n-\n-      // HTML을 캔버스로 변환\n-      const canvas = await html2canvas(root, {\n-        scale: 1.5,\n-        useCORS: true,\n-        logging: false,\n-        imageTimeout: 0,\n-        backgroundColor: null\n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: finalReply }\n       });\n \n-      // PDF 생성\n-      const imgWidth = 210; // A4 가로 크기 (mm)\n-      const pageHeight = 297; // A4 세로 크기 (mm)\n-      const imgHeight = canvas.height * imgWidth / canvas.width;\n-      const pdf = new jsPDF('p', 'mm');\n+      updateLastMessage(finalReply);\n+      setUploadedFiles([]);\n \n-      // 여러 페이지로 나누기\n-      let heightLeft = imgHeight;\n-      let position = 0;\n-      let pageNumber = 1;\n-\n-      // 첫 페이지 추가 (JPEG 압축 사용)\n-      const imageData = canvas.toDataURL('image/jpeg', 0.7);\n-      pdf.addImage(imageData, 'JPEG', 0, position, imgWidth, imgHeight);\n-      heightLeft -= pageHeight;\n-\n-      // 남은 높이가 있으면 추가 페이지 생성\n-      while (heightLeft >= 0) {\n-        position = -(pageHeight * pageNumber);\n-        pdf.addPage();\n-        pdf.addImage(imageData, 'JPEG', 0, position, imgWidth, imgHeight);\n-        heightLeft -= pageHeight;\n-        pageNumber++;\n-      }\n\\ No newline at end of file\n-      \n-      // PDF를 Blob으로 생성\n-      const pdfBlob = pdf.output('blob');\n-      \n-      // Blob URL 생성\n-      const blobUrl = URL.createObjectURL(pdfBlob);\n-      \n-      // 새 창에서 PDF 열기\n-      window.open(blobUrl, '_blank');\n-      \n-      // React root 정리\n-      reactRoot.unmount();\n-      \n-      // 메모리 누수 방지를 위해 일정 시간 후 Blob URL 해제\n-      setTimeout(() => {\n-        URL.revokeObjectURL(blobUrl);\n-      }, 60000); // 1분 후 해제\n-      \n-      // 임시 요소들 제거\n-      document.body.removeChild(tempDiv);\n-      success('PDF가 생성되었습니다.');\n-    } catch (error) {\n-      console.error('PDF 생성 중 오류:', error);\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n     }\n   };\n \n-\n-\n-  const getCompanyCode = () => {\n-    const pathParts = window.location.pathname.split('/');\n-    const companyCodeIndex = pathParts.indexOf('aiclient') + 1;\n-    return (companyCodeIndex > 0 && pathParts.length > companyCodeIndex)\n-      ? pathParts[companyCodeIndex]\n-      : 'heredot';\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n   };\n-\n-  const companyCode = getCompanyCode();\n-\n-  // ⭐️ 새로운 공유 URL 생성 로직\n-  const shareUrl = estimate.uuid\n-    ? `${window.location.origin}${getDownloadEstimateUrl(companyCode, estimate.uuid)}`\n-    : '';\n-\n-\n-  \n-  // estimated_period가 '30주'와 같은 문자열일 경우를 가정하고 숫자만 추출합니다.\n-  const weekValue = parseInt(estimate.estimated_period);\n-\n-  // 1개월의 평균 주 수 (30.41일 / 7일)를 사용하여 개월 수를 계산하고 올림 처리합니다.\n-  const weeksPerMonth = 4.345;\n-  const monthValue = Math.ceil(weekValue / weeksPerMonth);\n-\n-  // 최종적으로 보여줄 기간 문자열을 생성합니다.\n-  const displayPeriod = `(약 ${monthValue}개월)`;\n-\n-  const handleCopy = async () => {\n-    try {\n-      await navigator.clipboard.writeText(shareUrl)\n-      success('링크가 복사되었습니다.')\n-    } catch {\n-      // ignore\n-    }\n-  }\n-\n-  return (\n-    <CardWrapper>\n-      <Header>\n-        <Flex>\n-        <Title>{estimate.project_name}</Title>\n-        <Right>\n-        <span style={{ display: 'flex', gap: '8px' }}>\n-        <Icon\n-    onClick={() => setOpenShare(true)}\n-    // ⭐️ isDarkMode 상태에 따라 이미지 경로를 변경\n-    src={isDarkMode ? '/ai-estimate/share2_dark.png' : '/ai-estimate/share2_light.png'}\n-    width={36}\n-    height={36}\n-  />\n-          <Icon \n-            onClick={generatePDF}\n-            src={isDarkMode ? '/ai-estimate/download_dark.png' : '/ai-estimate/download_light.png'} \n-            width={36} \n-            height={36} \n-          />\n-        </span>\n-        </Right>\n-\n-        </Flex>\n-        <Price>\n-          KRW {estimate.total_price}\n-          <span>(부가세 별도)</span>\n-        </Price>\n-        {/* 계산된 displayPeriod를 Period 컴포넌트에 적용 */}\n-        <Period>\n-          <span style={{marginRight: '4px'}}>{estimate.estimated_period}</span>\n-          <span className=\"p\">{displayPeriod}</span>\n-        </Period>\n-        {/* <ActionButtons>\n-          <ActionButton>AI 예산 줄이기</ActionButton>\n-          <Line></Line>\n-          <ActionButton>AI 맞춤 추천</ActionButton>\n-        </ActionButtons> */}\n-      </Header>\n-\n-      <Modal open={openShare} title=\"견적서 공유\" onClose={() => setOpenShare(false)} width={520}>\n-        <div style={{ color: '#A1A1AA', fontSize: 14, marginBottom: 32 }}>공유받은 사용자는 견적 내용을 확인할 수 있습니다.</div>\n-        <ShareInput>\n-          <input readOnly value={shareUrl} placeholder=\"https://aigocorp.com/id...\" />\n-          <button onClick={handleCopy}>링크복사</button>\n-        </ShareInput>\n-      </Modal>\n-    </CardWrapper>\n-  );\n-};\n-\n-export default EstimateCard;\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756451870094,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -108,28 +108,34 @@\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n       let finalReply = reply;\n \n+      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n+          // 1. UUID 생성\n           const pdfUuid = crypto.randomUUID();\n           \n+          // 2. 응답 메시지에 UUID를 포함시키는 로직 추가\n           finalReply = reply.replace(\n             '<script type=\"application/json\" id=\"invoiceData\">',\n             `<script type=\"application/json\" id=\"invoiceData\" data-uuid=\"${pdfUuid}\">`\n           );\n \n+          // 3. PDF 생성 및 업로드 (UUID를 파일명으로 사용)\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          // PDF 업로드 실패 시 UUID를 추가하지 않은 원래의 응답을 사용\n           finalReply = reply;\n         }\n       }\n \n+      // ⭐️ 수정된 메시지를 서버에 전송하고 스토어 업데이트\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n         content: { type: 'text', value: finalReply }\n       });\n"
                },
                {
                    "date": 1756451967397,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,4 +1,6 @@\n+// src/hooks/useChatActions.ts\n+\n import { useState } from 'react';\n import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n@@ -106,42 +108,28 @@\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n-      let finalReply = reply;\n-\n       // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n-          // 1. UUID 생성\n-          const pdfUuid = crypto.randomUUID();\n-          \n-          // 2. 응답 메시지에 UUID를 포함시키는 로직 추가\n-          finalReply = reply.replace(\n-            '<script type=\"application/json\" id=\"invoiceData\">',\n-            `<script type=\"application/json\" id=\"invoiceData\" data-uuid=\"${pdfUuid}\">`\n-          );\n-\n-          // 3. PDF 생성 및 업로드 (UUID를 파일명으로 사용)\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          // PDF 업로드 실패 시 UUID를 추가하지 않은 원래의 응답을 사용\n-          finalReply = reply;\n         }\n       }\n \n-      // ⭐️ 수정된 메시지를 서버에 전송하고 스토어 업데이트\n       await sendChatMessage(currentSessionId, {\n         role: 'AI',\n-        content: { type: 'text', value: finalReply }\n+        content: { type: 'text', value: reply }\n       });\n \n-      updateLastMessage(finalReply);\n+      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756614839799,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n+const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -23,16 +23,21 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n+    \n+    // UUID 추출 로직 추가\n+    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n+    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    return data as ProjectEstimate;\n+    // 추출한 UUID를 데이터 객체에 추가하여 반환\n+    return { ...data, uuid } as ExtractedEstimateData;\n+\n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n-\n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n }\n"
                },
                {
                    "date": 1756614865056,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -10,8 +10,12 @@\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n+export interface ExtractedEstimateData extends ProjectEstimate {\n+  uuid?: string; // uuid 속성 추가\n+}\n+\n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n@@ -36,8 +40,10 @@\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n+\n+\n interface UseChatActionsProps {\n   modelName: ModelName;\n   selectedPromptId: string;\n }\n"
                },
                {
                    "date": 1756614883885,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -10,11 +10,8 @@\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n \n-export interface ExtractedEstimateData extends ProjectEstimate {\n-  uuid?: string; // uuid 속성 추가\n-}\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n   try {\n"
                },
                {
                    "date": 1756615525606,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,9 +8,9 @@\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n \n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n"
                },
                {
                    "date": 1756615654979,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,16 +5,16 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n-\n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -24,25 +24,19 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n-    \n-    // UUID 추출 로직 추가\n-    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n-    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    // 추출한 UUID를 데이터 객체에 추가하여 반환\n-    return { ...data, uuid } as ExtractedEstimateData;\n+    return data as ProjectEstimate;\n \n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n \n-\n interface UseChatActionsProps {\n-  modelName: ModelName;\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -59,22 +53,70 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n+  const [estimateFilePath, setEstimateFilePath] = useState<string | null>(null); // ⭐️ filePath 상태 추가\n \n-  // 파일 업로드 관련 함수들... (생략)\n-  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n-  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n-  const removeFile = (fileName: string) => { /* ... */ };\n-  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n-  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n-  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n \n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n+    \n+    // 이전에 생성된 filePath 상태 초기화\n+    setEstimateFilePath(null);\n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n@@ -122,22 +164,57 @@\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath를 상태에 저장\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const newFilePath = uploadResponse.data as string;\n+              setEstimateFilePath(newFilePath);\n+              \n+              // ⭐️ AI 응답 메시지에 filePath 정보를 추가\n+              // 이 부분은 AI 모델이 filePath를 직접 반환하지 않으므로, 클라이언트에서 인위적으로 추가합니다.\n+              // 이 정보는 EstimateCard 컴포넌트가 공유 URL을 생성하는 데 사용됩니다.\n+              const updatedEstimateData = {\n+                  ...estimateData,\n+                  filePath: newFilePath,\n+              };\n+              const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+              await sendChatMessage(currentSessionId, {\n+                  role: 'AI',\n+                  content: { type: 'text', value: updatedReply }\n+              });\n+              updateLastMessage(updatedReply);\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n         }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n       }\n \n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: reply }\n-      });\n-\n-      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n@@ -157,6 +234,7 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n+    estimateFilePath, // ⭐️ filePath 상태를 반환에 추가\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756615933572,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,16 +5,16 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n \n+\n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n+const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -24,19 +24,25 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n+    \n+    // UUID 추출 로직 추가\n+    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n+    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    return data as ProjectEstimate;\n+    // 추출한 UUID를 데이터 객체에 추가하여 반환\n+    return { ...data, uuid } as ExtractedEstimateData;\n \n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n \n+\n interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  modelName: ModelName;\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -53,70 +59,22 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n-  const [estimateFilePath, setEstimateFilePath] = useState<string | null>(null); // ⭐️ filePath 상태 추가\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n+  // 파일 업로드 관련 함수들... (생략)\n+  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n+  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n+  const removeFile = (fileName: string) => { /* ... */ };\n+  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n+  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n+  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n \n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n-    \n-    // 이전에 생성된 filePath 상태 초기화\n-    setEstimateFilePath(null);\n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n@@ -164,57 +122,22 @@\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n-          \n-          // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n-\n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath를 상태에 저장\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const newFilePath = uploadResponse.data as string;\n-              setEstimateFilePath(newFilePath);\n-              \n-              // ⭐️ AI 응답 메시지에 filePath 정보를 추가\n-              // 이 부분은 AI 모델이 filePath를 직접 반환하지 않으므로, 클라이언트에서 인위적으로 추가합니다.\n-              // 이 정보는 EstimateCard 컴포넌트가 공유 URL을 생성하는 데 사용됩니다.\n-              const updatedEstimateData = {\n-                  ...estimateData,\n-                  filePath: newFilePath,\n-              };\n-              const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-              await sendChatMessage(currentSessionId, {\n-                  role: 'AI',\n-                  content: { type: 'text', value: updatedReply }\n-              });\n-              updateLastMessage(updatedReply);\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n+          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          \n-          // PDF 업로드 실패 시 원래 응답 사용\n-          await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-          });\n-          updateLastMessage(reply);\n         }\n-      } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n-        await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-        });\n-        updateLastMessage(reply);\n       }\n \n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: reply }\n+      });\n+\n+      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n@@ -234,7 +157,6 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n-    estimateFilePath, // ⭐️ filePath 상태를 반환에 추가\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756616071242,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,16 +5,16 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n-\n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -24,25 +24,19 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n-    \n-    // UUID 추출 로직 추가\n-    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n-    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    // 추출한 UUID를 데이터 객체에 추가하여 반환\n-    return { ...data, uuid } as ExtractedEstimateData;\n+    return data as ProjectEstimate;\n \n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n \n-\n interface UseChatActionsProps {\n-  modelName: ModelName;\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -60,17 +54,61 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수들... (생략)\n-  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n-  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n-  const removeFile = (fileName: string) => { /* ... */ };\n-  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n-  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n-  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n \n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -122,22 +160,63 @@\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const filePath = uploadResponse.data as string;\n+              \n+              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n+              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n+              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n+              \n+              if (extractedUuid) {\n+                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n+                  // 견적서 JSON 데이터에 uuid 필드를 추가\n+                  const updatedEstimateData = {\n+                      ...estimateData,\n+                      uuid: extractedUuid, // UUID를 견적서 데이터에 포함\n+                  };\n+                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+                  await sendChatMessage(currentSessionId, {\n+                      role: 'AI',\n+                      content: { type: 'text', value: updatedReply }\n+                  });\n+                  updateLastMessage(updatedReply);\n+              } else {\n+                  throw new Error('UUID를 추출할 수 없습니다.');\n+              }\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n         }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n       }\n \n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: reply }\n-      });\n-\n-      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756622040926,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,16 +5,16 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import type { ModelName } from '@/app/ai-estimate/types';\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n \n+\n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n+const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -24,19 +24,25 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n+    \n+    // UUID 추출 로직 추가\n+    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n+    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    return data as ProjectEstimate;\n+    // 추출한 UUID를 데이터 객체에 추가하여 반환\n+    return { ...data, uuid } as ExtractedEstimateData;\n \n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n \n+\n interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  modelName: ModelName;\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -54,61 +60,17 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n+  // 파일 업로드 관련 함수들... (생략)\n+  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n+  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n+  const removeFile = (fileName: string) => { /* ... */ };\n+  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n+  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n+  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n \n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -160,63 +122,22 @@\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n-          \n-          // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n-\n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const filePath = uploadResponse.data as string;\n-              \n-              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n-              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n-              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n-              \n-              if (extractedUuid) {\n-                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n-                  // 견적서 JSON 데이터에 uuid 필드를 추가\n-                  const updatedEstimateData = {\n-                      ...estimateData,\n-                      uuid: extractedUuid, // UUID를 견적서 데이터에 포함\n-                  };\n-                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-                  await sendChatMessage(currentSessionId, {\n-                      role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n-                  });\n-                  updateLastMessage(updatedReply);\n-              } else {\n-                  throw new Error('UUID를 추출할 수 없습니다.');\n-              }\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n+          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n+          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          \n-          // PDF 업로드 실패 시 원래 응답 사용\n-          await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-          });\n-          updateLastMessage(reply);\n         }\n-      } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n-        await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-        });\n-        updateLastMessage(reply);\n       }\n \n+      await sendChatMessage(currentSessionId, {\n+        role: 'AI',\n+        content: { type: 'text', value: reply }\n+      });\n+\n+      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756622094990,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -5,16 +5,16 @@\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { ModelName } from '@/app/ai-estimate/types';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ExtractedEstimateData } from '@/app/ai-estimate/types/projectEstimate';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n-\n // 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ExtractedEstimateData | null => {\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n     const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n     if (!match) return null;\n \n@@ -24,25 +24,19 @@\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n       console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n-    \n-    // UUID 추출 로직 추가\n-    const uuidMatch = content.match(/<div id=\"invoice-uuid\">([a-f0-9-]+)<\\/div>/);\n-    const uuid = uuidMatch ? uuidMatch[1] : undefined;\n \n-    // 추출한 UUID를 데이터 객체에 추가하여 반환\n-    return { ...data, uuid } as ExtractedEstimateData;\n+    return data as ProjectEstimate;\n \n   } catch (error) {\n     console.error('Failed to parse estimate data:', error);\n     return null;\n   }\n };\n \n-\n interface UseChatActionsProps {\n-  modelName: ModelName;\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -60,17 +54,61 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수들... (생략)\n-  const handleFileUpload = (fileData: FileUploadData) => { /* ... */ };\n-  const handleFileUploadProgress = (progress: number) => { /* ... */ };\n-  const removeFile = (fileName: string) => { /* ... */ };\n-  const handleDragOver = (e: React.DragEvent) => { /* ... */ };\n-  const handleDragLeave = (e: React.DragEvent) => { /* ... */ };\n-  const handleDrop = (e: React.DragEvent) => { /* ... */ };\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => { /* ... */ };\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n \n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -122,22 +160,63 @@\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          // 추출한 estimateData를 generateAndUploadPdf 함수에 전달\n-          await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, success, error);\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const filePath = uploadResponse.data as string;\n+              \n+              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n+              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n+              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n+              \n+              if (extractedUuid) {\n+                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n+                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n+                  const updatedEstimateData = {\n+                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n+                      uuid: extractedUuid, // UUID만 추가\n+                  };\n+                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+                  await sendChatMessage(currentSessionId, {\n+                      role: 'AI',\n+                      content: { type: 'text', value: updatedReply }\n+                  });\n+                  updateLastMessage(updatedReply);\n+              } else {\n+                  throw new Error('UUID를 추출할 수 없습니다.');\n+              }\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n         }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n       }\n \n-      await sendChatMessage(currentSessionId, {\n-        role: 'AI',\n-        content: { type: 'text', value: reply }\n-      });\n-\n-      updateLastMessage(reply);\n       setUploadedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756680553535,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,8 +107,19 @@\n       });\n     }\n   };\n \n+  // ⭐️ 추가: onFilesChange 함수\n+  const onFilesChange = (files: File[]) => {\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -236,6 +247,7 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n+    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756680919944,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -107,19 +107,8 @@\n       });\n     }\n   };\n \n-  // ⭐️ 추가: onFilesChange 함수\n-  const onFilesChange = (files: File[]) => {\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -247,7 +236,6 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n-    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756681017162,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, uploadFilesToServer, getFileUrl } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -54,14 +54,38 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n+  // ⭐️ 수정: 서버 API를 사용하는 파일 업로드 함수\n+  const handleFileUpload = async (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    setIsUploading(true);\n     setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+    \n+    try {\n+      const response = await uploadFilesToServer(files);\n+      \n+      if (response && response.statusCode === 200 && response.data) {\n+        // 서버에서 반환된 파일명들을 FileUploadData 형태로 변환\n+        const fileDataArray: FileUploadData[] = response.data.map((fileName, index) => ({\n+          name: files[index].name,\n+          fileUri: getFileUrl(fileName), // 서버 URL로 변경\n+          mimeType: files[index].type,\n+          size: files[index].size,\n+        }));\n+        \n+        setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+        success(`파일 ${files.length}개가 업로드되었습니다.`);\n+      } else {\n+        throw new Error(response?.error?.message || '파일 업로드에 실패했습니다.');\n+      }\n+    } catch (error) {\n+      error(`파일 업로드 실패: ${(error as Error).message}`);\n+    } finally {\n+      setIsUploading(false);\n+      setUploadProgress(0);\n+    }\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -86,29 +110,26 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.dataTransfer.files);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n     const files = Array.from(e.target.files || []);\n     if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n+      handleFileUpload(files);\n     }\n   };\n \n+  // ⭐️ 추가: onFilesChange 함수 (BottomInput에서 사용)\n+  const onFilesChange = (files: File[]) => {\n+    handleFileUpload(files);\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -236,6 +257,7 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n+    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756688288118,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, uploadFilesToServer, getFileUrl } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -54,38 +54,14 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // ⭐️ 수정: 서버 API를 사용하는 파일 업로드 함수\n-  const handleFileUpload = async (files: File[]) => {\n-    if (files.length === 0) return;\n-    \n-    setIsUploading(true);\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n     setUploadProgress(0);\n-    \n-    try {\n-      const response = await uploadFilesToServer(files);\n-      \n-      if (response && response.statusCode === 200 && response.data) {\n-        // 서버에서 반환된 파일명들을 FileUploadData 형태로 변환\n-        const fileDataArray: FileUploadData[] = response.data.map((fileName, index) => ({\n-          name: files[index].name,\n-          fileUri: getFileUrl(fileName), // 서버 URL로 변경\n-          mimeType: files[index].type,\n-          size: files[index].size,\n-        }));\n-        \n-        setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-        success(`파일 ${files.length}개가 업로드되었습니다.`);\n-      } else {\n-        throw new Error(response?.error?.message || '파일 업로드에 실패했습니다.');\n-      }\n-    } catch (error) {\n-      error(`파일 업로드 실패: ${(error as Error).message}`);\n-    } finally {\n-      setIsUploading(false);\n-      setUploadProgress(0);\n-    }\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -110,26 +86,29 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-          const files = Array.from(e.dataTransfer.files);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n   };\n \n-    const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n     const files = Array.from(e.target.files || []);\n     if (files.length > 0) {\n-      handleFileUpload(files);\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n     }\n   };\n \n-  // ⭐️ 추가: onFilesChange 함수 (BottomInput에서 사용)\n-  const onFilesChange = (files: File[]) => {\n-    handleFileUpload(files);\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -257,7 +236,6 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n-    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756688384044,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, uploadFilesToServer, getFileUrl, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -54,14 +54,51 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n+  // ⭐️ 수정: 서버 API를 사용하는 파일 업로드 함수\n+  const handleFileUpload = async (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    // ⭐️ 파일 검증\n+    const invalidFiles = files.filter(file => !validateFileType(file));\n+    if (invalidFiles.length > 0) {\n+      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    const oversizedFiles = files.filter(file => !validateFileSize(file));\n+    if (oversizedFiles.length > 0) {\n+      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    setIsUploading(true);\n     setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+    \n+    try {\n+      const response = await uploadFilesToServer(files);\n+      \n+      if (response && response.statusCode === 200 && response.data) {\n+        // 서버에서 반환된 파일명들을 FileUploadData 형태로 변환\n+        const fileDataArray: FileUploadData[] = response.data.map((fileName, index) => ({\n+          name: files[index].name,\n+          fileUri: getFileUrl(fileName), // 서버 URL로 변경\n+          mimeType: files[index].type,\n+          size: files[index].size,\n+        }));\n+        \n+        setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+        success(`파일 ${files.length}개가 업로드되었습니다.`);\n+      } else {\n+        throw new Error(response?.error?.message || '파일 업로드에 실패했습니다.');\n+      }\n+    } catch (error) {\n+      error(`파일 업로드 실패: ${(error as Error).message}`);\n+    } finally {\n+      setIsUploading(false);\n+      setUploadProgress(0);\n+    }\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -86,29 +123,26 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.dataTransfer.files);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n     const files = Array.from(e.target.files || []);\n     if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n+      handleFileUpload(files);\n     }\n   };\n \n+  // ⭐️ 추가: onFilesChange 함수 (BottomInput에서 사용)\n+  const onFilesChange = (files: File[]) => {\n+    handleFileUpload(files);\n+  };\n+\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -236,6 +270,7 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n+    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756689500082,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,241 @@\n+// src/hooks/useChatActions.ts\n+\n+import { useState } from 'react';\n+import useAI from './useAI';\n+import { useToast } from '@/components/common/ToastProvider';\n+import { useChatStore } from '@/store/chatStore';\n+import { combinePrompts } from '@/ai/promptTemplates';\n+import { uploadFiles, FileUploadData } from '@/firebase.functions';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+\n+// 견적서 데이터를 추출하는 유틸리티 함수\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!match) return null;\n+\n+    const jsonStr = match[1];\n+    const data = JSON.parse(jsonStr);\n+\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n+      console.error('Invalid estimate data structure:', data);\n+      return null;\n+    }\n+\n+    return data as ProjectEstimate;\n+\n+  } catch (error) {\n+    console.error('Failed to parse estimate data:', error);\n+    return null;\n+  }\n+};\n+\n+interface UseChatActionsProps {\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  selectedPromptId: string;\n+}\n+\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n+\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n+\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n+\n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+\n+    setIsProcessing(true);\n+\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        \n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+        } else {\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n+\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n+      \n+      const filesForGemini = uploadedFiles.map((file) => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+      }));\n+\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForGemini);\n+\n+      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n+      const estimateData = extractEstimateData(reply);\n+      if (estimateData) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const filePath = uploadResponse.data as string;\n+              \n+              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n+              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n+              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n+              \n+              if (extractedUuid) {\n+                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n+                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n+                  const updatedEstimateData = {\n+                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n+                      uuid: extractedUuid, // UUID만 추가\n+                  };\n+                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+                  await sendChatMessage(currentSessionId, {\n+                      role: 'AI',\n+                      content: { type: 'text', value: updatedReply }\n+                  });\n+                  updateLastMessage(updatedReply);\n+              } else {\n+                  throw new Error('UUID를 추출할 수 없습니다.');\n+              }\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n+        }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n+      }\n+\n+      setUploadedFiles([]);\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n+    }\n+  };\n+\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n+  };\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756689572470,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,249 +6,8 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-\n-// 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n-  try {\n-    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n-    if (!match) return null;\n-\n-    const jsonStr = match[1];\n-    const data = JSON.parse(jsonStr);\n-\n-    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n-      console.error('Invalid estimate data structure:', data);\n-      return null;\n-    }\n-\n-    return data as ProjectEstimate;\n-\n-  } catch (error) {\n-    console.error('Failed to parse estimate data:', error);\n-    return null;\n-  }\n-};\n-\n-interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n-  selectedPromptId: string;\n-}\n-\n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n-\n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n-\n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n-  };\n-\n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n-  };\n-\n-  const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n-\n-    setIsProcessing(true);\n-\n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n-    }\n-    \n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-        } else {\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n-\n-    try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n-      });\n-      \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n-\n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n-\n-      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n-      const estimateData = extractEstimateData(reply);\n-      if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n-          \n-          // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n-\n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const filePath = uploadResponse.data as string;\n-              \n-              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n-              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n-              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n-              \n-              if (extractedUuid) {\n-                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n-                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n-                  const updatedEstimateData = {\n-                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n-                      uuid: extractedUuid, // UUID만 추가\n-                  };\n-                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-                  await sendChatMessage(currentSessionId, {\n-                      role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n-                  });\n-                  updateLastMessage(updatedReply);\n-              } else {\n-                  throw new Error('UUID를 추출할 수 없습니다.');\n-              }\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          \n-          // PDF 업로드 실패 시 원래 응답 사용\n-          await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-          });\n-          updateLastMessage(reply);\n-        }\n-      } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n-        await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-        });\n-        updateLastMessage(reply);\n-      }\n-\n-      setUploadedFiles([]);\n-\n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n-  };\n-\n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n-}\n-// src/hooks/useChatActions.ts\n-\n-import { useState } from 'react';\n-import useAI from './useAI';\n-import { useToast } from '@/components/common/ToastProvider';\n-import { useChatStore } from '@/store/chatStore';\n-import { combinePrompts } from '@/ai/promptTemplates';\n-import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n import { createChatSession, sendChatMessage, uploadFilesToServer, getFileUrl, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n"
                },
                {
                    "date": 1756691250703,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, uploadFilesToServer, getFileUrl, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -54,51 +54,14 @@\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n \n-  // ⭐️ 수정: 서버 API를 사용하는 파일 업로드 함수\n-  const handleFileUpload = async (files: File[]) => {\n-    if (files.length === 0) return;\n-    \n-    // ⭐️ 파일 검증\n-    const invalidFiles = files.filter(file => !validateFileType(file));\n-    if (invalidFiles.length > 0) {\n-      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    const oversizedFiles = files.filter(file => !validateFileSize(file));\n-    if (oversizedFiles.length > 0) {\n-      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    setIsUploading(true);\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n     setUploadProgress(0);\n-    \n-    try {\n-      const response = await uploadFilesToServer(files);\n-      \n-      if (response && response.statusCode === 200 && response.data) {\n-        // 서버에서 반환된 파일명들을 FileUploadData 형태로 변환\n-        const fileDataArray: FileUploadData[] = response.data.map((fileName, index) => ({\n-          name: files[index].name,\n-          fileUri: getFileUrl(fileName), // 서버 URL로 변경\n-          mimeType: files[index].type,\n-          size: files[index].size,\n-        }));\n-        \n-        setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-        success(`파일 ${files.length}개가 업로드되었습니다.`);\n-      } else {\n-        throw new Error(response?.error?.message || '파일 업로드에 실패했습니다.');\n-      }\n-    } catch (error) {\n-      error(`파일 업로드 실패: ${(error as Error).message}`);\n-    } finally {\n-      setIsUploading(false);\n-      setUploadProgress(0);\n-    }\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -123,26 +86,29 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-          const files = Array.from(e.dataTransfer.files);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n   };\n \n-    const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n     const files = Array.from(e.target.files || []);\n     if (files.length > 0) {\n-      handleFileUpload(files);\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n     }\n   };\n \n-  // ⭐️ 추가: onFilesChange 함수 (BottomInput에서 사용)\n-  const onFilesChange = (files: File[]) => {\n-    handleFileUpload(files);\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n@@ -270,7 +236,6 @@\n     handleDrop,\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n-    onFilesChange, // ⭐️ 추가: onFilesChange 함수\n   };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756691368827,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n"
                },
                {
                    "date": 1756691623200,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n"
                },
                {
                    "date": 1756691720764,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -53,15 +53,39 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n+  const handleFileUpload = (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    // ⭐️ 파일 검증\n+    const invalidFiles = files.filter(file => !validateFileType(file));\n+    if (invalidFiles.length > 0) {\n+      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    const oversizedFiles = files.filter(file => !validateFileSize(file));\n+    if (oversizedFiles.length > 0) {\n+      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n+    const fileDataArray: FileUploadData[] = files.map(file => ({\n+      name: file.name,\n+      fileUri: URL.createObjectURL(file), // 임시 URL\n+      mimeType: file.type,\n+      size: file.size,\n+    }));\n+    \n+    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+    // ⭐️ 실제 File 객체들도 저장\n+    setSelectedFiles(prev => [...prev, ...files]);\n+    success(`파일 ${files.length}개가 추가되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -139,13 +163,20 @@\n         return;\n       }\n     }\n \n-    try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n-      });\n+          try {\n+        // ⭐️ 수정: 파일이 있을 경우 sendMessageWithFiles 사용, 없으면 기존 방식 사용\n+        if (selectedFiles.length > 0) {\n+          // 파일과 메시지를 함께 전송\n+          await sendMessageWithFiles(currentSessionId, input, selectedFiles);\n+        } else {\n+          // 파일이 없으면 기존 방식 사용\n+          await sendChatMessage(currentSessionId, { \n+            role: 'USER', \n+            content: { type: 'text', value: input }\n+          });\n+        }\n       \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n         fileUri: file.fileUri,\n@@ -216,8 +247,9 @@\n         updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n+      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n"
                },
                {
                    "date": 1756692224728,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -53,39 +53,15 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n-  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n \n-  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n-  const handleFileUpload = (files: File[]) => {\n-    if (files.length === 0) return;\n-    \n-    // ⭐️ 파일 검증\n-    const invalidFiles = files.filter(file => !validateFileType(file));\n-    if (invalidFiles.length > 0) {\n-      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    const oversizedFiles = files.filter(file => !validateFileSize(file));\n-    if (oversizedFiles.length > 0) {\n-      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n-    const fileDataArray: FileUploadData[] = files.map(file => ({\n-      name: file.name,\n-      fileUri: URL.createObjectURL(file), // 임시 URL\n-      mimeType: file.type,\n-      size: file.size,\n-    }));\n-    \n-    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-    // ⭐️ 실제 File 객체들도 저장\n-    setSelectedFiles(prev => [...prev, ...files]);\n-    success(`파일 ${files.length}개가 추가되었습니다.`);\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -163,20 +139,13 @@\n         return;\n       }\n     }\n \n-          try {\n-        // ⭐️ 수정: 파일이 있을 경우 sendMessageWithFiles 사용, 없으면 기존 방식 사용\n-        if (selectedFiles.length > 0) {\n-          // 파일과 메시지를 함께 전송\n-          await sendMessageWithFiles(currentSessionId, input, selectedFiles);\n-        } else {\n-          // 파일이 없으면 기존 방식 사용\n-          await sendChatMessage(currentSessionId, { \n-            role: 'USER', \n-            content: { type: 'text', value: input }\n-          });\n-        }\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n       \n       const filesForGemini = uploadedFiles.map((file) => ({\n         name: file.name,\n         fileUri: file.fileUri,\n@@ -247,9 +216,8 @@\n         updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n-      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n"
                },
                {
                    "date": 1756692412372,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,9 +6,9 @@\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { uploadFiles, FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -53,15 +53,39 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n+  const handleFileUpload = (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    // ⭐️ 파일 검증\n+    const invalidFiles = files.filter(file => !validateFileType(file));\n+    if (invalidFiles.length > 0) {\n+      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    const oversizedFiles = files.filter(file => !validateFileSize(file));\n+    if (oversizedFiles.length > 0) {\n+      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n+    const fileDataArray: FileUploadData[] = files.map(file => ({\n+      name: file.name,\n+      fileUri: URL.createObjectURL(file), // 임시 URL\n+      mimeType: file.type,\n+      size: file.size,\n+    }));\n+    \n+    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+    // ⭐️ 실제 File 객체들도 저장\n+    setSelectedFiles(prev => [...prev, ...files]);\n+    success(`파일 ${files.length}개가 추가되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -86,27 +110,19 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.dataTransfer.files);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n   const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.target.files || []);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n@@ -139,19 +155,37 @@\n         return;\n       }\n     }\n \n-    try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n-      });\n+          try {\n+        // ⭐️ 수정: 파일이 있을 경우 sendMessageWithFiles 사용, 없으면 기존 방식 사용\n+        let serverFileUrls: string[] = [];\n+        if (selectedFiles.length > 0) {\n+          // 파일과 메시지를 함께 전송\n+          const response = await sendMessageWithFiles(currentSessionId, input, selectedFiles);\n+          if (response && response.statusCode === 200 && response.data) {\n+            serverFileUrls = response.data.fileUrls || [];\n+          }\n+        } else {\n+          // 파일이 없으면 기존 방식 사용\n+          await sendChatMessage(currentSessionId, { \n+            role: 'USER', \n+            content: { type: 'text', value: input }\n+          });\n+        }\n       \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n+      // ⭐️ 서버에서 업로드된 파일 URL들을 사용하거나, 임시 URL 사용\n+      const filesForGemini = serverFileUrls.length > 0 \n+        ? serverFileUrls.map((url, index) => ({\n+            name: selectedFiles[index].name,\n+            fileUri: url, // 서버 URL 사용\n+            mimeType: selectedFiles[index].type,\n+          }))\n+        : uploadedFiles.map((file) => ({\n+            name: file.name,\n+            fileUri: file.fileUri,\n+            mimeType: file.mimeType,\n+          }));\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n@@ -216,8 +250,9 @@\n         updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n+      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n"
                },
                {
                    "date": 1756692999670,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,241 @@\n+// src/hooks/useChatActions.ts\n+\n+import { useState } from 'react';\n+import useAI from './useAI';\n+import { useToast } from '@/components/common/ToastProvider';\n+import { useChatStore } from '@/store/chatStore';\n+import { combinePrompts } from '@/ai/promptTemplates';\n+import { uploadFiles, FileUploadData } from '@/firebase.functions';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+\n+// 견적서 데이터를 추출하는 유틸리티 함수\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!match) return null;\n+\n+    const jsonStr = match[1];\n+    const data = JSON.parse(jsonStr);\n+\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n+      console.error('Invalid estimate data structure:', data);\n+      return null;\n+    }\n+\n+    return data as ProjectEstimate;\n+\n+  } catch (error) {\n+    console.error('Failed to parse estimate data:', error);\n+    return null;\n+  }\n+};\n+\n+interface UseChatActionsProps {\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  selectedPromptId: string;\n+}\n+\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n+\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n+\n+  // 파일 업로드 관련 함수들\n+  const handleFileUpload = (fileData: FileUploadData) => {\n+    setUploadedFiles((prev) => [...prev, fileData]);\n+    setUploadProgress(0);\n+    setIsUploading(false);\n+    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  };\n+\n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileName: string) => {\n+    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      setIsUploading(true);\n+      uploadFiles(files, {\n+        onUpload: handleFileUpload,\n+        progress: handleFileUploadProgress,\n+      });\n+    }\n+  };\n+\n+  const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+\n+    setIsProcessing(true);\n+\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        \n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+        } else {\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n+\n+    try {\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: { type: 'text', value: input }\n+      });\n+      \n+      const filesForGemini = uploadedFiles.map((file) => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+      }));\n+\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForGemini);\n+\n+      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n+      const estimateData = extractEstimateData(reply);\n+      if (estimateData) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const filePath = uploadResponse.data as string;\n+              \n+              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n+              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n+              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n+              \n+              if (extractedUuid) {\n+                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n+                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n+                  const updatedEstimateData = {\n+                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n+                      uuid: extractedUuid, // UUID만 추가\n+                  };\n+                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+                  await sendChatMessage(currentSessionId, {\n+                      role: 'AI',\n+                      content: { type: 'text', value: updatedReply }\n+                  });\n+                  updateLastMessage(updatedReply);\n+              } else {\n+                  throw new Error('UUID를 추출할 수 없습니다.');\n+              }\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n+        }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n+      }\n+\n+      setUploadedFiles([]);\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n+    }\n+  };\n+\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n+  };\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756693006864,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -237,281 +237,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-}\n-// src/hooks/useChatActions.ts\n-\n-import { useState } from 'react';\n-import useAI from './useAI';\n-import { useToast } from '@/components/common/ToastProvider';\n-import { useChatStore } from '@/store/chatStore';\n-import { combinePrompts } from '@/ai/promptTemplates';\n-import { uploadFiles, FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-\n-// 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n-  try {\n-    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n-    if (!match) return null;\n-\n-    const jsonStr = match[1];\n-    const data = JSON.parse(jsonStr);\n-\n-    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n-      console.error('Invalid estimate data structure:', data);\n-      return null;\n-    }\n-\n-    return data as ProjectEstimate;\n-\n-  } catch (error) {\n-    console.error('Failed to parse estimate data:', error);\n-    return null;\n-  }\n-};\n-\n-interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n-  selectedPromptId: string;\n-}\n-\n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n-\n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n-  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n-\n-  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n-  const handleFileUpload = (files: File[]) => {\n-    if (files.length === 0) return;\n-    \n-    // ⭐️ 파일 검증\n-    const invalidFiles = files.filter(file => !validateFileType(file));\n-    if (invalidFiles.length > 0) {\n-      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    const oversizedFiles = files.filter(file => !validateFileSize(file));\n-    if (oversizedFiles.length > 0) {\n-      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n-    const fileDataArray: FileUploadData[] = files.map(file => ({\n-      name: file.name,\n-      fileUri: URL.createObjectURL(file), // 임시 URL\n-      mimeType: file.type,\n-      size: file.size,\n-    }));\n-    \n-    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-    // ⭐️ 실제 File 객체들도 저장\n-    setSelectedFiles(prev => [...prev, ...files]);\n-    success(`파일 ${files.length}개가 추가되었습니다.`);\n-  };\n-\n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-          const files = Array.from(e.dataTransfer.files);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-          const files = Array.from(e.target.files || []);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n-  };\n-\n-  const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n-\n-    setIsProcessing(true);\n-\n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n-    }\n-    \n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-        } else {\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n-\n-          try {\n-        // ⭐️ 수정: 파일이 있을 경우 sendMessageWithFiles 사용, 없으면 기존 방식 사용\n-        let serverFileUrls: string[] = [];\n-        if (selectedFiles.length > 0) {\n-          // 파일과 메시지를 함께 전송\n-          const response = await sendMessageWithFiles(currentSessionId, input, selectedFiles);\n-          if (response && response.statusCode === 200 && response.data) {\n-            serverFileUrls = response.data.fileUrls || [];\n-          }\n-        } else {\n-          // 파일이 없으면 기존 방식 사용\n-          await sendChatMessage(currentSessionId, { \n-            role: 'USER', \n-            content: { type: 'text', value: input }\n-          });\n-        }\n-      \n-      // ⭐️ 서버에서 업로드된 파일 URL들을 사용하거나, 임시 URL 사용\n-      const filesForGemini = serverFileUrls.length > 0 \n-        ? serverFileUrls.map((url, index) => ({\n-            name: selectedFiles[index].name,\n-            fileUri: url, // 서버 URL 사용\n-            mimeType: selectedFiles[index].type,\n-          }))\n-        : uploadedFiles.map((file) => ({\n-            name: file.name,\n-            fileUri: file.fileUri,\n-            mimeType: file.mimeType,\n-          }));\n-\n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n-\n-      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n-      const estimateData = extractEstimateData(reply);\n-      if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n-          \n-          // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n-\n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const filePath = uploadResponse.data as string;\n-              \n-              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n-              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n-              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n-              \n-              if (extractedUuid) {\n-                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n-                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n-                  const updatedEstimateData = {\n-                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n-                      uuid: extractedUuid, // UUID만 추가\n-                  };\n-                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-                  await sendChatMessage(currentSessionId, {\n-                      role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n-                  });\n-                  updateLastMessage(updatedReply);\n-              } else {\n-                  throw new Error('UUID를 추출할 수 없습니다.');\n-              }\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          \n-          // PDF 업로드 실패 시 원래 응답 사용\n-          await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-          });\n-          updateLastMessage(reply);\n-        }\n-      } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n-        await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-        });\n-        updateLastMessage(reply);\n-      }\n-\n-      setUploadedFiles([]);\n-      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n-\n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n-  };\n-\n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756693102348,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,11 +4,11 @@\n import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n-import { uploadFiles, FileUploadData } from '@/firebase.functions';\n+import { FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage } from '@/lib/api/user/userApi';\n+import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -53,15 +53,39 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n \n-  // 파일 업로드 관련 함수들\n-  const handleFileUpload = (fileData: FileUploadData) => {\n-    setUploadedFiles((prev) => [...prev, fileData]);\n-    setUploadProgress(0);\n-    setIsUploading(false);\n-    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n+  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n+  const handleFileUpload = (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    // ⭐️ 파일 검증\n+    const invalidFiles = files.filter(file => !validateFileType(file));\n+    if (invalidFiles.length > 0) {\n+      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    const oversizedFiles = files.filter(file => !validateFileSize(file));\n+    if (oversizedFiles.length > 0) {\n+      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n+    const fileDataArray: FileUploadData[] = files.map(file => ({\n+      name: file.name,\n+      fileUri: URL.createObjectURL(file), // 임시 URL\n+      mimeType: file.type,\n+      size: file.size,\n+    }));\n+    \n+    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+    // ⭐️ 실제 File 객체들도 저장\n+    setSelectedFiles(prev => [...prev, ...files]);\n+    success(`파일 ${files.length}개가 추가되었습니다.`);\n   };\n \n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n@@ -86,27 +110,19 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.dataTransfer.files);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n   const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      setIsUploading(true);\n-      uploadFiles(files, {\n-        onUpload: handleFileUpload,\n-        progress: handleFileUploadProgress,\n-      });\n-    }\n+          const files = Array.from(e.target.files || []);\n+      if (files.length > 0) {\n+        handleFileUpload(files);\n+      }\n   };\n \n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n@@ -139,20 +155,39 @@\n         return;\n       }\n     }\n \n-    try {\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: { type: 'text', value: input }\n-      });\n-      \n-      const filesForGemini = uploadedFiles.map((file) => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-      }));\n+          try {\n+        // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n+        let uploadedFileNames: string[] = [];\n+        if (selectedFiles.length > 0) {\n+          // 1. 파일을 먼저 업로드\n+          const uploadResponse = await uploadFiles(selectedFiles);\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+            uploadedFileNames = uploadResponse.data;\n+          }\n+        }\n \n+        // 2. 메시지와 파일 정보를 함께 전송\n+        const messageContent = {\n+          content: input,\n+          file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n+        };\n+\n+        await sendChatMessage(currentSessionId, { \n+          role: 'USER', \n+          content: messageContent\n+        });\n+        \n+        // 3. AI에 전송할 파일 정보 (서버 URL 사용)\n+        const filesForGemini = uploadedFileNames.length > 0 \n+          ? uploadedFileNames.map((fileName, index) => ({\n+              name: selectedFiles[index].name,\n+              fileUri: `/api/file/${fileName}`, // 서버 파일 URL\n+              mimeType: selectedFiles[index].type,\n+            }))\n+          : [];\n+\n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForGemini);\n \n       // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n@@ -216,8 +251,9 @@\n         updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n+      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n"
                },
                {
                    "date": 1756694061751,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -33,8 +33,49 @@\n     return null;\n   }\n };\n \n+// 파일 크기 검증 함수 (단일 파일 20MB, 복수 파일 합계 20MB)\n+const validateFilesSize = (files: File[], maxSizeMB: number = 20): { isValid: boolean; errorMessage?: string } => {\n+  const maxSizeBytes = maxSizeMB * 1024 * 1024;\n+  \n+  // 단일 파일 검증\n+  for (const file of files) {\n+    if (file.size > maxSizeBytes) {\n+      return { \n+        isValid: false, \n+        errorMessage: `파일 \"${file.name}\"이(가) 20MB를 초과합니다.` \n+      };\n+    }\n+  }\n+  \n+  // 복수 파일 합계 검증\n+  const totalSize = files.reduce((sum, file) => sum + file.size, 0);\n+  if (totalSize > maxSizeBytes) {\n+    return { \n+      isValid: false, \n+      errorMessage: `전체 파일 크기가 20MB를 초과합니다. (${(totalSize / 1024 / 1024).toFixed(2)}MB)` \n+    };\n+  }\n+  \n+  return { isValid: true };\n+};\n+\n+// 파일을 base64로 인코딩하는 함수\n+const fileToBase64 = (file: File): Promise<string> => {\n+  return new Promise((resolve, reject) => {\n+    const reader = new FileReader();\n+    reader.readAsDataURL(file);\n+    reader.onload = () => {\n+      const result = reader.result as string;\n+      // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n+      const base64 = result.split(',')[1];\n+      resolve(base64);\n+    };\n+    reader.onerror = error => reject(error);\n+  });\n+};\n+\n interface UseChatActionsProps {\n   modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n   selectedPromptId: string;\n }\n@@ -59,18 +100,19 @@\n   // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n   const handleFileUpload = (files: File[]) => {\n     if (files.length === 0) return;\n     \n-    // ⭐️ 파일 검증\n+    // ⭐️ 파일 형식 검증\n     const invalidFiles = files.filter(file => !validateFileType(file));\n     if (invalidFiles.length > 0) {\n       error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n       return;\n     }\n     \n-    const oversizedFiles = files.filter(file => !validateFileSize(file));\n-    if (oversizedFiles.length > 0) {\n-      error(`파일 크기가 20MB를 초과합니다: ${oversizedFiles.map(f => f.name).join(', ')}`);\n+    // ⭐️ 파일 크기 검증 (단일 파일 20MB, 복수 파일 합계 20MB)\n+    const sizeValidation = validateFilesSize(files);\n+    if (!sizeValidation.isValid) {\n+      error(sizeValidation.errorMessage || '파일 크기 검증에 실패했습니다.');\n       return;\n     }\n     \n     // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n@@ -92,8 +134,9 @@\n   };\n \n   const removeFile = (fileName: string) => {\n     setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+    setSelectedFiles((prev) => prev.filter((file) => file.name !== fileName));\n   };\n \n   const handleDragOver = (e: React.DragEvent) => {\n     e.preventDefault();\n@@ -110,19 +153,19 @@\n   const handleDrop = (e: React.DragEvent) => {\n     e.preventDefault();\n     setIsDragOver(false);\n \n-          const files = Array.from(e.dataTransfer.files);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      handleFileUpload(files);\n+    }\n   };\n \n   const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-          const files = Array.from(e.target.files || []);\n-      if (files.length > 0) {\n-        handleFileUpload(files);\n-      }\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      handleFileUpload(files);\n+    }\n   };\n \n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n@@ -155,41 +198,68 @@\n         return;\n       }\n     }\n \n-          try {\n-        // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n-        let uploadedFileNames: string[] = [];\n-        if (selectedFiles.length > 0) {\n-          // 1. 파일을 먼저 업로드\n-          const uploadResponse = await uploadFiles(selectedFiles);\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-            uploadedFileNames = uploadResponse.data;\n-          }\n+    try {\n+      // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n+      let uploadedFileNames: string[] = [];\n+      let filesForGemini: Array<{name: string, fileUri: string, mimeType: string, base64?: string}> = [];\n+      \n+      if (selectedFiles.length > 0) {\n+        console.log('=== 파일 업로드 프로세스 시작 ===');\n+        console.log('선택된 파일들:', selectedFiles.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)}MB)`));\n+        \n+        // 1. 파일을 먼저 업로드\n+        const uploadResponse = await uploadFiles(selectedFiles);\n+        console.log('파일 업로드 API 응답:', uploadResponse);\n+        \n+        if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+          uploadedFileNames = uploadResponse.data;\n+          console.log('업로드된 파일명들:', uploadedFileNames);\n+          \n+          // 2. 업로드된 파일명으로 URL 생성 및 base64 인코딩 준비\n+          console.log('파일 URL 생성 및 base64 인코딩 시작...');\n+          filesForGemini = await Promise.all(\n+            uploadedFileNames.map(async (fileName, index) => {\n+              const file = selectedFiles[index];\n+              const base64 = await fileToBase64(file);\n+              const fileUrl = `/api/file/${fileName}`;\n+              console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n+              return {\n+                name: file.name,\n+                fileUri: fileUrl, // 서버 파일 URL\n+                mimeType: file.type,\n+                base64: base64 // AI에 전송할 base64 데이터\n+              };\n+            })\n+          );\n+          console.log('=== 파일 업로드 프로세스 완료 ===');\n+        } else {\n+          throw new Error(uploadResponse?.error?.message || '파일 업로드에 실패했습니다.');\n         }\n+      }\n \n-        // 2. 메시지와 파일 정보를 함께 전송\n-        const messageContent = {\n-          content: input,\n-          file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n-        };\n+      // 3. 메시지와 파일 정보를 함께 전송\n+      const messageContent = {\n+        content: input,\n+        file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n+      };\n \n-        await sendChatMessage(currentSessionId, { \n-          role: 'USER', \n-          content: messageContent\n-        });\n-        \n-        // 3. AI에 전송할 파일 정보 (서버 URL 사용)\n-        const filesForGemini = uploadedFileNames.length > 0 \n-          ? uploadedFileNames.map((fileName, index) => ({\n-              name: selectedFiles[index].name,\n-              fileUri: `/api/file/${fileName}`, // 서버 파일 URL\n-              mimeType: selectedFiles[index].type,\n-            }))\n-          : [];\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: messageContent\n+      });\n+      \n+      // 4. AI에 전송할 파일 정보 (base64 포함)\n+      const filesForAI = filesForGemini.map(file => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+        base64: file.base64 // base64 데이터 포함\n+      }));\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForGemini);\n+      const reply = await sendChat(combinedPrompt, filesForAI);\n \n       // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n"
                },
                {
                    "date": 1756694520518,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -132,11 +132,15 @@\n   const handleFileUploadProgress = (progress: number) => {\n     setUploadProgress(progress);\n   };\n \n-  const removeFile = (fileName: string) => {\n-    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n-    setSelectedFiles((prev) => prev.filter((file) => file.name !== fileName));\n+  const removeFile = (fileUri: string) => {\n+    // fileUri로 파일을 찾아서 제거\n+    const fileToRemove = uploadedFiles.find(file => file.fileUri === fileUri);\n+    if (fileToRemove) {\n+      setUploadedFiles((prev) => prev.filter((file) => file.fileUri !== fileUri));\n+      setSelectedFiles((prev) => prev.filter((file) => file.name !== fileToRemove.name));\n+    }\n   };\n \n   const handleDragOver = (e: React.DragEvent) => {\n     e.preventDefault();\n"
                },
                {
                    "date": 1756694972235,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -177,10 +177,10 @@\n     setIsProcessing(true);\n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n+      const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n${fileInfo}`;\n     }\n     \n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n"
                },
                {
                    "date": 1756695149154,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -225,9 +225,9 @@\n           filesForGemini = await Promise.all(\n             uploadedFileNames.map(async (fileName, index) => {\n               const file = selectedFiles[index];\n               const base64 = await fileToBase64(file);\n-              const fileUrl = `/api/file/${fileName}`;\n+              const fileUrl = `/api/file/download/${fileName}`;\n               console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n               return {\n                 name: file.name,\n                 fileUri: fileUrl, // 서버 파일 URL\n"
                },
                {
                    "date": 1756695235519,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -236,8 +236,11 @@\n               };\n             })\n           );\n           console.log('=== 파일 업로드 프로세스 완료 ===');\n+          \n+          // ⭐️ 파일 업로드 완료 후 잠시 대기 (서버에서 파일 저장 완료 대기)\n+          await new Promise(resolve => setTimeout(resolve, 1000));\n         } else {\n           throw new Error(uploadResponse?.error?.message || '파일 업로드에 실패했습니다.');\n         }\n       }\n"
                },
                {
                    "date": 1756701921192,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -3,12 +3,13 @@\n import { useState } from 'react';\n import useAI from './useAI';\n import { useToast } from '@/components/common/ToastProvider';\n import { useChatStore } from '@/store/chatStore';\n+import { useAuthStore } from '@/store/authStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { FileUploadData } from '@/firebase.functions';\n import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n+import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n \n@@ -74,8 +75,21 @@\n     reader.onerror = error => reject(error);\n   });\n };\n \n+// 비회원 UUID 관리 함수\n+const getOrCreateGuestUuid = (): string => {\n+  const storedUuid = localStorage.getItem('guest-uuid');\n+  if (storedUuid) {\n+    return storedUuid;\n+  }\n+  \n+  // UUID가 없으면 새로 생성\n+  const newUuid = uuidv4();\n+  localStorage.setItem('guest-uuid', newUuid);\n+  return newUuid;\n+};\n+\n interface UseChatActionsProps {\n   modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n   selectedPromptId: string;\n }\n@@ -88,8 +102,9 @@\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n+  const { isAuthenticated } = useAuthStore();\n \n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n@@ -187,10 +202,21 @@\n     \n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n-        const createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        let createResponse;\n         \n+        // 인증 상태에 따라 다른 API 호출\n+        if (isAuthenticated()) {\n+          // 회원인 경우 기존 API 사용\n+          createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        } else {\n+          // 비회원인 경우 비회원 전용 API 사용\n+          const guestUuid = getOrCreateGuestUuid();\n+          console.log('비회원 UUID:', guestUuid);\n+          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n+        }\n+        \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n         } else {\n"
                },
                {
                    "date": 1756705426505,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -300,10 +300,31 @@\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n+          // 사용자 ID 가져오기 (회원 또는 비회원)\n+          let userId: string;\n+          if (isAuthenticated()) {\n+            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n+            const authStorage = localStorage.getItem('auth-storage');\n+            if (authStorage) {\n+              const authData = JSON.parse(authStorage);\n+              userId = authData.state?.user?._id;\n+            }\n+          } else {\n+            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n+            userId = localStorage.getItem('guest-uuid') || uuidv4();\n+            if (!localStorage.getItem('guest-uuid')) {\n+              localStorage.setItem('guest-uuid', userId);\n+            }\n+          }\n+\n+          if (!userId) {\n+            throw new Error('사용자 ID를 가져올 수 없습니다.');\n+          }\n+\n           // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, success, error);\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n           // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n           if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n               const filePath = uploadResponse.data as string;\n"
                },
                {
                    "date": 1756705925976,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,12 +215,12 @@\n           console.log('비회원 UUID:', guestUuid);\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-        } else {\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n+  currentSessionId = createResponse.data[0]._id;\n+  setChatSessionId(currentSessionId);\n+}else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756705935001,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -216,11 +216,11 @@\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n-  currentSessionId = createResponse.data[0]._id;\n-  setChatSessionId(currentSessionId);\n-}else {\n+          currentSessionId = createResponse.data[0]._id;\n+          setChatSessionId(currentSessionId);\n+        }else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756705943283,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -218,9 +218,9 @@\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n           currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n-        }else {\n+        } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n         error(`채팅방 생성 실패: ${(e as Error).message}`);\n"
                },
                {
                    "date": 1756706748925,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,8 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+import \n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -215,10 +216,10 @@\n           console.log('비회원 UUID:', guestUuid);\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n-          currentSessionId = createResponse.data[0]._id;\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n"
                },
                {
                    "date": 1756706761111,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import \n+import { useAuthStore } from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -97,8 +97,9 @@\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n   const { sendChat } = useAI(modelName);\n   const { success, error } = useToast();\n+  const \n   const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n     addMessage: s.addMessage,\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n"
                },
                {
                    "date": 1756706835668,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { useAuthStore } from '@/store/authStore';\n+import { useAuthStore  from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -97,9 +97,9 @@\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n   const { sendChat } = useAI(modelName);\n   const { success, error } = useToast();\n-  const \n+  const isAuthenticated = useAuthStore((s) => s.isAuthenticated);\n   const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n     addMessage: s.addMessage,\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n"
                },
                {
                    "date": 1756706840766,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { useAuthStore  from '@/store/authStore';\n+import { useAuthStore } from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1756706851481,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { useAuthStore } from '@/store/authStore';\n+import { is } from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1756706862054,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { is } from '@/store/authStore';\n+import { isAuthenticated } from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -104,9 +104,8 @@\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n-  const { isAuthenticated } = useAuthStore();\n \n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n"
                },
                {
                    "date": 1756706867533,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -104,9 +104,9 @@\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n-\n+ const is\n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n"
                },
                {
                    "date": 1756706879707,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { isAuthenticated } from '@/store/authStore';\n+import {  } from '@/store/authStore';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -104,9 +104,9 @@\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n- const is\n+ const isAuthenticated = useAuthStore((s) => s.isAuthenticated);\n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n"
                },
                {
                    "date": 1756706934605,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,9 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import {  } from '@/store/authStore';\n+import { useAuthStore } from '@/store/authStore'\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1756706942807,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,9 +11,8 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-import { useAuthStore } from '@/store/authStore'\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1756707053303,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -103,9 +103,8 @@\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n- const isAuthenticated = useAuthStore((s) => s.isAuthenticated);\n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n@@ -203,9 +202,10 @@\n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         let createResponse;\n-        \n+        let uid: string;\n+\n         // 인증 상태에 따라 다른 API 호출\n         if (isAuthenticated()) {\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n"
                },
                {
                    "date": 1756707345152,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -96,15 +96,16 @@\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n   const { sendChat } = useAI(modelName);\n   const { success, error } = useToast();\n-  const isAuthenticated = useAuthStore((s) => s.isAuthenticated);\n   const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n     addMessage: s.addMessage,\n     updateLastMessage: s.updateLastMessage,\n     chatSessionId: s.chatSessionId,\n     setChatSessionId: s.setChatSessionId,\n   }));\n+  const { isAuthenticated } = useAuthStore();\n+\n   const [isProcessing, setIsProcessing] = useState(false);\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n@@ -202,10 +203,9 @@\n     let currentSessionId = chatSessionId;\n     if (!currentSessionId) {\n       try {\n         let createResponse;\n-        let uid: string;\n-\n+        \n         // 인증 상태에 따라 다른 API 호출\n         if (isAuthenticated()) {\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n@@ -279,8 +279,9 @@\n \n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n         content: messageContent\n+        uid\n       });\n       \n       // 4. AI에 전송할 파일 정보 (base64 포함)\n       const filesForAI = filesForGemini.map(file => ({\n"
                },
                {
                    "date": 1756707354462,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -278,10 +278,10 @@\n       };\n \n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n-        content: messageContent\n-        uid\n+        content: messageContent,\n+        uid: userId,\n       });\n       \n       // 4. AI에 전송할 파일 정보 (base64 포함)\n       const filesForAI = filesForGemini.map(file => ({\n@@ -344,9 +344,10 @@\n                   const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n \n                   await sendChatMessage(currentSessionId, {\n                       role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n+                      content: { type: 'text', value: updatedReply },\n+                      uid: userId,\n                   });\n                   updateLastMessage(updatedReply);\n               } else {\n                   throw new Error('UUID를 추출할 수 없습니다.');\n"
                },
                {
                    "date": 1756707360105,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -363,9 +363,10 @@\n           \n           // PDF 업로드 실패 시 원래 응답 사용\n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply }\n+            content: { type: 'text', value: reply },\n+            uid: userId,\n           });\n           updateLastMessage(reply);\n         }\n       } else {\n"
                },
                {
                    "date": 1756707432088,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -216,9 +216,9 @@\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n+          currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n"
                },
                {
                    "date": 1756707439949,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -215,9 +215,9 @@\n           console.log('비회원 UUID:', guestUuid);\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n           currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n"
                },
                {
                    "date": 1756707533743,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -302,9 +302,8 @@\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n           // 사용자 ID 가져오기 (회원 또는 비회원)\n-          let userId: string;\n           if (isAuthenticated()) {\n             // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n             const authStorage = localStorage.getItem('auth-storage');\n             if (authStorage) {\n"
                },
                {
                    "date": 1756707545641,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -205,8 +205,10 @@\n       try {\n         let createResponse;\n         \n         // 인증 상태에 따라 다른 API 호출\n+        let userId: string;\n+\n         if (isAuthenticated()) {\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         } else {\n"
                },
                {
                    "date": 1756707557431,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -205,10 +205,8 @@\n       try {\n         let createResponse;\n         \n         // 인증 상태에 따라 다른 API 호출\n-        let userId: string;\n-\n         if (isAuthenticated()) {\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         } else {\n@@ -304,8 +302,9 @@\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n           // 사용자 ID 가져오기 (회원 또는 비회원)\n+          let userId: string;\n           if (isAuthenticated()) {\n             // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n             const authStorage = localStorage.getItem('auth-storage');\n             if (authStorage) {\n"
                },
                {
                    "date": 1756707592666,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -189,8 +189,9 @@\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n+    let userId: string; \n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n"
                },
                {
                    "date": 1756707615995,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -303,9 +303,8 @@\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n           // 사용자 ID 가져오기 (회원 또는 비회원)\n-          let userId: string;\n           if (isAuthenticated()) {\n             // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n             const authStorage = localStorage.getItem('auth-storage');\n             if (authStorage) {\n"
                },
                {
                    "date": 1756707683788,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,24 +54,24 @@\n   if (totalSize > maxSizeBytes) {\n     return { \n       isValid: false, \n       errorMessage: `전체 파일 크기가 20MB를 초과합니다. (${(totalSize / 1024 / 1024).toFixed(2)}MB)` \n-    };\n-  }\n-  \n-  return { isValid: true };\n+      };\n+    }\n+    \n+    return { isValid: true };\n };\n \n // 파일을 base64로 인코딩하는 함수\n const fileToBase64 = (file: File): Promise<string> => {\n-  return new Promise((resolve, reject) => {\n+    return new Promise((resolve, reject) => {\n     const reader = new FileReader();\n     reader.readAsDataURL(file);\n     reader.onload = () => {\n-      const result = reader.result as string;\n-      // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n-      const base64 = result.split(',')[1];\n-      resolve(base64);\n+        const result = reader.result as string;\n+        // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n+        const base64 = result.split(',')[1];\n+        resolve(base64);\n     };\n     reader.onerror = error => reject(error);\n   });\n };\n@@ -189,10 +189,30 @@\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n-    let userId: string; \n \n+    // ⭐ userId 변수를 함수 최상단에 선언\n+    let userId: string;\n+    \n+    // ⭐ userId 값 할당\n+    if (isAuthenticated()) {\n+      const authStorage = localStorage.getItem('auth-storage');\n+      if (authStorage) {\n+        const authData = JSON.parse(authStorage);\n+        userId = authData.state?.user?._id;\n+      }\n+    } else {\n+      userId = getOrCreateGuestUuid();\n+    }\n+    \n+    // ⭐ userId가 할당되지 않았을 경우를 대비한 방어 코드\n+    if (!userId) {\n+      error('사용자 ID를 가져올 수 없습니다.');\n+      setIsProcessing(false);\n+      return;\n+    }\n+\n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n${fileInfo}`;\n@@ -211,11 +231,11 @@\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         } else {\n           // 비회원인 경우 비회원 전용 API 사용\n-          const guestUuid = getOrCreateGuestUuid();\n-          console.log('비회원 UUID:', guestUuid);\n-          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n+          // ⭐ getOrCreateGuestUuid() 대신 위에서 할당된 userId 사용\n+          console.log('비회원 UUID:', userId);\n+          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n         }\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n           currentSessionId = createResponse.data[0]._id;\n@@ -302,28 +322,8 @@\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n-          // 사용자 ID 가져오기 (회원 또는 비회원)\n-          if (isAuthenticated()) {\n-            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n-            const authStorage = localStorage.getItem('auth-storage');\n-            if (authStorage) {\n-              const authData = JSON.parse(authStorage);\n-              userId = authData.state?.user?._id;\n-            }\n-          } else {\n-            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n-            userId = localStorage.getItem('guest-uuid') || uuidv4();\n-            if (!localStorage.getItem('guest-uuid')) {\n-              localStorage.setItem('guest-uuid', userId);\n-            }\n-          }\n-\n-          if (!userId) {\n-            throw new Error('사용자 ID를 가져올 수 없습니다.');\n-          }\n-\n           // PDF 생성 및 업로드 함수 호출\n           const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n           // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n@@ -372,9 +372,10 @@\n       } else {\n         // 견적서가 아닐 경우 기존 메시지 전송\n         await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply }\n+            content: { type: 'text', value: reply },\n+            uid: userId,\n         });\n         updateLastMessage(reply);\n       }\n \n@@ -400,5 +401,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-}\n\\ No newline at end of file\n+} 여기 `createChatSession` 함수에서 userId값을 안쓰는데? 그거랑 `generateAndUploadPdf`에서도 `userId` 값 이 두번 선언된거 같아 보여.\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756707689553,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -401,5 +401,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-} 여기 `createChatSession` 함수에서 userId값을 안쓰는데? 그거랑 `generateAndUploadPdf`에서도 `userId` 값 이 두번 선언된거 같아 보여.\n\\ No newline at end of file\n+} \n\\ No newline at end of file\n"
                },
                {
                    "date": 1756713935900,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,24 +54,24 @@\n   if (totalSize > maxSizeBytes) {\n     return { \n       isValid: false, \n       errorMessage: `전체 파일 크기가 20MB를 초과합니다. (${(totalSize / 1024 / 1024).toFixed(2)}MB)` \n-      };\n-    }\n-    \n-    return { isValid: true };\n+    };\n+  }\n+  \n+  return { isValid: true };\n };\n \n // 파일을 base64로 인코딩하는 함수\n const fileToBase64 = (file: File): Promise<string> => {\n-    return new Promise((resolve, reject) => {\n+  return new Promise((resolve, reject) => {\n     const reader = new FileReader();\n     reader.readAsDataURL(file);\n     reader.onload = () => {\n-        const result = reader.result as string;\n-        // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n-        const base64 = result.split(',')[1];\n-        resolve(base64);\n+      const result = reader.result as string;\n+      // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n+      const base64 = result.split(',')[1];\n+      resolve(base64);\n     };\n     reader.onerror = error => reject(error);\n   });\n };\n@@ -190,29 +190,8 @@\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n \n-    // ⭐ userId 변수를 함수 최상단에 선언\n-    let userId: string;\n-    \n-    // ⭐ userId 값 할당\n-    if (isAuthenticated()) {\n-      const authStorage = localStorage.getItem('auth-storage');\n-      if (authStorage) {\n-        const authData = JSON.parse(authStorage);\n-        userId = authData.state?.user?._id;\n-      }\n-    } else {\n-      userId = getOrCreateGuestUuid();\n-    }\n-    \n-    // ⭐ userId가 할당되지 않았을 경우를 대비한 방어 코드\n-    if (!userId) {\n-      error('사용자 ID를 가져올 수 없습니다.');\n-      setIsProcessing(false);\n-      return;\n-    }\n-\n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n${fileInfo}`;\n@@ -231,15 +210,15 @@\n           // 회원인 경우 기존 API 사용\n           createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n         } else {\n           // 비회원인 경우 비회원 전용 API 사용\n-          // ⭐ getOrCreateGuestUuid() 대신 위에서 할당된 userId 사용\n-          console.log('비회원 UUID:', userId);\n-          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n+          const guestUuid = getOrCreateGuestUuid();\n+          console.log('비회원 UUID:', guestUuid);\n+          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n         }\n         \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n-          currentSessionId = createResponse.data[0]._id;\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n@@ -299,10 +278,9 @@\n       };\n \n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n-        content: messageContent,\n-        uid: userId,\n+        content: messageContent\n       });\n       \n       // 4. AI에 전송할 파일 정보 (base64 포함)\n       const filesForAI = filesForGemini.map(file => ({\n@@ -322,8 +300,29 @@\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n           \n+          // 사용자 ID 가져오기 (회원 또는 비회원)\n+          let userId: string;\n+          if (isAuthenticated()) {\n+            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n+            const authStorage = localStorage.getItem('auth-storage');\n+            if (authStorage) {\n+              const authData = JSON.parse(authStorage);\n+              userId = authData.state?.user?._id;\n+            }\n+          } else {\n+            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n+            userId = localStorage.getItem('guest-uuid') || uuidv4();\n+            if (!localStorage.getItem('guest-uuid')) {\n+              localStorage.setItem('guest-uuid', userId);\n+            }\n+          }\n+\n+          if (!userId) {\n+            throw new Error('사용자 ID를 가져올 수 없습니다.');\n+          }\n+\n           // PDF 생성 및 업로드 함수 호출\n           const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n           // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n@@ -344,10 +343,9 @@\n                   const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n \n                   await sendChatMessage(currentSessionId, {\n                       role: 'AI',\n-                      content: { type: 'text', value: updatedReply },\n-                      uid: userId,\n+                      content: { type: 'text', value: updatedReply }\n                   });\n                   updateLastMessage(updatedReply);\n               } else {\n                   throw new Error('UUID를 추출할 수 없습니다.');\n@@ -363,19 +361,17 @@\n           \n           // PDF 업로드 실패 시 원래 응답 사용\n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply },\n-            uid: userId,\n+            content: { type: 'text', value: reply }\n           });\n           updateLastMessage(reply);\n         }\n       } else {\n         // 견적서가 아닐 경우 기존 메시지 전송\n         await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply },\n-            uid: userId,\n+            content: { type: 'text', value: reply }\n         });\n         updateLastMessage(reply);\n       }\n \n@@ -401,5 +397,5 @@\n     handleFileInput,\n     removeFile,\n     uploadProgress,\n   };\n-} \n\\ No newline at end of file\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756714623080,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,401 @@\n+// src/hooks/useChatActions.ts\n+\n+import { useState } from 'react';\n+import useAI from './useAI';\n+import { useToast } from '@/components/common/ToastProvider';\n+import { useChatStore } from '@/store/chatStore';\n+import { useAuthStore } from '@/store/authStore';\n+import { combinePrompts } from '@/ai/promptTemplates';\n+import { FileUploadData } from '@/firebase.functions';\n+import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n+import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n+import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n+import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+\n+// 견적서 데이터를 추출하는 유틸리티 함수\n+const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  try {\n+    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    if (!match) return null;\n+\n+    const jsonStr = match[1];\n+    const data = JSON.parse(jsonStr);\n+\n+    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n+      console.error('Invalid estimate data structure:', data);\n+      return null;\n+    }\n+\n+    return data as ProjectEstimate;\n+\n+  } catch (error) {\n+    console.error('Failed to parse estimate data:', error);\n+    return null;\n+  }\n+};\n+\n+// 파일 크기 검증 함수 (단일 파일 20MB, 복수 파일 합계 20MB)\n+const validateFilesSize = (files: File[], maxSizeMB: number = 20): { isValid: boolean; errorMessage?: string } => {\n+  const maxSizeBytes = maxSizeMB * 1024 * 1024;\n+  \n+  // 단일 파일 검증\n+  for (const file of files) {\n+    if (file.size > maxSizeBytes) {\n+      return { \n+        isValid: false, \n+        errorMessage: `파일 \"${file.name}\"이(가) 20MB를 초과합니다.` \n+      };\n+    }\n+  }\n+  \n+  // 복수 파일 합계 검증\n+  const totalSize = files.reduce((sum, file) => sum + file.size, 0);\n+  if (totalSize > maxSizeBytes) {\n+    return { \n+      isValid: false, \n+      errorMessage: `전체 파일 크기가 20MB를 초과합니다. (${(totalSize / 1024 / 1024).toFixed(2)}MB)` \n+    };\n+  }\n+  \n+  return { isValid: true };\n+};\n+\n+// 파일을 base64로 인코딩하는 함수\n+const fileToBase64 = (file: File): Promise<string> => {\n+  return new Promise((resolve, reject) => {\n+    const reader = new FileReader();\n+    reader.readAsDataURL(file);\n+    reader.onload = () => {\n+      const result = reader.result as string;\n+      // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n+      const base64 = result.split(',')[1];\n+      resolve(base64);\n+    };\n+    reader.onerror = error => reject(error);\n+  });\n+};\n+\n+// 비회원 UUID 관리 함수\n+const getOrCreateGuestUuid = (): string => {\n+  const storedUuid = localStorage.getItem('guest-uuid');\n+  if (storedUuid) {\n+    return storedUuid;\n+  }\n+  \n+  // UUID가 없으면 새로 생성\n+  const newUuid = uuidv4();\n+  localStorage.setItem('guest-uuid', newUuid);\n+  return newUuid;\n+};\n+\n+interface UseChatActionsProps {\n+  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  selectedPromptId: string;\n+}\n+\n+export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n+  const { sendChat } = useAI(modelName);\n+  const { success, error } = useToast();\n+  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n+    addMessage: s.addMessage,\n+    updateLastMessage: s.updateLastMessage,\n+    chatSessionId: s.chatSessionId,\n+    setChatSessionId: s.setChatSessionId,\n+  }));\n+  const { isAuthenticated } = useAuthStore();\n+\n+  const [isProcessing, setIsProcessing] = useState(false);\n+  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n+  const [isDragOver, setIsDragOver] = useState(false);\n+  const [uploadProgress, setUploadProgress] = useState(0);\n+  const [isUploading, setIsUploading] = useState(false);\n+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n+\n+  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n+  const handleFileUpload = (files: File[]) => {\n+    if (files.length === 0) return;\n+    \n+    // ⭐️ 파일 형식 검증\n+    const invalidFiles = files.filter(file => !validateFileType(file));\n+    if (invalidFiles.length > 0) {\n+      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n+      return;\n+    }\n+    \n+    // ⭐️ 파일 크기 검증 (단일 파일 20MB, 복수 파일 합계 20MB)\n+    const sizeValidation = validateFilesSize(files);\n+    if (!sizeValidation.isValid) {\n+      error(sizeValidation.errorMessage || '파일 크기 검증에 실패했습니다.');\n+      return;\n+    }\n+    \n+    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n+    const fileDataArray: FileUploadData[] = files.map(file => ({\n+      name: file.name,\n+      fileUri: URL.createObjectURL(file), // 임시 URL\n+      mimeType: file.type,\n+      size: file.size,\n+    }));\n+    \n+    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n+    // ⭐️ 실제 File 객체들도 저장\n+    setSelectedFiles(prev => [...prev, ...files]);\n+    success(`파일 ${files.length}개가 추가되었습니다.`);\n+  };\n+\n+  const handleFileUploadProgress = (progress: number) => {\n+    setUploadProgress(progress);\n+  };\n+\n+  const removeFile = (fileUri: string) => {\n+    // fileUri로 파일을 찾아서 제거\n+    const fileToRemove = uploadedFiles.find(file => file.fileUri === fileUri);\n+    if (fileToRemove) {\n+      setUploadedFiles((prev) => prev.filter((file) => file.fileUri !== fileUri));\n+      setSelectedFiles((prev) => prev.filter((file) => file.name !== fileToRemove.name));\n+    }\n+  };\n+\n+  const handleDragOver = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    if (!isUploading && !isProcessing) {\n+      setIsDragOver(true);\n+    }\n+  };\n+\n+  const handleDragLeave = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+  };\n+\n+  const handleDrop = (e: React.DragEvent) => {\n+    e.preventDefault();\n+    setIsDragOver(false);\n+\n+    const files = Array.from(e.dataTransfer.files);\n+    if (files.length > 0) {\n+      handleFileUpload(files);\n+    }\n+  };\n+\n+  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n+    const files = Array.from(e.target.files || []);\n+    if (files.length > 0) {\n+      handleFileUpload(files);\n+    }\n+  };\n+\n+  export const handleSubmit = async (input: string) => {\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+\n+    setIsProcessing(true);\n+\n+    let userMessageContent = input;\n+    if (uploadedFiles.length > 0) {\n+      const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n+      userMessageContent = `${input}\\n\\n${fileInfo}`;\n+    }\n+    \n+    addMessage({ role: 'user', content: userMessageContent });\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n+    \n+    let currentSessionId = chatSessionId;\n+    if (!currentSessionId) {\n+      try {\n+        let createResponse;\n+        \n+        // 인증 상태에 따라 다른 API 호출\n+        if (isAuthenticated()) {\n+          // 회원인 경우 기존 API 사용\n+          createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+        } else {\n+          // 비회원인 경우 비회원 전용 API 사용\n+          const guestUuid = getOrCreateGuestUuid();\n+          console.log('비회원 UUID:', guestUuid);\n+          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n+        }\n+        \n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+          currentSessionId = createResponse.data._id;\n+          setChatSessionId(currentSessionId);\n+        } else {\n+          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n+        }\n+      } catch (e) {\n+        error(`채팅방 생성 실패: ${(e as Error).message}`);\n+        setIsProcessing(false);\n+        return;\n+      }\n+    }\n+\n+    try {\n+      // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n+      let uploadedFileNames: string[] = [];\n+      let filesForGemini: Array<{name: string, fileUri: string, mimeType: string, base64?: string}> = [];\n+      \n+      if (selectedFiles.length > 0) {\n+        console.log('=== 파일 업로드 프로세스 시작 ===');\n+        console.log('선택된 파일들:', selectedFiles.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)}MB)`));\n+        \n+        // 1. 파일을 먼저 업로드\n+        const uploadResponse = await uploadFiles(selectedFiles);\n+        console.log('파일 업로드 API 응답:', uploadResponse);\n+        \n+        if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+          uploadedFileNames = uploadResponse.data;\n+          console.log('업로드된 파일명들:', uploadedFileNames);\n+          \n+          // 2. 업로드된 파일명으로 URL 생성 및 base64 인코딩 준비\n+          console.log('파일 URL 생성 및 base64 인코딩 시작...');\n+          filesForGemini = await Promise.all(\n+            uploadedFileNames.map(async (fileName, index) => {\n+              const file = selectedFiles[index];\n+              const base64 = await fileToBase64(file);\n+              const fileUrl = `/api/file/download/${fileName}`;\n+              console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n+              return {\n+                name: file.name,\n+                fileUri: fileUrl, // 서버 파일 URL\n+                mimeType: file.type,\n+                base64: base64 // AI에 전송할 base64 데이터\n+              };\n+            })\n+          );\n+          console.log('=== 파일 업로드 프로세스 완료 ===');\n+          \n+          // ⭐️ 파일 업로드 완료 후 잠시 대기 (서버에서 파일 저장 완료 대기)\n+          await new Promise(resolve => setTimeout(resolve, 1000));\n+        } else {\n+          throw new Error(uploadResponse?.error?.message || '파일 업로드에 실패했습니다.');\n+        }\n+      }\n+\n+      // 3. 메시지와 파일 정보를 함께 전송\n+      const messageContent = {\n+        content: input,\n+        file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n+      };\n+\n+      await sendChatMessage(currentSessionId, { \n+        role: 'USER', \n+        content: messageContent\n+      });\n+      \n+      // 4. AI에 전송할 파일 정보 (base64 포함)\n+      const filesForAI = filesForGemini.map(file => ({\n+        name: file.name,\n+        fileUri: file.fileUri,\n+        mimeType: file.mimeType,\n+        base64: file.base64 // base64 데이터 포함\n+      }));\n+\n+      const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      const reply = await sendChat(combinedPrompt, filesForAI);\n+\n+      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n+      const estimateData = extractEstimateData(reply);\n+      if (estimateData) {\n+        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        try {\n+          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n+          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          \n+          // 사용자 ID 가져오기 (회원 또는 비회원)\n+          let userId: string;\n+          if (isAuthenticated()) {\n+            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n+            const authStorage = localStorage.getItem('auth-storage');\n+            if (authStorage) {\n+              const authData = JSON.parse(authStorage);\n+              userId = authData.state?.user?._id;\n+            }\n+          } else {\n+            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n+            userId = localStorage.getItem('guest-uuid') || uuidv4();\n+            if (!localStorage.getItem('guest-uuid')) {\n+              localStorage.setItem('guest-uuid', userId);\n+            }\n+          }\n+\n+          if (!userId) {\n+            throw new Error('사용자 ID를 가져올 수 없습니다.');\n+          }\n+\n+          // PDF 생성 및 업로드 함수 호출\n+          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n+\n+          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+              const filePath = uploadResponse.data as string;\n+              \n+              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n+              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n+              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n+              \n+              if (extractedUuid) {\n+                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n+                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n+                  const updatedEstimateData = {\n+                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n+                      uuid: extractedUuid, // UUID만 추가\n+                  };\n+                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n+\n+                  await sendChatMessage(currentSessionId, {\n+                      role: 'AI',\n+                      content: { type: 'text', value: updatedReply }\n+                  });\n+                  updateLastMessage(updatedReply);\n+              } else {\n+                  throw new Error('UUID를 추출할 수 없습니다.');\n+              }\n+\n+          } else {\n+              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n+          }\n+\n+        } catch (pdfError) {\n+          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n+          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          \n+          // PDF 업로드 실패 시 원래 응답 사용\n+          await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+          });\n+          updateLastMessage(reply);\n+        }\n+      } else {\n+        // 견적서가 아닐 경우 기존 메시지 전송\n+        await sendChatMessage(currentSessionId, {\n+            role: 'AI',\n+            content: { type: 'text', value: reply }\n+        });\n+        updateLastMessage(reply);\n+      }\n+\n+      setUploadedFiles([]);\n+      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n+\n+    } catch (e) {\n+      error(`메시지 전송 실패: ${(e as Error).message}`);\n+    } finally {\n+      setIsProcessing(false);\n+    }\n+  };\n+\n+  return {\n+    handleSubmit,\n+    isProcessing,\n+    uploadedFiles,\n+    isDragOver,\n+    isUploading,\n+    handleDragOver,\n+    handleDragLeave,\n+    handleDrop,\n+    handleFileInput,\n+    removeFile,\n+    uploadProgress,\n+  };\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1756714632634,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -185,409 +185,8 @@\n       handleFileUpload(files);\n     }\n   };\n \n-  export const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n-\n-    setIsProcessing(true);\n-\n-    let userMessageContent = input;\n-    if (uploadedFiles.length > 0) {\n-      const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n-      userMessageContent = `${input}\\n\\n${fileInfo}`;\n-    }\n-    \n-    addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n-    \n-    let currentSessionId = chatSessionId;\n-    if (!currentSessionId) {\n-      try {\n-        let createResponse;\n-        \n-        // 인증 상태에 따라 다른 API 호출\n-        if (isAuthenticated()) {\n-          // 회원인 경우 기존 API 사용\n-          createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n-        } else {\n-          // 비회원인 경우 비회원 전용 API 사용\n-          const guestUuid = getOrCreateGuestUuid();\n-          console.log('비회원 UUID:', guestUuid);\n-          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n-        }\n-        \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n-          setChatSessionId(currentSessionId);\n-        } else {\n-          throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n-        }\n-      } catch (e) {\n-        error(`채팅방 생성 실패: ${(e as Error).message}`);\n-        setIsProcessing(false);\n-        return;\n-      }\n-    }\n-\n-    try {\n-      // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n-      let uploadedFileNames: string[] = [];\n-      let filesForGemini: Array<{name: string, fileUri: string, mimeType: string, base64?: string}> = [];\n-      \n-      if (selectedFiles.length > 0) {\n-        console.log('=== 파일 업로드 프로세스 시작 ===');\n-        console.log('선택된 파일들:', selectedFiles.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)}MB)`));\n-        \n-        // 1. 파일을 먼저 업로드\n-        const uploadResponse = await uploadFiles(selectedFiles);\n-        console.log('파일 업로드 API 응답:', uploadResponse);\n-        \n-        if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-          uploadedFileNames = uploadResponse.data;\n-          console.log('업로드된 파일명들:', uploadedFileNames);\n-          \n-          // 2. 업로드된 파일명으로 URL 생성 및 base64 인코딩 준비\n-          console.log('파일 URL 생성 및 base64 인코딩 시작...');\n-          filesForGemini = await Promise.all(\n-            uploadedFileNames.map(async (fileName, index) => {\n-              const file = selectedFiles[index];\n-              const base64 = await fileToBase64(file);\n-              const fileUrl = `/api/file/download/${fileName}`;\n-              console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n-              return {\n-                name: file.name,\n-                fileUri: fileUrl, // 서버 파일 URL\n-                mimeType: file.type,\n-                base64: base64 // AI에 전송할 base64 데이터\n-              };\n-            })\n-          );\n-          console.log('=== 파일 업로드 프로세스 완료 ===');\n-          \n-          // ⭐️ 파일 업로드 완료 후 잠시 대기 (서버에서 파일 저장 완료 대기)\n-          await new Promise(resolve => setTimeout(resolve, 1000));\n-        } else {\n-          throw new Error(uploadResponse?.error?.message || '파일 업로드에 실패했습니다.');\n-        }\n-      }\n-\n-      // 3. 메시지와 파일 정보를 함께 전송\n-      const messageContent = {\n-        content: input,\n-        file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n-      };\n-\n-      await sendChatMessage(currentSessionId, { \n-        role: 'USER', \n-        content: messageContent\n-      });\n-      \n-      // 4. AI에 전송할 파일 정보 (base64 포함)\n-      const filesForAI = filesForGemini.map(file => ({\n-        name: file.name,\n-        fileUri: file.fileUri,\n-        mimeType: file.mimeType,\n-        base64: file.base64 // base64 데이터 포함\n-      }));\n-\n-      const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      const reply = await sendChat(combinedPrompt, filesForAI);\n-\n-      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n-      const estimateData = extractEstimateData(reply);\n-      if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n-        try {\n-          const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n-          \n-          // 사용자 ID 가져오기 (회원 또는 비회원)\n-          let userId: string;\n-          if (isAuthenticated()) {\n-            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n-            const authStorage = localStorage.getItem('auth-storage');\n-            if (authStorage) {\n-              const authData = JSON.parse(authStorage);\n-              userId = authData.state?.user?._id;\n-            }\n-          } else {\n-            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n-            userId = localStorage.getItem('guest-uuid') || uuidv4();\n-            if (!localStorage.getItem('guest-uuid')) {\n-              localStorage.setItem('guest-uuid', userId);\n-            }\n-          }\n-\n-          if (!userId) {\n-            throw new Error('사용자 ID를 가져올 수 없습니다.');\n-          }\n-\n-          // PDF 생성 및 업로드 함수 호출\n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n-\n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const filePath = uploadResponse.data as string;\n-              \n-              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n-              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n-              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n-              \n-              if (extractedUuid) {\n-                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n-                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n-                  const updatedEstimateData = {\n-                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n-                      uuid: extractedUuid, // UUID만 추가\n-                  };\n-                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-                  await sendChatMessage(currentSessionId, {\n-                      role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n-                  });\n-                  updateLastMessage(updatedReply);\n-              } else {\n-                  throw new Error('UUID를 추출할 수 없습니다.');\n-              }\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n-        } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n-          \n-          // PDF 업로드 실패 시 원래 응답 사용\n-          await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-          });\n-          updateLastMessage(reply);\n-        }\n-      } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n-        await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply }\n-        });\n-        updateLastMessage(reply);\n-      }\n-\n-      setUploadedFiles([]);\n-      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n-\n-    } catch (e) {\n-      error(`메시지 전송 실패: ${(e as Error).message}`);\n-    } finally {\n-      setIsProcessing(false);\n-    }\n-  };\n-\n-  return {\n-    handleSubmit,\n-    isProcessing,\n-    uploadedFiles,\n-    isDragOver,\n-    isUploading,\n-    handleDragOver,\n-    handleDragLeave,\n-    handleDrop,\n-    handleFileInput,\n-    removeFile,\n-    uploadProgress,\n-  };\n-}\n-// src/hooks/useChatActions.ts\n-\n-import { useState } from 'react';\n-import useAI from './useAI';\n-import { useToast } from '@/components/common/ToastProvider';\n-import { useChatStore } from '@/store/chatStore';\n-import { useAuthStore } from '@/store/authStore';\n-import { combinePrompts } from '@/ai/promptTemplates';\n-import { FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n-import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n-import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n-import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n-\n-// 견적서 데이터를 추출하는 유틸리티 함수\n-const extractEstimateData = (content: string): ProjectEstimate | null => {\n-  try {\n-    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n-    if (!match) return null;\n-\n-    const jsonStr = match[1];\n-    const data = JSON.parse(jsonStr);\n-\n-    if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n-      console.error('Invalid estimate data structure:', data);\n-      return null;\n-    }\n-\n-    return data as ProjectEstimate;\n-\n-  } catch (error) {\n-    console.error('Failed to parse estimate data:', error);\n-    return null;\n-  }\n-};\n-\n-// 파일 크기 검증 함수 (단일 파일 20MB, 복수 파일 합계 20MB)\n-const validateFilesSize = (files: File[], maxSizeMB: number = 20): { isValid: boolean; errorMessage?: string } => {\n-  const maxSizeBytes = maxSizeMB * 1024 * 1024;\n-  \n-  // 단일 파일 검증\n-  for (const file of files) {\n-    if (file.size > maxSizeBytes) {\n-      return { \n-        isValid: false, \n-        errorMessage: `파일 \"${file.name}\"이(가) 20MB를 초과합니다.` \n-      };\n-    }\n-  }\n-  \n-  // 복수 파일 합계 검증\n-  const totalSize = files.reduce((sum, file) => sum + file.size, 0);\n-  if (totalSize > maxSizeBytes) {\n-    return { \n-      isValid: false, \n-      errorMessage: `전체 파일 크기가 20MB를 초과합니다. (${(totalSize / 1024 / 1024).toFixed(2)}MB)` \n-    };\n-  }\n-  \n-  return { isValid: true };\n-};\n-\n-// 파일을 base64로 인코딩하는 함수\n-const fileToBase64 = (file: File): Promise<string> => {\n-  return new Promise((resolve, reject) => {\n-    const reader = new FileReader();\n-    reader.readAsDataURL(file);\n-    reader.onload = () => {\n-      const result = reader.result as string;\n-      // data:image/jpeg;base64, 부분을 제거하고 base64 부분만 반환\n-      const base64 = result.split(',')[1];\n-      resolve(base64);\n-    };\n-    reader.onerror = error => reject(error);\n-  });\n-};\n-\n-// 비회원 UUID 관리 함수\n-const getOrCreateGuestUuid = (): string => {\n-  const storedUuid = localStorage.getItem('guest-uuid');\n-  if (storedUuid) {\n-    return storedUuid;\n-  }\n-  \n-  // UUID가 없으면 새로 생성\n-  const newUuid = uuidv4();\n-  localStorage.setItem('guest-uuid', newUuid);\n-  return newUuid;\n-};\n-\n-interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n-  selectedPromptId: string;\n-}\n-\n-export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n-  const { sendChat } = useAI(modelName);\n-  const { success, error } = useToast();\n-  const { addMessage, updateLastMessage, chatSessionId, setChatSessionId } = useChatStore((s) => ({\n-    addMessage: s.addMessage,\n-    updateLastMessage: s.updateLastMessage,\n-    chatSessionId: s.chatSessionId,\n-    setChatSessionId: s.setChatSessionId,\n-  }));\n-  const { isAuthenticated } = useAuthStore();\n-\n-  const [isProcessing, setIsProcessing] = useState(false);\n-  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n-  const [isDragOver, setIsDragOver] = useState(false);\n-  const [uploadProgress, setUploadProgress] = useState(0);\n-  const [isUploading, setIsUploading] = useState(false);\n-  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n-\n-  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n-  const handleFileUpload = (files: File[]) => {\n-    if (files.length === 0) return;\n-    \n-    // ⭐️ 파일 형식 검증\n-    const invalidFiles = files.filter(file => !validateFileType(file));\n-    if (invalidFiles.length > 0) {\n-      error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n-      return;\n-    }\n-    \n-    // ⭐️ 파일 크기 검증 (단일 파일 20MB, 복수 파일 합계 20MB)\n-    const sizeValidation = validateFilesSize(files);\n-    if (!sizeValidation.isValid) {\n-      error(sizeValidation.errorMessage || '파일 크기 검증에 실패했습니다.');\n-      return;\n-    }\n-    \n-    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n-    const fileDataArray: FileUploadData[] = files.map(file => ({\n-      name: file.name,\n-      fileUri: URL.createObjectURL(file), // 임시 URL\n-      mimeType: file.type,\n-      size: file.size,\n-    }));\n-    \n-    setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-    // ⭐️ 실제 File 객체들도 저장\n-    setSelectedFiles(prev => [...prev, ...files]);\n-    success(`파일 ${files.length}개가 추가되었습니다.`);\n-  };\n-\n-  const handleFileUploadProgress = (progress: number) => {\n-    setUploadProgress(progress);\n-  };\n-\n-  const removeFile = (fileUri: string) => {\n-    // fileUri로 파일을 찾아서 제거\n-    const fileToRemove = uploadedFiles.find(file => file.fileUri === fileUri);\n-    if (fileToRemove) {\n-      setUploadedFiles((prev) => prev.filter((file) => file.fileUri !== fileUri));\n-      setSelectedFiles((prev) => prev.filter((file) => file.name !== fileToRemove.name));\n-    }\n-  };\n-\n-  const handleDragOver = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    if (!isUploading && !isProcessing) {\n-      setIsDragOver(true);\n-    }\n-  };\n-\n-  const handleDragLeave = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-  };\n-\n-  const handleDrop = (e: React.DragEvent) => {\n-    e.preventDefault();\n-    setIsDragOver(false);\n-\n-    const files = Array.from(e.dataTransfer.files);\n-    if (files.length > 0) {\n-      handleFileUpload(files);\n-    }\n-  };\n-\n-  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n-    const files = Array.from(e.target.files || []);\n-    if (files.length > 0) {\n-      handleFileUpload(files);\n-    }\n-  };\n-\n   const handleSubmit = async (input: string) => {\n     if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n"
                },
                {
                    "date": 1756715774596,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -200,8 +200,10 @@\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n     \n     let currentSessionId = chatSessionId;\n+    let userId; \n+    \n     if (!currentSessionId) {\n       try {\n         let createResponse;\n         \n"
                },
                {
                    "date": 1756715820786,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,13 +6,13 @@\n import { useChatStore } from '@/store/chatStore';\n import { useAuthStore } from '@/store/authStore';\n import { combinePrompts } from '@/ai/promptTemplates';\n import { FileUploadData } from '@/firebase.functions';\n-import type { SimpleModel } from './useAI'; // ModelName을 SimpleModel로 변경\n+import type { SimpleModel } from './useAI';\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n-import { v4 as uuidv4 } from 'uuid'; // UUID 라이브러리 추가\n+import { v4 as uuidv4 } from 'uuid';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n@@ -89,9 +89,9 @@\n   return newUuid;\n };\n \n interface UseChatActionsProps {\n-  modelName: SimpleModel; // ModelName을 SimpleModel로 변경\n+  modelName: SimpleModel;\n   selectedPromptId: string;\n }\n \n export function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n@@ -109,38 +109,34 @@\n   const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n   const [isDragOver, setIsDragOver] = useState(false);\n   const [uploadProgress, setUploadProgress] = useState(0);\n   const [isUploading, setIsUploading] = useState(false);\n-  const [selectedFiles, setSelectedFiles] = useState<File[]>([]); // 실제 File 객체를 저장할 상태\n+  const [selectedFiles, setSelectedFiles] = useState<File[]>([]);\n \n-  // ⭐️ 수정: 파일을 즉시 업로드하지 않고 미리보기만 추가\n+  // 파일을 즉시 업로드하지 않고 미리보기만 추가\n   const handleFileUpload = (files: File[]) => {\n     if (files.length === 0) return;\n     \n-    // ⭐️ 파일 형식 검증\n     const invalidFiles = files.filter(file => !validateFileType(file));\n     if (invalidFiles.length > 0) {\n       error(`지원하지 않는 파일 형식입니다: ${invalidFiles.map(f => f.name).join(', ')}`);\n       return;\n     }\n     \n-    // ⭐️ 파일 크기 검증 (단일 파일 20MB, 복수 파일 합계 20MB)\n     const sizeValidation = validateFilesSize(files);\n     if (!sizeValidation.isValid) {\n       error(sizeValidation.errorMessage || '파일 크기 검증에 실패했습니다.');\n       return;\n     }\n     \n-    // ⭐️ 임시 URL로 미리보기 추가 (실제 업로드는 하지 않음)\n     const fileDataArray: FileUploadData[] = files.map(file => ({\n       name: file.name,\n-      fileUri: URL.createObjectURL(file), // 임시 URL\n+      fileUri: URL.createObjectURL(file),\n       mimeType: file.type,\n       size: file.size,\n     }));\n     \n     setUploadedFiles(prev => [...prev, ...fileDataArray]);\n-    // ⭐️ 실제 File 객체들도 저장\n     setSelectedFiles(prev => [...prev, ...files]);\n     success(`파일 ${files.length}개가 추가되었습니다.`);\n   };\n \n@@ -148,9 +144,8 @@\n     setUploadProgress(progress);\n   };\n \n   const removeFile = (fileUri: string) => {\n-    // fileUri로 파일을 찾아서 제거\n     const fileToRemove = uploadedFiles.find(file => file.fileUri === fileUri);\n     if (fileToRemove) {\n       setUploadedFiles((prev) => prev.filter((file) => file.fileUri !== fileUri));\n       setSelectedFiles((prev) => prev.filter((file) => file.name !== fileToRemove.name));\n@@ -200,23 +195,28 @@\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n     \n     let currentSessionId = chatSessionId;\n-    let userId; \n-    \n+    let userId: string | null = null; // userId 변수 초기화\n+\n+    if (isAuthenticated()) {\n+      const authStorage = localStorage.getItem('auth-storage');\n+      if (authStorage) {\n+        const authData = JSON.parse(authStorage);\n+        userId = authData.state?.user?._id;\n+      }\n+    } else {\n+      userId = getOrCreateGuestUuid();\n+    }\n+\n     if (!currentSessionId) {\n       try {\n         let createResponse;\n         \n-        // 인증 상태에 따라 다른 API 호출\n         if (isAuthenticated()) {\n-          // 회원인 경우 기존 API 사용\n-          createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅');\n+          createResponse = await createChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n         } else {\n-          // 비회원인 경우 비회원 전용 API 사용\n-          const guestUuid = getOrCreateGuestUuid();\n-          console.log('비회원 UUID:', guestUuid);\n-          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', guestUuid);\n+          createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n         }\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n           currentSessionId = createResponse.data._id;\n@@ -231,25 +231,22 @@\n       }\n     }\n \n     try {\n-      // ⭐️ 새로운 플로우: 파일이 있을 경우 먼저 업로드하고 파일명을 받음\n       let uploadedFileNames: string[] = [];\n       let filesForGemini: Array<{name: string, fileUri: string, mimeType: string, base64?: string}> = [];\n       \n       if (selectedFiles.length > 0) {\n         console.log('=== 파일 업로드 프로세스 시작 ===');\n         console.log('선택된 파일들:', selectedFiles.map(f => `${f.name} (${(f.size / 1024 / 1024).toFixed(2)}MB)`));\n         \n-        // 1. 파일을 먼저 업로드\n         const uploadResponse = await uploadFiles(selectedFiles);\n         console.log('파일 업로드 API 응답:', uploadResponse);\n         \n         if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n           uploadedFileNames = uploadResponse.data;\n           console.log('업로드된 파일명들:', uploadedFileNames);\n           \n-          // 2. 업로드된 파일명으로 URL 생성 및 base64 인코딩 준비\n           console.log('파일 URL 생성 및 base64 인코딩 시작...');\n           filesForGemini = await Promise.all(\n             uploadedFileNames.map(async (fileName, index) => {\n               const file = selectedFiles[index];\n@@ -257,97 +254,74 @@\n               const fileUrl = `/api/file/download/${fileName}`;\n               console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n               return {\n                 name: file.name,\n-                fileUri: fileUrl, // 서버 파일 URL\n+                fileUri: fileUrl,\n                 mimeType: file.type,\n-                base64: base64 // AI에 전송할 base64 데이터\n+                base64: base64\n               };\n             })\n           );\n           console.log('=== 파일 업로드 프로세스 완료 ===');\n           \n-          // ⭐️ 파일 업로드 완료 후 잠시 대기 (서버에서 파일 저장 완료 대기)\n           await new Promise(resolve => setTimeout(resolve, 1000));\n         } else {\n           throw new Error(uploadResponse?.error?.message || '파일 업로드에 실패했습니다.');\n         }\n       }\n \n-      // 3. 메시지와 파일 정보를 함께 전송\n       const messageContent = {\n         content: input,\n-        file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined // 첫 번째 파일만 사용\n+        file: uploadedFileNames.length > 0 ? uploadedFileNames[0] : undefined\n       };\n \n+      // 사용자 메시지 전송 API 호출 시 uid 추가\n       await sendChatMessage(currentSessionId, { \n         role: 'USER', \n-        content: messageContent\n+        content: messageContent,\n+        uid: userId\n       });\n       \n-      // 4. AI에 전송할 파일 정보 (base64 포함)\n       const filesForAI = filesForGemini.map(file => ({\n         name: file.name,\n         fileUri: file.fileUri,\n         mimeType: file.mimeType,\n-        base64: file.base64 // base64 데이터 포함\n+        base64: file.base64\n       }));\n \n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n       const reply = await sendChat(combinedPrompt, filesForAI);\n \n-      // ⭐️ 핵심 수정 부분: 여기서 견적서 데이터를 추출하고 PDF 함수를 호출합니다.\n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n         console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4(); // ⭐️ 고유한 UUID 생성\n+          const pdfUuid = uuidv4();\n           \n-          // 사용자 ID 가져오기 (회원 또는 비회원)\n-          let userId: string;\n-          if (isAuthenticated()) {\n-            // 회원인 경우: 로컬스토리지에서 사용자 ID 가져오기\n-            const authStorage = localStorage.getItem('auth-storage');\n-            if (authStorage) {\n-              const authData = JSON.parse(authStorage);\n-              userId = authData.state?.user?._id;\n-            }\n-          } else {\n-            // 비회원인 경우: guest-uuid 가져오기 (없으면 생성)\n-            userId = localStorage.getItem('guest-uuid') || uuidv4();\n-            if (!localStorage.getItem('guest-uuid')) {\n-              localStorage.setItem('guest-uuid', userId);\n-            }\n-          }\n-\n           if (!userId) {\n             throw new Error('사용자 ID를 가져올 수 없습니다.');\n           }\n \n-          // PDF 생성 및 업로드 함수 호출\n           const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n-          // ⭐️ 업로드 성공 시, 서버에서 반환된 filePath에서 UUID를 추출하고 견적서 데이터에 포함\n           if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n               const filePath = uploadResponse.data as string;\n               \n-              // ⭐️ filePath에서 UUID 추출 (예: \"heredot/ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7.pdf\" -> \"ca3f2dad-89f0-4e65-bc1f-bb2c5884f6d7\")\n               const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n               const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n               \n               if (extractedUuid) {\n-                  // ⭐️ AI 응답 메시지에 UUID 정보를 추가\n-                  // 견적서 JSON 데이터에 uuid 필드를 추가하되, 기존 구조는 그대로 유지\n                   const updatedEstimateData = {\n-                      ...estimateData, // 기존 견적서 데이터를 그대로 유지\n-                      uuid: extractedUuid, // UUID만 추가\n+                      ...estimateData,\n+                      uuid: extractedUuid,\n                   };\n                   const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n \n                   await sendChatMessage(currentSessionId, {\n                       role: 'AI',\n-                      content: { type: 'text', value: updatedReply }\n+                      content: { type: 'text', value: updatedReply },\n+                      uid: userId // AI 메시지에도 uid 추가\n                   });\n                   updateLastMessage(updatedReply);\n               } else {\n                   throw new Error('UUID를 추출할 수 없습니다.');\n@@ -360,26 +334,26 @@\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n           \n-          // PDF 업로드 실패 시 원래 응답 사용\n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply }\n+            content: { type: 'text', value: reply },\n+            uid: userId // AI 메시지에도 uid 추가\n           });\n           updateLastMessage(reply);\n         }\n       } else {\n-        // 견적서가 아닐 경우 기존 메시지 전송\n         await sendChatMessage(currentSessionId, {\n             role: 'AI',\n-            content: { type: 'text', value: reply }\n+            content: { type: 'text', value: reply },\n+            uid: userId // AI 메시지에도 uid 추가\n         });\n         updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n-      setSelectedFiles([]); // ⭐️ 선택된 파일들도 초기화\n+      setSelectedFiles([]);\n \n     } catch (e) {\n       error(`메시지 전송 실패: ${(e as Error).message}`);\n     } finally {\n"
                },
                {
                    "date": 1756715901319,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -303,9 +303,9 @@\n           }\n \n           const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse[0].data) {\n               const filePath = uploadResponse.data as string;\n               \n               const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n               const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n"
                },
                {
                    "date": 1756715934085,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -218,9 +218,9 @@\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n         }\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n-          currentSessionId = createResponse.data._id;\n+          currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n@@ -303,9 +303,9 @@\n           }\n \n           const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n \n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse[0].data) {\n+          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n               const filePath = uploadResponse.data as string;\n               \n               const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n               const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n"
                },
                {
                    "date": 1756715940185,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -217,9 +217,9 @@\n         } else {\n           createResponse = await createGuestChatSession(input.slice(0, 20) || '새로운 채팅', userId);\n         }\n         \n-        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data._id) {\n+        if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n           currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n"
                },
                {
                    "date": 1757200408258,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,8 +11,10 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid';\n++ import { ensureEstimateUuid, buildFullEstimateData, extractIntroFromReply } from '@/utils/estimate';\n++ import { uploadEstimatePdf } from '@/lib/api/user/userApi';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1757200415373,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -11,10 +11,10 @@\n import { createChatSession, createGuestChatSession, sendChatMessage, sendMessageWithFiles, validateFileType, validateFileSize, uploadFiles } from '@/lib/api/user/userApi';\n import { generateAndUploadPdf } from '@/hooks/pdfUtils';\n import type { ProjectEstimate } from '@/app/ai-estimate/types/projectEstimate';\n import { v4 as uuidv4 } from 'uuid';\n-+ import { ensureEstimateUuid, buildFullEstimateData, extractIntroFromReply } from '@/utils/estimate';\n-+ import { uploadEstimatePdf } from '@/lib/api/user/userApi';\n+import { ensureEstimateUuid, buildFullEstimateData, extractIntroFromReply } from '@/hooks/estimate';\n+import { uploadEstimatePdf } from '@/lib/api/user/userApi';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n   try {\n"
                },
                {
                    "date": 1757200439447,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -294,9 +294,9 @@\n       const reply = await sendChat(combinedPrompt, filesForAI);\n \n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. PDF 변환 및 업로드를 시작합니다.');\n+        +       console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           const pdfUuid = uuidv4();\n           \n"
                },
                {
                    "date": 1757200492740,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -294,46 +294,39 @@\n       const reply = await sendChat(combinedPrompt, filesForAI);\n \n       const estimateData = extractEstimateData(reply);\n       if (estimateData) {\n-        +       console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n+        console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n-          const pdfUuid = uuidv4();\n-          \n-          if (!userId) {\n-            throw new Error('사용자 ID를 가져올 수 없습니다.');\n-          }\n+          if (!userId) throw new Error('사용자 ID를 가져올 수 없습니다.');\n++         \n++         // 1) uuid 보장 (클라 생성)\n++         ensureEstimateUuid(estimateData);\n++         // 2) 인트로 추출 후 인트로 + JSON 스크립트로 data 구성\n++         const intro = extractIntroFromReply(reply);\n++         const dataStr = buildFullEstimateData(estimateData, intro);\n++         // 3) 서버 저장 (생성: id 미전달)\n++         const uploadResponse = await uploadEstimatePdf(\n++           currentSessionId,\n++           invoiceTitle,\n++           userId,\n++           dataStr\n++           // estimateId 생략 (생성)\n++         );\n++         if (uploadResponse?.statusCode !== 200) {\n++           throw new Error(uploadResponse?.error?.message || '견적 저장 실패');\n++         }\n++         // 4) 채팅 타임라인에도 uuid가 들어간 최신 JSON만 남기기\n++         const updatedReply =\n++           `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n++         await sendChatMessage(currentSessionId, {\n++           role: 'AI',\n++           content: { type: 'text', value: updatedReply },\n++           uid: userId\n++         });\n++         updateLastMessage(updatedReply);\n \n-          const uploadResponse = await generateAndUploadPdf(estimateData, currentSessionId, invoiceTitle, pdfUuid, userId, success, error);\n-\n-          if (uploadResponse && uploadResponse.statusCode === 200 && uploadResponse.data) {\n-              const filePath = uploadResponse.data as string;\n-              \n-              const uuidMatch = filePath.match(/([a-f0-9-]+)\\.pdf$/);\n-              const extractedUuid = uuidMatch ? uuidMatch[1] : null;\n-              \n-              if (extractedUuid) {\n-                  const updatedEstimateData = {\n-                      ...estimateData,\n-                      uuid: extractedUuid,\n-                  };\n-                  const updatedReply = `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(updatedEstimateData)}</script>`;\n-\n-                  await sendChatMessage(currentSessionId, {\n-                      role: 'AI',\n-                      content: { type: 'text', value: updatedReply },\n-                      uid: userId // AI 메시지에도 uid 추가\n-                  });\n-                  updateLastMessage(updatedReply);\n-              } else {\n-                  throw new Error('UUID를 추출할 수 없습니다.');\n-              }\n-\n-          } else {\n-              throw new Error(uploadResponse?.error?.message || 'PDF 업로드에 실패했습니다.');\n-          }\n-\n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n           \n"
                },
                {
                    "date": 1757200504715,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -298,34 +298,34 @@\n         console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           if (!userId) throw new Error('사용자 ID를 가져올 수 없습니다.');\n-+         \n-+         // 1) uuid 보장 (클라 생성)\n-+         ensureEstimateUuid(estimateData);\n-+         // 2) 인트로 추출 후 인트로 + JSON 스크립트로 data 구성\n-+         const intro = extractIntroFromReply(reply);\n-+         const dataStr = buildFullEstimateData(estimateData, intro);\n-+         // 3) 서버 저장 (생성: id 미전달)\n-+         const uploadResponse = await uploadEstimatePdf(\n-+           currentSessionId,\n-+           invoiceTitle,\n-+           userId,\n-+           dataStr\n-+           // estimateId 생략 (생성)\n-+         );\n-+         if (uploadResponse?.statusCode !== 200) {\n-+           throw new Error(uploadResponse?.error?.message || '견적 저장 실패');\n-+         }\n-+         // 4) 채팅 타임라인에도 uuid가 들어간 최신 JSON만 남기기\n-+         const updatedReply =\n-+           `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n-+         await sendChatMessage(currentSessionId, {\n-+           role: 'AI',\n-+           content: { type: 'text', value: updatedReply },\n-+           uid: userId\n-+         });\n-+         updateLastMessage(updatedReply);\n+         \n+         // 1) uuid 보장 (클라 생성)\n+         ensureEstimateUuid(estimateData);\n+         // 2) 인트로 추출 후 인트로 + JSON 스크립트로 data 구성\n+         const intro = extractIntroFromReply(reply);\n+         const dataStr = buildFullEstimateData(estimateData, intro);\n+         // 3) 서버 저장 (생성: id 미전달)\n+         const uploadResponse = await uploadEstimatePdf(\n+           currentSessionId,\n+           invoiceTitle,\n+           userId,\n+           dataStr\n+           // estimateId 생략 (생성)\n+         );\n+         if (uploadResponse?.statusCode !== 200) {\n+           throw new Error(uploadResponse?.error?.message || '견적 저장 실패');\n+         }\n+         // 4) 채팅 타임라인에도 uuid가 들어간 최신 JSON만 남기기\n+         const updatedReply =\n+           `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n+         await sendChatMessage(currentSessionId, {\n+           role: 'AI',\n+           content: { type: 'text', value: updatedReply },\n+           uid: userId\n+         });\n+         updateLastMessage(updatedReply);    \n \n         } catch (pdfError) {\n           console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n           error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n"
                },
                {
                    "date": 1757200517874,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -326,10 +326,10 @@\n          });\n          updateLastMessage(updatedReply);    \n \n         } catch (pdfError) {\n-          console.error('PDF 생성 또는 업로드 중 오류 발생:', pdfError);\n-          error(`견적서 업로드 실패: ${(pdfError as Error).message}`);\n+          console.error('견적 저장 중 오류 발생:', pdfError);\n++         error(`견적 저장 실패: ${(pdfError as Error).message}`);\n           \n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n             content: { type: 'text', value: reply },\n"
                },
                {
                    "date": 1757203112313,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -222,8 +222,11 @@\n         \n         if (createResponse && createResponse.statusCode === 200 && createResponse.data && createResponse.data.length > 0 && createResponse.data[0]._id) {\n           currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n+\n+          localStorage.setItem('chatSessionId', currentSessionId);\n+\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n@@ -327,9 +330,9 @@\n          updateLastMessage(updatedReply);    \n \n         } catch (pdfError) {\n           console.error('견적 저장 중 오류 발생:', pdfError);\n-+         error(`견적 저장 실패: ${(pdfError as Error).message}`);\n+         error(`견적 저장 실패: ${(pdfError as Error).message}`);\n           \n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n             content: { type: 'text', value: reply },\n"
                },
                {
                    "date": 1757203117605,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -224,9 +224,8 @@\n           currentSessionId = createResponse.data[0]._id;\n           setChatSessionId(currentSessionId);\n \n           localStorage.setItem('chatSessionId', currentSessionId);\n-\n         } else {\n           throw new Error(createResponse.error?.message || '채팅방 생성에 실패했습니다.');\n         }\n       } catch (e) {\n"
                },
                {
                    "date": 1757222800769,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -254,9 +254,9 @@\n           filesForGemini = await Promise.all(\n             uploadedFileNames.map(async (fileName, index) => {\n               const file = selectedFiles[index];\n               const base64 = await fileToBase64(file);\n-              const fileUrl = `/api/file/download/${fileName}`;\n+              const fileUrl = `/api/file/${fileName}`;\n               console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n               return {\n                 name: file.name,\n                 fileUri: fileUrl,\n"
                },
                {
                    "date": 1757258862150,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -254,9 +254,9 @@\n           filesForGemini = await Promise.all(\n             uploadedFileNames.map(async (fileName, index) => {\n               const file = selectedFiles[index];\n               const base64 = await fileToBase64(file);\n-              const fileUrl = `/api/file/${fileName}`;\n+              const fileUrl = `/api/file/download/${fileName}`;\n               console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n               return {\n                 name: file.name,\n                 fileUri: fileUrl,\n"
                },
                {
                    "date": 1757258883371,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -254,9 +254,9 @@\n           filesForGemini = await Promise.all(\n             uploadedFileNames.map(async (fileName, index) => {\n               const file = selectedFiles[index];\n               const base64 = await fileToBase64(file);\n-              const fileUrl = `/api/file/download/${fileName}`;\n+              const fileUrl = `/api/file/${fileName}`;\n               console.log(`파일 처리 완료: ${file.name} -> ${fileUrl} (base64 길이: ${base64.length})`);\n               return {\n                 name: file.name,\n                 fileUri: fileUrl,\n"
                },
                {
                    "date": 1757258988873,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,24 +16,24 @@\n import { uploadEstimatePdf } from '@/lib/api/user/userApi';\n \n // 견적서 데이터를 추출하는 유틸리티 함수\n const extractEstimateData = (content: string): ProjectEstimate | null => {\n+  // ✅ 문자열이 아니면 바로 종료\n+  if (typeof content !== 'string') return null;\n+\n   try {\n-    const match = content.match(/<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/);\n+    const match = content.match(\n+      /<script type=\"application\\/json\" id=\"invoiceData\">([\\s\\S]*?)<\\/script>/\n+    );\n     if (!match) return null;\n \n-    const jsonStr = match[1];\n-    const data = JSON.parse(jsonStr);\n-\n+    const data = JSON.parse(match[1]);\n     if (!data || typeof data !== 'object' || !Array.isArray(data.categories)) {\n-      console.error('Invalid estimate data structure:', data);\n       return null;\n     }\n-\n     return data as ProjectEstimate;\n-\n-  } catch (error) {\n-    console.error('Failed to parse estimate data:', error);\n+  } catch (err) {\n+    console.error('Failed to parse estimate data:', err);\n     return null;\n   }\n };\n \n"
                },
                {
                    "date": 1757284226160,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -318,35 +318,47 @@\n          if (uploadResponse?.statusCode !== 200) {\n            throw new Error(uploadResponse?.error?.message || '견적 저장 실패');\n          }\n          // 4) 채팅 타임라인에도 uuid가 들어간 최신 JSON만 남기기\n-         const updatedReply =\n-           `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n+         const updatedReply = intro + \n+           `\\n\\n<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n          await sendChatMessage(currentSessionId, {\n            role: 'AI',\n            content: { type: 'text', value: updatedReply },\n            uid: userId\n          });\n-         updateLastMessage(updatedReply);    \n+         updateLastMessage((prev) => ({\n+           ...prev,\n+           content: updatedReply,\n+           isLoading: false\n+         }));\n \n         } catch (pdfError) {\n           console.error('견적 저장 중 오류 발생:', pdfError);\n-         error(`견적 저장 실패: ${(pdfError as Error).message}`);\n+          error(`견적 저장 실패: ${(pdfError as Error).message}`);\n           \n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n             content: { type: 'text', value: reply },\n-            uid: userId // AI 메시지에도 uid 추가\n+            uid: userId\n           });\n-          updateLastMessage(reply);\n+          updateLastMessage((prev) => ({\n+            ...prev,\n+            content: reply,\n+            isLoading: false\n+          }));\n         }\n       } else {\n         await sendChatMessage(currentSessionId, {\n-            role: 'AI',\n-            content: { type: 'text', value: reply },\n-            uid: userId // AI 메시지에도 uid 추가\n+          role: 'AI',\n+          content: { type: 'text', value: reply },\n+          uid: userId\n         });\n-        updateLastMessage(reply);\n+        updateLastMessage((prev) => ({\n+          ...prev,\n+          content: reply,\n+          isLoading: false\n+        }));\n       }\n \n       setUploadedFiles([]);\n       setSelectedFiles([]);\n"
                },
                {
                    "date": 1757284409714,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -183,20 +183,28 @@\n     }\n   };\n \n   const handleSubmit = async (input: string) => {\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n+    console.log('🚀 handleSubmit 시작:', { input });\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) {\n+      console.log('❌ 입력이 없거나 처리 중:', { input, isProcessing });\n+      return;\n+    }\n \n     setIsProcessing(true);\n+    console.log('📝 처리 시작 - isProcessing:', true);\n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n${fileInfo}`;\n+      console.log('📎 파일 정보 추가:', { userMessageContent });\n     }\n     \n+    console.log('💬 메시지 추가 전 상태');\n     addMessage({ role: 'user', content: userMessageContent });\n     addMessage({ role: 'ai', content: '', isLoading: true });\n+    console.log('💬 메시지 추가 완료');\n     \n     let currentSessionId = chatSessionId;\n     let userId: string | null = null; // userId 변수 초기화\n \n@@ -291,14 +299,20 @@\n         mimeType: file.mimeType,\n         base64: file.base64\n       }));\n \n+      console.log('🤖 AI 응답 요청 시작');\n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n+      console.log('📤 전송할 프롬프트:', { combinedPrompt });\n+      \n       const reply = await sendChat(combinedPrompt, filesForAI);\n+      console.log('📥 AI 응답 수신:', { reply });\n \n       const estimateData = extractEstimateData(reply);\n+      console.log('🔍 견적서 데이터 추출 결과:', { hasEstimateData: !!estimateData });\n+      \n       if (estimateData) {\n-        console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n+        console.log('📋 견적서 JSON 감지 - 처리 시작', { estimateData });\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           if (!userId) throw new Error('사용자 ID를 가져올 수 없습니다.');\n          \n@@ -325,40 +339,61 @@\n            role: 'AI',\n            content: { type: 'text', value: updatedReply },\n            uid: userId\n          });\n-         updateLastMessage((prev) => ({\n-           ...prev,\n-           content: updatedReply,\n-           isLoading: false\n-         }));\n+         console.log('📝 메시지 업데이트 시작 (견적서):', { updatedReply });\n+         updateLastMessage((prev) => {\n+           console.log('이전 메시지 상태:', prev);\n+           const updated = {\n+             ...prev,\n+             content: updatedReply,\n+             isLoading: false\n+           };\n+           console.log('업데이트될 메시지 상태:', updated);\n+           return updated;\n+         });\n+         console.log('✅ 메시지 업데이트 완료 (견적서)');\n \n         } catch (pdfError) {\n-          console.error('견적 저장 중 오류 발생:', pdfError);\n+          console.error('❌ 견적 저장 중 오류 발생:', pdfError);\n           error(`견적 저장 실패: ${(pdfError as Error).message}`);\n           \n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n             content: { type: 'text', value: reply },\n             uid: userId\n           });\n-          updateLastMessage((prev) => ({\n-            ...prev,\n-            content: reply,\n-            isLoading: false\n-          }));\n+          console.log('📝 메시지 업데이트 시작 (에러):', { reply });\n+          updateLastMessage((prev) => {\n+            console.log('이전 메시지 상태 (에러):', prev);\n+            const updated = {\n+              ...prev,\n+              content: reply,\n+              isLoading: false\n+            };\n+            console.log('업데이트될 메시지 상태 (에러):', updated);\n+            return updated;\n+          });\n+          console.log('✅ 메시지 업데이트 완료 (에러)');\n         }\n       } else {\n         await sendChatMessage(currentSessionId, {\n           role: 'AI',\n           content: { type: 'text', value: reply },\n           uid: userId\n         });\n-        updateLastMessage((prev) => ({\n-          ...prev,\n-          content: reply,\n-          isLoading: false\n-        }));\n+        console.log('📝 메시지 업데이트 시작 (일반):', { reply });\n+        updateLastMessage((prev) => {\n+          console.log('이전 메시지 상태 (일반):', prev);\n+          const updated = {\n+            ...prev,\n+            content: reply,\n+            isLoading: false\n+          };\n+          console.log('업데이트될 메시지 상태 (일반):', updated);\n+          return updated;\n+        });\n+        console.log('✅ 메시지 업데이트 완료 (일반)');\n       }\n \n       setUploadedFiles([]);\n       setSelectedFiles([]);\n"
                },
                {
                    "date": 1757284513788,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -200,10 +200,19 @@\n       console.log('📎 파일 정보 추가:', { userMessageContent });\n     }\n     \n     console.log('💬 메시지 추가 전 상태');\n+    // 사용자 메시지 추가\n+    console.log('👤 사용자 메시지 추가:', { content: userMessageContent });\n     addMessage({ role: 'user', content: userMessageContent });\n-    addMessage({ role: 'ai', content: '', isLoading: true });\n+\n+    // AI 메시지 자리표시자 추가\n+    console.log('🤖 AI 메시지 자리표시자 추가');\n+    addMessage({ \n+      role: 'ai', \n+      content: '생각하는 중...', \n+      isLoading: true \n+    });\n     console.log('💬 메시지 추가 완료');\n     \n     let currentSessionId = chatSessionId;\n     let userId: string | null = null; // userId 변수 초기화\n@@ -340,18 +349,20 @@\n            content: { type: 'text', value: updatedReply },\n            uid: userId\n          });\n          console.log('📝 메시지 업데이트 시작 (견적서):', { updatedReply });\n+         console.log('🔄 AI 메시지 업데이트 (견적서) 시작');\n          updateLastMessage((prev) => {\n            console.log('이전 메시지 상태:', prev);\n            const updated = {\n              ...prev,\n              content: updatedReply,\n              isLoading: false\n            };\n-           console.log('업데이트될 메시지 상태:', updated);\n+           console.log('✅ 업데이트될 메시지:', updated);\n            return updated;\n          });\n+         console.log('🔄 AI 메시지 업데이트 (견적서) 완료');\n          console.log('✅ 메시지 업데이트 완료 (견적서)');\n \n         } catch (pdfError) {\n           console.error('❌ 견적 저장 중 오류 발생:', pdfError);\n"
                },
                {
                    "date": 1757284687138,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -183,37 +183,20 @@\n     }\n   };\n \n   const handleSubmit = async (input: string) => {\n-    console.log('🚀 handleSubmit 시작:', { input });\n-    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) {\n-      console.log('❌ 입력이 없거나 처리 중:', { input, isProcessing });\n-      return;\n-    }\n+    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n \n     setIsProcessing(true);\n-    console.log('📝 처리 시작 - isProcessing:', true);\n \n     let userMessageContent = input;\n     if (uploadedFiles.length > 0) {\n       const fileInfo = uploadedFiles.map((file) => `[첨부파일: ${file.name}]`).join('\\n');\n       userMessageContent = `${input}\\n\\n${fileInfo}`;\n-      console.log('📎 파일 정보 추가:', { userMessageContent });\n     }\n     \n-    console.log('💬 메시지 추가 전 상태');\n-    // 사용자 메시지 추가\n-    console.log('👤 사용자 메시지 추가:', { content: userMessageContent });\n     addMessage({ role: 'user', content: userMessageContent });\n-\n-    // AI 메시지 자리표시자 추가\n-    console.log('🤖 AI 메시지 자리표시자 추가');\n-    addMessage({ \n-      role: 'ai', \n-      content: '생각하는 중...', \n-      isLoading: true \n-    });\n-    console.log('💬 메시지 추가 완료');\n+    addMessage({ role: 'ai', content: '', isLoading: true });\n     \n     let currentSessionId = chatSessionId;\n     let userId: string | null = null; // userId 변수 초기화\n \n@@ -308,20 +291,14 @@\n         mimeType: file.mimeType,\n         base64: file.base64\n       }));\n \n-      console.log('🤖 AI 응답 요청 시작');\n       const combinedPrompt = combinePrompts(selectedPromptId, input);\n-      console.log('📤 전송할 프롬프트:', { combinedPrompt });\n-      \n       const reply = await sendChat(combinedPrompt, filesForAI);\n-      console.log('📥 AI 응답 수신:', { reply });\n \n       const estimateData = extractEstimateData(reply);\n-      console.log('🔍 견적서 데이터 추출 결과:', { hasEstimateData: !!estimateData });\n-      \n       if (estimateData) {\n-        console.log('📋 견적서 JSON 감지 - 처리 시작', { estimateData });\n+        console.log('견적서 JSON을 감지했습니다. uuid 보장 및 서버 저장을 진행합니다.');\n         try {\n           const invoiceTitle = estimateData.project_name || '새로운 견적서';\n           if (!userId) throw new Error('사용자 ID를 가져올 수 없습니다.');\n          \n@@ -341,70 +318,35 @@\n          if (uploadResponse?.statusCode !== 200) {\n            throw new Error(uploadResponse?.error?.message || '견적 저장 실패');\n          }\n          // 4) 채팅 타임라인에도 uuid가 들어간 최신 JSON만 남기기\n-         const updatedReply = intro + \n-           `\\n\\n<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n+         const updatedReply =\n+           `<script type=\"application/json\" id=\"invoiceData\">${JSON.stringify(estimateData)}</script>`;\n          await sendChatMessage(currentSessionId, {\n            role: 'AI',\n            content: { type: 'text', value: updatedReply },\n            uid: userId\n          });\n-         console.log('📝 메시지 업데이트 시작 (견적서):', { updatedReply });\n-         console.log('🔄 AI 메시지 업데이트 (견적서) 시작');\n-         updateLastMessage((prev) => {\n-           console.log('이전 메시지 상태:', prev);\n-           const updated = {\n-             ...prev,\n-             content: updatedReply,\n-             isLoading: false\n-           };\n-           console.log('✅ 업데이트될 메시지:', updated);\n-           return updated;\n-         });\n-         console.log('🔄 AI 메시지 업데이트 (견적서) 완료');\n-         console.log('✅ 메시지 업데이트 완료 (견적서)');\n+         updateLastMessage(updatedReply);    \n \n         } catch (pdfError) {\n-          console.error('❌ 견적 저장 중 오류 발생:', pdfError);\n-          error(`견적 저장 실패: ${(pdfError as Error).message}`);\n+          console.error('견적 저장 중 오류 발생:', pdfError);\n+         error(`견적 저장 실패: ${(pdfError as Error).message}`);\n           \n           await sendChatMessage(currentSessionId, {\n             role: 'AI',\n             content: { type: 'text', value: reply },\n-            uid: userId\n+            uid: userId // AI 메시지에도 uid 추가\n           });\n-          console.log('📝 메시지 업데이트 시작 (에러):', { reply });\n-          updateLastMessage((prev) => {\n-            console.log('이전 메시지 상태 (에러):', prev);\n-            const updated = {\n-              ...prev,\n-              content: reply,\n-              isLoading: false\n-            };\n-            console.log('업데이트될 메시지 상태 (에러):', updated);\n-            return updated;\n-          });\n-          console.log('✅ 메시지 업데이트 완료 (에러)');\n+          updateLastMessage(reply);\n         }\n       } else {\n         await sendChatMessage(currentSessionId, {\n-          role: 'AI',\n-          content: { type: 'text', value: reply },\n-          uid: userId\n+            role: 'AI',\n+            content: { type: 'text', value: reply },\n+            uid: userId // AI 메시지에도 uid 추가\n         });\n-        console.log('📝 메시지 업데이트 시작 (일반):', { reply });\n-        updateLastMessage((prev) => {\n-          console.log('이전 메시지 상태 (일반):', prev);\n-          const updated = {\n-            ...prev,\n-            content: reply,\n-            isLoading: false\n-          };\n-          console.log('업데이트될 메시지 상태 (일반):', updated);\n-          return updated;\n-        });\n-        console.log('✅ 메시지 업데이트 완료 (일반)');\n+        updateLastMessage(reply);\n       }\n \n       setUploadedFiles([]);\n       setSelectedFiles([]);\n"
                }
            ],
            "date": 1756365557553,
            "name": "Commit-0",
            "content": "// src/hooks/useChatActions.ts\n\nimport { useState } from 'react';\nimport useAI from './useAI'; // 기존 useAI 훅 경로\nimport { useToast } from '@/components/common/ToastProvider';\nimport { useChatStore } from '@/store/chatStore';\nimport { combinePrompts } from '@/ai/promptTemplates';\nimport { uploadFiles, FileUploadData } from '@/firebase.functions';\nimport type { ModelName } from '@/app/ai-estimate/types'; // ModelName 타입 경로\n\ninterface UseChatActionsProps {\n  modelName: ModelName;\n  selectedPromptId: string;\n}\n\nexport function useChatActions({ modelName, selectedPromptId }: UseChatActionsProps) {\n  const { sendChat } = useAI(modelName);\n  const { success, error } = useToast();\n  const { addMessage, updateLastMessage } = useChatStore((s) => ({\n    addMessage: s.addMessage,\n    updateLastMessage: s.updateLastMessage,\n  }));\n\n  const [isProcessing, setIsProcessing] = useState(false);\n  const [uploadedFiles, setUploadedFiles] = useState<FileUploadData[]>([]);\n  const [isDragOver, setIsDragOver] = useState(false);\n  const [uploadProgress, setUploadProgress] = useState(0);\n  const [isUploading, setIsUploading] = useState(false);\n\n  // 파일 업로드 관련 함수\n  const handleFileUpload = (fileData: FileUploadData) => {\n    setUploadedFiles((prev) => [...prev, fileData]);\n    setUploadProgress(0);\n    setIsUploading(false);\n    success(`파일 \"${fileData.name}\"이 업로드되었습니다.`);\n  };\n\n  const handleFileUploadProgress = (progress: number) => {\n    setUploadProgress(progress);\n  };\n\n  const removeFile = (fileName: string) => {\n    setUploadedFiles((prev) => prev.filter((file) => file.name !== fileName));\n  };\n\n  const handleDragOver = (e: React.DragEvent) => {\n    e.preventDefault();\n    if (!isUploading && !isProcessing) {\n      setIsDragOver(true);\n    }\n  };\n\n  const handleDragLeave = (e: React.DragEvent) => {\n    e.preventDefault();\n    setIsDragOver(false);\n  };\n\n  const handleDrop = (e: React.DragEvent) => {\n    e.preventDefault();\n    setIsDragOver(false);\n\n    const files = Array.from(e.dataTransfer.files);\n    if (files.length > 0) {\n      setIsUploading(true);\n      uploadFiles(files, {\n        onUpload: handleFileUpload,\n        progress: handleFileUploadProgress,\n      });\n    }\n  };\n\n  const handleFileInput = (e: React.ChangeEvent<HTMLInputElement>) => {\n    const files = Array.from(e.target.files || []);\n    if (files.length > 0) {\n      setIsUploading(true);\n      uploadFiles(files, {\n        onUpload: handleFileUpload,\n        progress: handleFileUploadProgress,\n      });\n    }\n  };\n\n  // 핵심 로직: handleSubmit\n  const handleSubmit = async (input: string) => {\n    if ((!input.trim() && uploadedFiles.length === 0) || isProcessing) return;\n\n    setIsProcessing(true);\n\n    let userMessageContent = input;\n    if (uploadedFiles.length > 0) {\n      const fileInfo = uploadedFiles.map((file) => `[파일: ${file.name}]`).join('\\n');\n      userMessageContent = `${input}\\n\\n첨부된 파일:\\n${fileInfo}`;\n    }\n    addMessage({ role: 'user', content: userMessageContent });\n    addMessage({ role: 'ai', content: '', isLoading: true });\n\n    try {\n      const filesForGemini = uploadedFiles.map((file) => ({\n        name: file.name,\n        fileUri: file.fileUri,\n        mimeType: file.mimeType,\n      }));\n\n      const combinedPrompt = combinePrompts(selectedPromptId, input);\n      const reply = await sendChat(combinedPrompt, filesForGemini);\n\n      updateLastMessage(reply);\n      setUploadedFiles([]);\n    } finally {\n      setIsProcessing(false);\n    }\n  };\n\n  return {\n    handleSubmit,\n    isProcessing,\n    uploadedFiles,\n    isDragOver,\n    uploadProgress,\n    isUploading,\n    handleDragOver,\n    handleDragLeave,\n    handleDrop,\n    handleFileInput,\n    removeFile,\n  };\n}"
        }
    ]
}