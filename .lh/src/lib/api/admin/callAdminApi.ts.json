{
    "sourceFile": "src/lib/api/admin/callAdminApi.ts",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 11,
            "patches": [
                {
                    "date": 1756963745125,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1757039048387,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,9 +30,10 @@\n   // 공통 헤더 설정\n   const headers = {\n     'Content-Type': 'application/json',\n     'Accept': 'application/json',\n-    'x-company-code': companyCode\n+    'x-company-code': companyCode,\n+    cre\n   };\n \n   let raw: any;\n \n"
                },
                {
                    "date": 1757377832011,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -4,21 +4,25 @@\n import { callApiPatch } from '@/lib/methods/callApiPatch';\n import { callApiDelete } from '@/lib/methods/callApiDelete';\n import { callNullCheck } from '@/lib/utils/nullChecker';\n import { triggerAdminLogout } from '@/contexts/AdminAuthContext';\n+import { devLog } from '@/lib/utils/devLogger';\n+import { de } from 'date-fns/locale';\n \n export async function callAdminApi<T = unknown>({\n   title,\n   url,\n   body,\n   method = 'POST',\n   isCallPageLoader = false,\n+  isWithToken = false,\n }: {\n   title: string;\n   url: string;\n   body?: Record<string, unknown>;\n   method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';\n   isCallPageLoader?: boolean;\n+  isWithToken?: boolean;\n }): Promise<T[]> {\n   // Company Code 설정\n   let companyCode = 'heredot';  // 기본값\n   const pathParts = window.location.pathname.split('/');\n@@ -26,58 +30,93 @@\n   if (companyCodeIndex > 0 && pathParts.length > companyCodeIndex) {\n     companyCode = pathParts[companyCodeIndex];\n   }\n \n+  devLog('url:', url);  \n+\n+  \n+\n   // 공통 헤더 설정\n-  const headers = {\n+  const headers: Record<string, string> = {\n     'Content-Type': 'application/json',\n     'Accept': 'application/json',\n     'x-company-code': companyCode,\n-    cre\n   };\n \n+  // 로컬/HTTP 환경에서 토큰이 필요한 경우 localStorage에서 가져오기\n+  if (isWithToken && (import.meta.env.VITE_ENV_NAME === 'dev' || window.location.protocol === 'http:')) {\n+    const adminToken = localStorage.getItem('adminToken');\n+    console.log('🔑 [토큰 체크]', {\n+      isWithToken,\n+      envName: import.meta.env.VITE_ENV_NAME,\n+      protocol: window.location.protocol,\n+      adminToken: adminToken ? 'exists' : 'not found'\n+    });\n+    devLog('🔑 [토큰 체크]', {\n+      isWithToken,\n+      envName: import.meta.env.VITE_ENV_NAME,\n+      protocol: window.location.protocol,\n+      adminToken: adminToken ? 'exists' : 'not found'\n+    });\n+    \n+    if (adminToken) {\n+      // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n+      headers['admin_token'] = adminToken;\n+      console.log('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n+      devLog('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n+    } else {\n+      devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n+    }\n+  }\n+\n   let raw: any;\n \n   // 'method' 값에 따라 다른 API 호출 함수를 사용\n-  if (method === 'GET') {\n-    raw = await callApiGet({\n-      title,\n-      url,\n-      isCallPageLoader,\n-      headers,\n-    });\n-  } else if (method === 'PUT') {\n-    raw = await callApiPut({\n-      title,\n-      url,\n-      body,\n-      isCallPageLoader,\n-      headers,\n-    });\n-  } else if (method === 'PATCH') {\n-    raw = await callApiPatch({\n-      title,\n-      url,\n-      body,\n-      isCallPageLoader,\n-      headers,\n-    });\n-  } else if (method === 'DELETE') {\n-    raw = await callApiDelete({\n-      title,\n-      url,\n-      isCallPageLoader,\n-      headers,\n-    });\n-  } else {\n-    // 기본적으로 POST를 사용\n-    raw = await callApiPost({\n-      title,\n-      url,\n-      body,\n-      isCallPageLoader,\n-      headers,\n-    });\n+  try {\n+    if (method === 'GET') {\n+      raw = await callApiGet({\n+        title,\n+        url,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'PUT') {\n+      raw = await callApiPut({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'PATCH') {\n+      raw = await callApiPatch({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'DELETE') {\n+      raw = await callApiDelete({\n+        title,\n+        url,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else {\n+      // 기본적으로 POST를 사용\n+      raw = await callApiPost({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    }\n+  } catch (error) {\n+    console.error('❌ [API 요청 에러]', error);\n+    devLog('❌ [API 요청 에러]', error);\n+    throw error;\n   }\n \n   // API 응답을 배열로 변환\n   const responseArray = Array.isArray(raw) ? raw : [raw];\n"
                },
                {
                    "date": 1757401824903,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -44,15 +44,20 @@\n \n   // 로컬/HTTP 환경에서 토큰이 필요한 경우 localStorage에서 가져오기\n   if (isWithToken && (import.meta.env.VITE_ENV_NAME === 'dev' || window.location.protocol === 'http:')) {\n     const adminToken = localStorage.getItem('adminToken');\n-    console.log('🔑 [토큰 체크]', {\n+    console.log('🔑 [callAdminApi 토큰 체크]', {\n+      title,\n+      url,\n       isWithToken,\n       envName: import.meta.env.VITE_ENV_NAME,\n       protocol: window.location.protocol,\n-      adminToken: adminToken ? 'exists' : 'not found'\n+      adminToken: adminToken ? `exists (${adminToken.substring(0, 10)}...)` : 'not found',\n+      localStorageAvailable: typeof localStorage !== 'undefined'\n     });\n-    devLog('🔑 [토큰 체크]', {\n+    devLog('🔑 [callAdminApi 토큰 체크]', {\n+      title,\n+      url,\n       isWithToken,\n       envName: import.meta.env.VITE_ENV_NAME,\n       protocol: window.location.protocol,\n       adminToken: adminToken ? 'exists' : 'not found'\n@@ -60,11 +65,12 @@\n     \n     if (adminToken) {\n       // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n       headers['admin_token'] = adminToken;\n-      console.log('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n+      console.log('🔑 [토큰 추가됨]', { 'admin_token': `${adminToken.substring(0, 10)}...` });\n       devLog('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n     } else {\n+      console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n       devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n     }\n   }\n \n"
                },
                {
                    "date": 1757403385190,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,141 @@\n+import { callApiPost } from '@/lib/methods/callApiPost';\n+import { callApiGet } from '@/lib/methods/callApiGet';\n+import { callApiPut } from '@/lib/methods/callApiPut';\n+import { callApiPatch } from '@/lib/methods/callApiPatch';\n+import { callApiDelete } from '@/lib/methods/callApiDelete';\n+import { callNullCheck } from '@/lib/utils/nullChecker';\n+import { triggerAdminLogout } from '@/contexts/AdminAuthContext';\n+import { devLog } from '@/lib/utils/devLogger';\n+import { de } from 'date-fns/locale';\n+\n+export async function callAdminApi<T = unknown>({\n+  title,\n+  url,\n+  body,\n+  method = 'POST',\n+  isCallPageLoader = false,\n+  isWithToken = false,\n+}: {\n+  title: string;\n+  url: string;\n+  body?: Record<string, unknown>;\n+  method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';\n+  isCallPageLoader?: boolean;\n+  isWithToken?: boolean;\n+}): Promise<T[]> {\n+  // Company Code 설정\n+  let companyCode = 'heredot';  // 기본값\n+  const pathParts = window.location.pathname.split('/');\n+  const companyCodeIndex = pathParts.indexOf('aiclient') + 1;\n+  if (companyCodeIndex > 0 && pathParts.length > companyCodeIndex) {\n+    companyCode = pathParts[companyCodeIndex];\n+  }\n+\n+  devLog('url:', url);  \n+\n+  \n+\n+  // 공통 헤더 설정\n+  const headers: Record<string, string> = {\n+    'Content-Type': 'application/json',\n+    'Accept': 'application/json',\n+    'x-company-code': companyCode,\n+  };\n+\n+  // 로컬/HTTP 환경에서 토큰이 필요한 경우 localStorage에서 가져오기\n+  if (isWithToken && (import.meta.env.VITE_ENV_NAME === 'dev' || window.location.protocol === 'http:')) {\n+    const adminToken = localStorage.getItem('adminToken');\n+    console.log('🔑 [callAdminApi 토큰 체크]', {\n+      title,\n+      url,\n+      isWithToken,\n+      envName: import.meta.env.VITE_ENV_NAME,\n+      protocol: window.location.protocol,\n+      adminToken: adminToken ? `exists (${adminToken.substring(0, 10)}...)` : 'not found',\n+      localStorageAvailable: typeof localStorage !== 'undefined'\n+    });\n+    devLog('🔑 [callAdminApi 토큰 체크]', {\n+      title,\n+      url,\n+      isWithToken,\n+      envName: import.meta.env.VITE_ENV_NAME,\n+      protocol: window.location.protocol,\n+      adminToken: adminToken ? 'exists' : 'not found'\n+    });\n+    \n+    if (adminToken) {\n+      // Authorization Bearer 토큰으로 변경\n+      headers['Authorization'] = `Bearer ${adminToken}`;\n+      console.log('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken.substring(0, 10)}...` });\n+      devLog('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken}` });\n+    } else {\n+      console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n+      devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n+    }\n+  }\n+\n+  let raw: any;\n+\n+  // 'method' 값에 따라 다른 API 호출 함수를 사용\n+  try {\n+    if (method === 'GET') {\n+      raw = await callApiGet({\n+        title,\n+        url,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'PUT') {\n+      raw = await callApiPut({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'PATCH') {\n+      raw = await callApiPatch({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else if (method === 'DELETE') {\n+      raw = await callApiDelete({\n+        title,\n+        url,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    } else {\n+      // 기본적으로 POST를 사용\n+      raw = await callApiPost({\n+        title,\n+        url,\n+        body,\n+        isCallPageLoader,\n+        headers,\n+      });\n+    }\n+  } catch (error) {\n+    console.error('❌ [API 요청 에러]', error);\n+    devLog('❌ [API 요청 에러]', error);\n+    throw error;\n+  }\n+\n+  // API 응답을 배열로 변환\n+  const responseArray = Array.isArray(raw) ? raw : [raw];\n+\n+  // 401 에러 감지 (쿠키 인증에서도 유지)\n+  if (\n+    responseArray[0]?.statusCode === 401 &&\n+    (responseArray[0]?.message === 'token is invalid' || responseArray[0]?.message === 'Unauthorized')\n+  ) {\n+    triggerAdminLogout();\n+    return [];\n+  }\n+\n+  // 응답이 배열이 아닌 경우 배열로 변환하여 처리\n+  return callNullCheck(responseArray);\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1757404357818,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -137,146 +137,5 @@\n   }\n \n   // 응답이 배열이 아닌 경우 배열로 변환하여 처리\n   return callNullCheck(responseArray);\n-}\n-import { callApiPost } from '@/lib/methods/callApiPost';\n-import { callApiGet } from '@/lib/methods/callApiGet';\n-import { callApiPut } from '@/lib/methods/callApiPut';\n-import { callApiPatch } from '@/lib/methods/callApiPatch';\n-import { callApiDelete } from '@/lib/methods/callApiDelete';\n-import { callNullCheck } from '@/lib/utils/nullChecker';\n-import { triggerAdminLogout } from '@/contexts/AdminAuthContext';\n-import { devLog } from '@/lib/utils/devLogger';\n-import { de } from 'date-fns/locale';\n-\n-export async function callAdminApi<T = unknown>({\n-  title,\n-  url,\n-  body,\n-  method = 'POST',\n-  isCallPageLoader = false,\n-  isWithToken = false,\n-}: {\n-  title: string;\n-  url: string;\n-  body?: Record<string, unknown>;\n-  method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';\n-  isCallPageLoader?: boolean;\n-  isWithToken?: boolean;\n-}): Promise<T[]> {\n-  // Company Code 설정\n-  let companyCode = 'heredot';  // 기본값\n-  const pathParts = window.location.pathname.split('/');\n-  const companyCodeIndex = pathParts.indexOf('aiclient') + 1;\n-  if (companyCodeIndex > 0 && pathParts.length > companyCodeIndex) {\n-    companyCode = pathParts[companyCodeIndex];\n-  }\n-\n-  devLog('url:', url);  \n-\n-  \n-\n-  // 공통 헤더 설정\n-  const headers: Record<string, string> = {\n-    'Content-Type': 'application/json',\n-    'Accept': 'application/json',\n-    'x-company-code': companyCode,\n-  };\n-\n-  // 로컬/HTTP 환경에서 토큰이 필요한 경우 localStorage에서 가져오기\n-  if (isWithToken && (import.meta.env.VITE_ENV_NAME === 'dev' || window.location.protocol === 'http:')) {\n-    const adminToken = localStorage.getItem('adminToken');\n-    console.log('🔑 [callAdminApi 토큰 체크]', {\n-      title,\n-      url,\n-      isWithToken,\n-      envName: import.meta.env.VITE_ENV_NAME,\n-      protocol: window.location.protocol,\n-      adminToken: adminToken ? `exists (${adminToken.substring(0, 10)}...)` : 'not found',\n-      localStorageAvailable: typeof localStorage !== 'undefined'\n-    });\n-    devLog('🔑 [callAdminApi 토큰 체크]', {\n-      title,\n-      url,\n-      isWithToken,\n-      envName: import.meta.env.VITE_ENV_NAME,\n-      protocol: window.location.protocol,\n-      adminToken: adminToken ? 'exists' : 'not found'\n-    });\n-    \n-    if (adminToken) {\n-      // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n-      headers['admin_token'] = adminToken;\n-      console.log('🔑 [토큰 추가됨]', { 'admin_token': `${adminToken.substring(0, 10)}...` });\n-      devLog('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n-    } else {\n-      console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n-      devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n-    }\n-  }\n-\n-  let raw: any;\n-\n-  // 'method' 값에 따라 다른 API 호출 함수를 사용\n-  try {\n-    if (method === 'GET') {\n-      raw = await callApiGet({\n-        title,\n-        url,\n-        isCallPageLoader,\n-        headers,\n-      });\n-    } else if (method === 'PUT') {\n-      raw = await callApiPut({\n-        title,\n-        url,\n-        body,\n-        isCallPageLoader,\n-        headers,\n-      });\n-    } else if (method === 'PATCH') {\n-      raw = await callApiPatch({\n-        title,\n-        url,\n-        body,\n-        isCallPageLoader,\n-        headers,\n-      });\n-    } else if (method === 'DELETE') {\n-      raw = await callApiDelete({\n-        title,\n-        url,\n-        isCallPageLoader,\n-        headers,\n-      });\n-    } else {\n-      // 기본적으로 POST를 사용\n-      raw = await callApiPost({\n-        title,\n-        url,\n-        body,\n-        isCallPageLoader,\n-        headers,\n-      });\n-    }\n-  } catch (error) {\n-    console.error('❌ [API 요청 에러]', error);\n-    devLog('❌ [API 요청 에러]', error);\n-    throw error;\n-  }\n-\n-  // API 응답을 배열로 변환\n-  const responseArray = Array.isArray(raw) ? raw : [raw];\n-\n-  // 401 에러 감지 (쿠키 인증에서도 유지)\n-  if (\n-    responseArray[0]?.statusCode === 401 &&\n-    (responseArray[0]?.message === 'token is invalid' || responseArray[0]?.message === 'Unauthorized')\n-  ) {\n-    triggerAdminLogout();\n-    return [];\n-  }\n-\n-  // 응답이 배열이 아닌 경우 배열로 변환하여 처리\n-  return callNullCheck(responseArray);\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1757404430988,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,12 +63,12 @@\n       adminToken: adminToken ? 'exists' : 'not found'\n     });\n     \n     if (adminToken) {\n-      // Authorization Bearer 토큰으로 변경\n-      headers['Authorization'] = `Bearer ${adminToken}`;\n-      console.log('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken.substring(0, 10)}...` });\n-      devLog('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken}` });\n+      // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n+      headers['admin_token'] = adminToken;\n+      console.log('🔑 [토큰 추가됨]', { 'admin_token': `${adminToken.substring(0, 10)}...` });\n+      devLog('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n     } else {\n       console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n       devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n     }\n"
                },
                {
                    "date": 1757404510503,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,12 +63,12 @@\n       adminToken: adminToken ? 'exists' : 'not found'\n     });\n     \n     if (adminToken) {\n-      // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n-      headers['admin_token'] = adminToken;\n-      console.log('🔑 [토큰 추가됨]', { 'admin_token': `${adminToken.substring(0, 10)}...` });\n-      devLog('🔑 [토큰 추가됨]', { 'admin_token': adminToken });\n+      // Authorization Bearer 토큰으로 변경\n+      headers['Authorization'] = `Bearer ${adminToken}`;\n+      console.log('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken.substring(0, 10)}...` });\n+      devLog('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken}` });\n     } else {\n       console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n       devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n     }\n"
                },
                {
                    "date": 1757405844659,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -43,17 +43,16 @@\n   };\n \n   // 로컬/HTTP 환경에서 토큰이 필요한 경우 localStorage에서 가져오기\n   if (isWithToken && (import.meta.env.VITE_ENV_NAME === 'dev' || window.location.protocol === 'http:')) {\n-    const adminToken = localStorage.getItem('adminToken');\n+    const adminToken = localStorage.getItem('admin_access_token');\n     console.log('🔑 [callAdminApi 토큰 체크]', {\n       title,\n       url,\n       isWithToken,\n       envName: import.meta.env.VITE_ENV_NAME,\n       protocol: window.location.protocol,\n-      adminToken: adminToken ? `exists (${adminToken.substring(0, 10)}...)` : 'not found',\n-      localStorageAvailable: typeof localStorage !== 'undefined'\n+      adminToken: adminToken ? 'exists' : 'not found'\n     });\n     devLog('🔑 [callAdminApi 토큰 체크]', {\n       title,\n       url,\n@@ -63,15 +62,14 @@\n       adminToken: adminToken ? 'exists' : 'not found'\n     });\n     \n     if (adminToken) {\n-      // Authorization Bearer 토큰으로 변경\n-      headers['Authorization'] = `Bearer ${adminToken}`;\n-      console.log('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken.substring(0, 10)}...` });\n-      devLog('🔑 [Bearer 토큰 추가됨]', { 'Authorization': `Bearer ${adminToken}` });\n+      // 서버가 기대하는 헤더명 사용 (admin_token으로 수정)\n+      headers['admin_token'] = adminToken;\n+      console.log('🔑 [admin_token 토큰 추가됨]', { admin_token: adminToken });\n+      devLog('🔑 [admin_token 토큰 추가됨]', { admin_token: adminToken });\n     } else {\n-      console.log('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n-      devLog('⚠️ [토큰 없음] localStorage에 adminToken이 없습니다');\n+      devLog('⚠️ [토큰 없음] localStorage에 admin_access_token이 없습니다');\n     }\n   }\n \n   let raw: any;\n"
                },
                {
                    "date": 1757494019866,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,8 +31,9 @@\n     companyCode = pathParts[companyCodeIndex];\n   }\n \n   devLog('url:', url);  \n+  devLog('body:', body);  \n \n   \n \n   // 공통 헤더 설정\n"
                },
                {
                    "date": 1757494350273,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,9 +31,9 @@\n     companyCode = pathParts[companyCodeIndex];\n   }\n \n   devLog('url:', url);  \n-  devLog('body:', body);  \n+devLog('body:', JSON.stringify(body, null, 2));\n \n   \n \n   // 공통 헤더 설정\n"
                },
                {
                    "date": 1757494418884,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -30,9 +30,9 @@\n   if (companyCodeIndex > 0 && pathParts.length > companyCodeIndex) {\n     companyCode = pathParts[companyCodeIndex];\n   }\n \n-  devLog('url:', url);  \n+devLog('url:', url);  \n devLog('body:', JSON.stringify(body, null, 2));\n \n   \n \n"
                }
            ],
            "date": 1756963745125,
            "name": "Commit-0",
            "content": "import { callApiPost } from '@/lib/methods/callApiPost';\nimport { callApiGet } from '@/lib/methods/callApiGet';\nimport { callApiPut } from '@/lib/methods/callApiPut';\nimport { callApiPatch } from '@/lib/methods/callApiPatch';\nimport { callApiDelete } from '@/lib/methods/callApiDelete';\nimport { callNullCheck } from '@/lib/utils/nullChecker';\nimport { triggerAdminLogout } from '@/contexts/AdminAuthContext';\n\nexport async function callAdminApi<T = unknown>({\n  title,\n  url,\n  body,\n  method = 'POST',\n  isCallPageLoader = false,\n}: {\n  title: string;\n  url: string;\n  body?: Record<string, unknown>;\n  method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';\n  isCallPageLoader?: boolean;\n}): Promise<T[]> {\n  // Company Code 설정\n  let companyCode = 'heredot';  // 기본값\n  const pathParts = window.location.pathname.split('/');\n  const companyCodeIndex = pathParts.indexOf('aiclient') + 1;\n  if (companyCodeIndex > 0 && pathParts.length > companyCodeIndex) {\n    companyCode = pathParts[companyCodeIndex];\n  }\n\n  // 공통 헤더 설정\n  const headers = {\n    'Content-Type': 'application/json',\n    'Accept': 'application/json',\n    'x-company-code': companyCode\n  };\n\n  let raw: any;\n\n  // 'method' 값에 따라 다른 API 호출 함수를 사용\n  if (method === 'GET') {\n    raw = await callApiGet({\n      title,\n      url,\n      isCallPageLoader,\n      headers,\n    });\n  } else if (method === 'PUT') {\n    raw = await callApiPut({\n      title,\n      url,\n      body,\n      isCallPageLoader,\n      headers,\n    });\n  } else if (method === 'PATCH') {\n    raw = await callApiPatch({\n      title,\n      url,\n      body,\n      isCallPageLoader,\n      headers,\n    });\n  } else if (method === 'DELETE') {\n    raw = await callApiDelete({\n      title,\n      url,\n      isCallPageLoader,\n      headers,\n    });\n  } else {\n    // 기본적으로 POST를 사용\n    raw = await callApiPost({\n      title,\n      url,\n      body,\n      isCallPageLoader,\n      headers,\n    });\n  }\n\n  // API 응답을 배열로 변환\n  const responseArray = Array.isArray(raw) ? raw : [raw];\n\n  // 401 에러 감지 (쿠키 인증에서도 유지)\n  if (\n    responseArray[0]?.statusCode === 401 &&\n    (responseArray[0]?.message === 'token is invalid' || responseArray[0]?.message === 'Unauthorized')\n  ) {\n    triggerAdminLogout();\n    return [];\n  }\n\n  // 응답이 배열이 아닌 경우 배열로 변환하여 처리\n  return callNullCheck(responseArray);\n}"
        }
    ]
}