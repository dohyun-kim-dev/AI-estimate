{
    "sourceFile": "src/lib/api.ts",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 3,
            "patches": [
                {
                    "date": 1756186422183,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1756186555384,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,9 +1,17 @@\n import { pageLoaderController } from '@/contexts/PageLoaderContext'\n import { devLog, devWarn } from '../utils/devLogger'\n \n-const API_HOST = import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535/api'\n+// API 호스트 설정\n+const API_HOST = import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535'\n \n+// API 경로 생성 함수\n+const createApiUrl = (endpoint: string) => {\n+  // endpoint가 이미 /api로 시작하면 그대로 사용, 아니면 /api 추가\n+  const apiPath = endpoint.startsWith('/api') ? endpoint : `/api${endpoint}`\n+  return `${API_HOST}${apiPath}`\n+}\n+\n interface CallApiPostParams {\n   title: string\n   endpoint: string // url 대신 endpoint를 받도록 변경\n   body?: Record<string, unknown>\n@@ -15,17 +23,20 @@\n   endpoint,\n   body = {},\n   isCallPageLoader = false,\n }: CallApiPostParams): Promise<T> {\n-  const url = `${API_HOST}${endpoint}`\n+  const url = createApiUrl(endpoint)\n   devLog(`📱 [${title}]`, url, body)\n   if (isCallPageLoader) pageLoaderController.open()\n \n   let returnValue = ''\n \n   try {\n     const headers: Record<string, string> = {\n       'Content-Type': 'application/json',\n+      'Access-Control-Allow-Origin': '*',\n+      'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',\n+      'Access-Control-Allow-Headers': 'Content-Type, Authorization'\n     }\n \n     // 쿠키 인증으로 변경 - 액세스 토큰 제거\n     // if (accessToken) {\n"
                },
                {
                    "date": 1756186620612,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import { pageLoaderController } from '@/contexts/PageLoaderContext'\n import { devLog, devWarn } from '../utils/devLogger'\n \n-// API 호스트 설정\n-const API_HOST = import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535'\n+// API 호스트 설정 (끝에 슬래시 제거)\n+const API_HOST = (import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535').replace(/\\/$/, '')\n \n // API 경로 생성 함수\n const createApiUrl = (endpoint: string) => {\n-  // endpoint가 이미 /api로 시작하면 그대로 사용, 아니면 /api 추가\n-  const apiPath = endpoint.startsWith('/api') ? endpoint : `/api${endpoint}`\n-  return `${API_HOST}${apiPath}`\n+  // 앞쪽 슬래시 보장\n+  const cleanEndpoint = endpoint.startsWith('/') ? endpoint : `/${endpoint}`\n+  // /api로 시작하지 않는 경우 추가\n+  const apiPath = cleanEndpoint.startsWith('/api') ? cleanEndpoint : `/api${cleanEndpoint}`\n+  const fullUrl = `${API_HOST}${apiPath}`\n+  \n+  // URL이 올바르게 생성되었는지 로그로 확인\n+  console.log('API Request URL:', fullUrl)\n+  \n+  return fullUrl\n }\n \n interface CallApiPostParams {\n   title: string\n@@ -32,11 +39,9 @@\n \n   try {\n     const headers: Record<string, string> = {\n       'Content-Type': 'application/json',\n-      'Access-Control-Allow-Origin': '*',\n-      'Access-Control-Allow-Methods': 'GET, POST, PUT, DELETE, OPTIONS',\n-      'Access-Control-Allow-Headers': 'Content-Type, Authorization'\n+      'Accept': 'application/json'\n     }\n \n     // 쿠키 인증으로 변경 - 액세스 토큰 제거\n     // if (accessToken) {\n"
                },
                {
                    "date": 1756186689452,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,21 +1,24 @@\n import { pageLoaderController } from '@/contexts/PageLoaderContext'\n import { devLog, devWarn } from '../utils/devLogger'\n \n-// API 호스트 설정 (끝에 슬래시 제거)\n-const API_HOST = (import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535').replace(/\\/$/, '')\n-\n // API 경로 생성 함수\n const createApiUrl = (endpoint: string) => {\n   // 앞쪽 슬래시 보장\n   const cleanEndpoint = endpoint.startsWith('/') ? endpoint : `/${endpoint}`\n   // /api로 시작하지 않는 경우 추가\n   const apiPath = cleanEndpoint.startsWith('/api') ? cleanEndpoint : `/api${cleanEndpoint}`\n-  const fullUrl = `${API_HOST}${apiPath}`\n   \n-  // URL이 올바르게 생성되었는지 로그로 확인\n-  console.log('API Request URL:', fullUrl)\n+  // 개발 환경에서는 상대 경로 사용 (프록시를 통해 요청)\n+  if (import.meta.env.DEV) {\n+    console.log('Development API Request:', apiPath)\n+    return apiPath\n+  }\n   \n+  // 프로덕션 환경에서는 전체 URL 사용\n+  const API_HOST = (import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535').replace(/\\/$/, '')\n+  const fullUrl = `${API_HOST}${apiPath}`\n+  console.log('Production API Request:', fullUrl)\n   return fullUrl\n }\n \n interface CallApiPostParams {\n"
                }
            ],
            "date": 1756186422183,
            "name": "Commit-0",
            "content": "import { pageLoaderController } from '@/contexts/PageLoaderContext'\nimport { devLog, devWarn } from '../utils/devLogger'\n\nconst API_HOST = import.meta.env.VITE_PUBLIC_API_HOST || 'http://121.157.229.40:8535/api'\n\ninterface CallApiPostParams {\n  title: string\n  endpoint: string // url 대신 endpoint를 받도록 변경\n  body?: Record<string, unknown>\n  isCallPageLoader?: boolean\n}\n\nexport async function callApiPost<T = unknown>({\n  title,\n  endpoint,\n  body = {},\n  isCallPageLoader = false,\n}: CallApiPostParams): Promise<T> {\n  const url = `${API_HOST}${endpoint}`\n  devLog(`📱 [${title}]`, url, body)\n  if (isCallPageLoader) pageLoaderController.open()\n\n  let returnValue = ''\n\n  try {\n    const headers: Record<string, string> = {\n      'Content-Type': 'application/json',\n    }\n\n    // 쿠키 인증으로 변경 - 액세스 토큰 제거\n    // if (accessToken) {\n    //   headers['Authorization'] = `Bearer ${accessToken}`\n    // }\n\n    const response = await fetch(url, {\n      method: 'POST',\n      headers,\n      body: JSON.stringify(body),\n      credentials: 'include', // 쿠키를 포함하기 위해 추가\n      mode: 'cors', // CORS 모드 명시적 설정\n    })\n\n    returnValue = await response.text()\n    devLog(`📱 [${title}] 응답`, returnValue)\n  } catch (error) {\n    devLog(`❌ [${title}] API 요청 에러`, error)\n    returnValue = '[]'\n  } finally {\n    if (isCallPageLoader) pageLoaderController.close()\n  }\n\n  try {\n    return JSON.parse(returnValue)\n  } catch (e) {\n    devWarn(`⚠️ [${title}] JSON 파싱 실패`, e)\n    return [] as T\n  }\n}\n\n"
        }
    ]
}