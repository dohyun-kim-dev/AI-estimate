import { Editor } from "@tiptap/core";
interface MenuBarProps {
    editor: Editor | null;
}
declare const MenuBar: ({ editor }: MenuBarProps) => import("react/jsx-runtime").JSX.Element | null;
export default MenuBar;
