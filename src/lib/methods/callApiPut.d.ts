export declare function callApiPut({ title, url, body, isCallPageLoader, }: {
    title: string;
    url: string;
    body?: Record<string, unknown>;
    isCallPageLoader?: boolean;
}): Promise<any>;
