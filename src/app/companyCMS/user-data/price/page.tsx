import PriceListPage from '@/app/superAdmin/userData/price/page';

// Company CMS에서도 같은 PriceListPage 사용 (URL 기반 회사 코드 자동 추출)
export default function CompanyCMSPricePage() {
  return <PriceListPage />;
}
